[tool.black]
line-length = 120
target-version = ["py37"]
exclude = 'users'

[tool.ruff]
line-length = 120
target-version = "py38"  # https://github.com/rwth-i6/returnn/issues/1326
extend-exclude = [
    "users",
]

[tool.ruff.lint]
# Disable fix for unused imports (`F401`).
unfixable = ["F401"]
