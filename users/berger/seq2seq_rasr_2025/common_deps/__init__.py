"""
Vendored copies of `i6_experiments.common` code that the SLT 2026 search-space
comparison setup depends on but which is not (yet) present on the main branch.

Keeping these here means the setup is runnable from `users/berger` alone, without
waiting for a pull request against the shared `common` package. If the upstream
counterparts ever land in `i6_experiments.common`, this package can be dropped and
the imports repointed.
"""
