Putting some custom neural network modeling code here,
specifically using RETURNN frontend (RF)
(although the code could also be used outside of RETURNN).

Some more modeling code is in `..returnn.models`.
