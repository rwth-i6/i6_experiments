# SAE 4A -- the lexicon inside the marginalised lattice

## State

Watcher command `bash ~/.claude/skills/sis/sis_watch.sh <pid> <config> 600`; **re-arm all watchers first after any resume.**

LIVE managers: none. The D14, D15, D17 and D18 graphs are complete. Logs `log/<config name>.manager.log`.

Standing rulings: 2026-09-21 this phase is the main line, no bar moves to make a gate pass; 2026-09-23 every main-line method is pure unsupervised and GAN-free, GAN-lineage and supervised inits are analysis only. G4a.9 = CANNOT_TELL as written. Audited, no open item: E60 (PLATEAU), D10e (untrained phi drove the collapse; a gold phi anchors), D12 = ROOM, D13 (gold-init lower on every term, logged read).

READ 2026-09-24: D14 (k2 term HELPS and is LEXICON-SPECIFIC; the decoded-label phi is within 0.003 of gold; gold phi FROZEN BETTER at ep8); D15 warm TRIGRAM NEEDED, cold NOT NEEDED and not LEXICON-SPECIFIC; D17 sup arms HELPS AT THE EDGE, k2lat arms NO EFFECT, every arm still collapses. D16 and D16r read TWO BASINS (audited). D18 (descriptive): every arm is still descending. On one common objective, the frozen-phi arms are worse by 0.11-0.12 despite better PER, so training phi lowers the objective by co-adaptation. At beta 0, the objective prefers the worse-PER rp arm. Cold-line escape work lives in `SAE_4A_lexlat_v2.md`.

NEXT: none in this phase. The objective-vs-phi finding feeds `SAE_4A_lexlat_v2.md` A14 (ii) and A15; see that file's NEXT.

Open user forks: 1. k2 form on the cold line (random-pronunciation null; rung 10000): D15 (c) and lexlat_v2 bear on it. 2. D12 = ROOM: the word-aggregate arm waits on the user's go. 3. Double-counted text prior: D15 is the chosen ablation; the beta-2 control and the P3-divided graph are not run.

## Design review amendments (2026-09-21, applied before any job)

1. Gate takes G4a.4's form: PER < 0.50 and rate are PASS conditions, not a health precondition, so the likeliest outcome (a cold arm at PER 0.82–0.91) reads FAIL, not CANNOT_TELL.
2. No bit-equality claim across packs: arms run the banked `lattice.py` path before the on-set; step-1 loss match pre-launch; ep4 agreement of the in-pack lexicon-free arms defines the cross-pack floor F, which enters M.
3. Pruning: escape sub-budget C_esc = 64, stated finality, pruned mass reported as median / p95 / max with the NEG_INF count (one NEG_INF utterance aborts the arm).
4. E0 measured at lam_lex 1/3, 2/3, 1; the pruned-mass engagement clause counts from sub-epoch 11.
5. Null magnitude match read over sub-epochs 8–11 only; later divergence is the success signature.
6. E0 kill reads before any arm: augmented max-plus decode must be closer to gold than the plain decode (paired delta <= -0.010), and E[N] must not drop more than 10 % (else ADD is refused and the blend is re-registered).
7. S / D / I per arm at ep10 and ep20.
8. E1 pins ctrl_20 ep10 parameters and includes the largest-T batch; the cost table counts four arms.
9.7 (2026-09-22, AFTER the read, disclosed) Positive control (i) recalibrated. The original ten clauses stand as registered and their verdict FAIL (6 of 10) is preserved in Results "Positive control, first read". A debugger and an independent auditor (both fresh contexts) derived what a correct implementation meets at the operating point (tau 2.0, eps 0.02, max_active 1000): -L_lex must carry the sentence's word-LM cost divided by tau (delta-emission limit 1.219 nats per gold token, 4x clause 1's bar), the 3x ratios of clauses 4 / 6 sit above the escape cap, the escape word is about 20 nats per word dearer than tiling with the full pronunciation inventory the derangement null keeps (clauses 5 / 7), and the null shares H's run-collapse (clause 10). Amended control, the clauses a correct implementation must meet and this run does: (a) log Z_H equals the k2-free closed form within 1e-3 nats on all 100 gold utterances (measured 1.9e-12); (b) -L_lex per utterance <= log Z_H - (single-string exact score)/tau on >= 95 of 100 gold/treatment utterances (measured 99, worst excess +1.5 nats, the pruning direction); (c) orderings gold < cold < noise on both graphs and treatment < null on gold and noise; (d) the escape arc reachable with price = per-phone escape price + the <unk> LM arc (3,120 arcs per graph, start state carries them); (e) original clauses 2, 3, 8, 9 unchanged. Not covered by either check: a k2-free full H.L.G forward (the bound is one-sided) and the gradients beyond clause 9. `reports/debug_k2_positive_control_fail_2026-09-22.md`, `reports/audit_k2_positive_control_2026-09-22.md`.

Orchestrator note on the probe checkpoint (2026-09-21): E0 and E1 run at the BANKED ctrl_50 ep10 checkpoint, the Step 0 / falsifier operating point (same bed, same hold-phase LR, its decode and prior-gap rows already banked, so E0's kill read (a) compares directly with Step 0's private-code rows), because ctrl_20 ep10 does not exist until the prepro pack reaches it. Wherever the Design says "ctrl_20 ep10" for a probe, read ctrl_50 ep10; the arms' pairing against ctrl_20 is unchanged.

## Objective

Does a pronunciation lexicon with a word trigram, placed INSIDE the marginalised blank-free lattice objective so the forward-backward sums over word-constrained phone strings, move a cold blank-free arm out of the content-free band at the same budget?

What this builds on is a discrimination measurement, not a training result: the exact lexicon prices gold 2.32 nats per token above the private code where the live trigram prices it 1.39 (`SAE_4A_prior.md` Step 0, audited), and the audit attributes 2.17 of that 2.32 to lexical routing, 0.16 to the escape convention's fixed prices. A discrimination gap does not convert into a gradient: falsifier (ii) showed a sampled estimator never reaches strings in the lexical region, and falsifier (i) showed the lexicon reward nearly flat around the ep4 decode (std 0.50 nats per utterance; 5.2 % of decodes segment strictly against gold's 87.6 %). Marginalising the constraint removes the sampling problem -- every word-constrained path carries posterior mass by construction, so the term is felt at every frame with no string drawn.

A POSITIVE result (paired PER improvement over ctrl_20 beyond the seed band AND beyond the shuffled-pronunciation null) licenses the claim that the prior lever works when the constraint is marginalised rather than sampled, and funds a second seed and a scaling round; it licenses nothing about supervision cost or word decoding, neither of which this phase runs. A NEGATIVE result licenses "the marginalised lexicon at this bed, this pruning budget and this on-set does not improve PER, and prior-side work in this direction is not funded further"; it does not license "a lexicon in the objective cannot work". A result failing the health clause or the pruning monitors licenses neither.

## Constraints

- No transcripts, alignments or any label enter training or checkpoint selection (absolute, `SAE_ref.md` "Current research constraints"). Every label-using read below is a disclosed diagnostic entering nothing. Plain PER only, as scored, never a rescored variant.
- No new control. ctrl_20 and its seed band ctrl_20_s1 exist in the running prepro pack `PackedBlankfreeTrainJob.5EIGJJ1MkcO9` (Slurm 1921103, `SAE_4A_prepro.md` State) and are reused by utterance-paired reads at matched sub-epoch, exactly as G4a.8 reads them.
- Bed unchanged: blank-free trigram lattice DP over 39 ARPAbet phones + SIL, stride 3, band |s - 3t| <= 25, d_min 2, phone D = 25 / SIL D = 50, beta = 1, lambda_rate = 3, rho = 9.6619373279 Hz, lambda_agg = 0.1, batch 88,000 padded frames / max_seqs 128, theta LR 1e-4, phi LR 3e-3, partition_epoch 4, reduction/checkpoint stride 32 (`SAE_4A_blankfree.md` "Registered first model"). Schedule N = 20: anneal 4 sub-epochs tau 8 -> 2, LR warmup 2, hold to 12, linear decay to 20, kept 1 / 4 / 10 / 20, 601 s per sub-epoch, 3.34 h per arm (`SAE_4A_budget.md` "Sub-epoch count for future arms").
- Single delta: exactly one change against ctrl_20 per arm. One extra arm is allowed, for the on-set sub-epoch only (Design 5).
- The lexicon and the word LM are FIXED resources; nothing on the lexicon side is estimated, which is why the precedent's re-smoothing device is not carried (Design 4).
- No edit to any banked blank-free module while the prepro pack runs: running RETURNN jobs re-import the recipe tree on every resubmit, and adding a class to an existing file moves that file's `__sis_version__` and re-hashes its Jobs. New modules only (Design 8).
- G2P status, stated rather than assumed. The Step 0 trie is the phonemisation bliss lexicon (first pronunciation per word) PLUS a Sequitur G2P lexicon for the words it misses (`reports/survey_lexicon_scorer_2026-09-20.md` s1; `phon_lm.py:182-215`), and the bed's own prior text is phonemised through the same chain (`w2vu2/pipeline.py:138-145`, `PhonemizeWithSilJob(..., bliss_lexicon=lex, g2p_lexicon=g2p, ...)`), so G2P-derived pronunciations are already a text-side resource inside the live trigram prior. `SAE_ref.md` "Live reward: no G2P anywhere" is a source trace about the psi / GRPO reward path, not about this bed. The claim "the lexicon carries no G2P" is therefore NOT made here; probe E-1 prices a G2P-free trie and Open decision 1 asks the orchestrator to rule.

## Design (pre-registered 2026-09-21, before any job)

### 1. Scorer and state space

The bed's DP state is `fwd [B, O = 51, H = 1681, 2]`: batch x band offset o = s - 3t + 25 x phone trigram history 41 x 41 x CTC repeat flag, advanced by `_forward_step` (`lattice.py:861-902`), float64 accumulation in `_logmm` (`lattice.py:642-665`), TF32 disabled, finite NEG_INF floor, hand-written backward on detached tables (`lattice.py:1040-1160`); autograd through the expanded transition tensor was rejected at about 9 GB per utterance (`lattice.py:78-92`). Each new segment adds `[log q + beta log P3(k | h) + reverse_segment_score] / tau`.

The treatment replaces the fixed trigram-history axis by a DYNAMIC lexical-context axis of declared size C. A context c carries the trie node n_c (position inside a pronunciation), the word-LM state sigma_c (integer id: the longest matched suffix in the CSR back-off trigram), and the last two emitted phones h_c so the bed's trigram term is preserved exactly. On emitting phone k:

- `n' = child[n_c, k]`; if the child exists, the path continues inside a word at no word-LM cost.
- If n_c is a word end, a word close is an alternative branch: emit w at `log p_wordLM(w | sigma_c)`, sigma' = successor(sigma_c, w), n' = child[root, k]. Homophones are separate branches (19,682 words share a pronunciation, about 13 %; survey s1).
- SIL forces a word close, is never consumed inside a word, and closes an open escape -- Klejch's silence-as-boundary anchor, which `prior_gap.py:461-467` already implements.
- ESCAPE, verbatim from `prior_gap.py:235-237, 430-433, 1555`: one `<unk>` word-LM transition per contiguous non-SIL span plus, per phone, the live Witten-Bell order-1 phone log probability plus log 0.5. ESCAPE and not STRICT, because inside a marginalised sum a `-inf` on non-segmentable prefixes concentrates the forward mass on a vanishing set in the sub-epochs right after the on-set, which is Nuhn and Ney's zero-probability trap in another guise.

The added transition score is `lam_lex x (word-LM increment + escape increment)`, ADDED to `beta log P3(k | h_c)`, not replacing it (Design 3, Open decision 2). Resources: trie over 151,731 words / 132,049 distinct pronunciations, max pronunciation 33 phones; KenLM word trigram order 3 modified Kneser-Ney, 1,000,000 lines / 19,629,091 tokens / 3,302,936 distinct bigram types, `words_o3.bin` 274,266,991 bytes (survey s1, s4). A dense LM table is impossible (151,731 x about 3.45e6 states, about 2 PB); the CSR back-off form of the same binary, about 20M explicit n-grams, is a few hundred MB on device (survey s4).

Gradient: the lexical increment is constant in theta and phi -- nothing on the lexicon side is trained -- so it enters the manual backward exactly as `beta log P3` does today, as an additive per-transition constant. theta and phi receive gradient only through log q and the reverse segment score, reweighted by the lexicon-shaped posterior. The lexicon changes which paths carry mass, not the local derivatives.

### 2. Pruning, as a declared budget

Exact marginalisation is not on the table: Nuhn and Ney (ACL 2014, Table 4) measure exact forward-backward EM over a word-typed state space at 224.88 h for V = 200 with a bigram and state flatly that it is intractable above 200 word types; at V = 3,661 with a trigram only preselection search ran (19.68 h, accuracy 90.92 against beam's 91.16 at 15x the speed).

Declared budget, primary operating point: **C = 1024 lexical contexts per utterance per frame**, kept by highest forward mass (logsumexp over band offset and repeat flag), the rest floored to NEG_INF and the axis compacted. Design review amendment 3, fixed now: (a) an ESCAPE sub-budget of C_esc = 64 of the C slots is reserved for escape-state contexts, because escape paths are dearer per phone and pure mass ranking would prune them first, after which a SIL or the utterance end kills every mid-word prefix and log Z falls to NEG_INF; (b) finality is stated: at the utterance end only contexts at a word end, in an open escape or at the trie root carry final mass, mid-word contexts do not; (c) the census and the training log report pruned mass as median, p95 and max per frame and the count of utterances with log Z = NEG_INF (must be 0 in every sub-epoch; one such utterance is an abort of that arm). The pruned objective is optimised EXACTLY, as in beam EM; the forward sum is a lower bound on the unpruned log Z and is reported as such, not as an error on the true gradient. No LM-side preselection (Nuhn and Ney's B_LM = 50 / B_lex = 5) is carried: the trie already bounds the successor set at most max_len = 33 prefix matches per position times about 1.15 homophones (survey s4). The whole budget is C.

Where the survey does NOT carry over, stated explicitly: its measured 23-38 live states per position (`prior_gap.json:max_states`) is an exact-Viterbi count on a FIXED string, and its conclusion that "the state set per step is tiny compared to lattice.py's 51 x 1681" is derived for that case. Inside the marginalised lattice the label string is free, so the reachable (trie node, word-LM state) count per frame is bounded by the lattice, not by 38. C is therefore measured (probe E0), never assumed.

### 3. Curriculum: when the lexicon turns on, and how strong

Klejch et al. 2022 needed a prior curriculum (character bigram -> character 5-gram -> word trigram + grapheme lexicon) because the full composition "is not feasible to use from the beginning". Falsifier (i) measures the same thing from the other side: at ep4 the lexicon reward has std 0.50 nats per utterance around the decode and 5.2 % of decodes segment strictly; at ep10, std 2.41 and 23.7 % segment. Before the code has lexical structure the term has nothing to grade.

Fixed now: lam_lex = 0 for sub-epochs 1-7; linear ramp 0 -> 1 over sub-epochs 8, 9, 10; full lam_lex = 1 for 11-20. Ten sub-epochs at full strength; kept checkpoints 10 and 20 sit after the on-set, 1 and 4 before it. lam_lex = 1 is fixed and not swept: beta = 1 for the trigram, the lexical increment is in the same nats-per-phone units, and the gold magnitudes are comparable (-2.19 against -3.20 nats per token, Step 0). A sweep would be a second knob; the shuffled-pronunciation null is the control for the added prior weight (Design 6).

### 4. Normalisation and guards

- Per retained frame comes for free: the term lives inside log Z and the lattice term is already `l_tau = mean_b(-log Z_b / retained_b)`. No per-token mean appears anywhere, so the length exploit has no substrate (the `lm_prior_norm = "units"` sign guarantee). Unlike the score-function arm's A5, no separate normalisation is introduced.
- Anti-deletion guard, three parts. (a) The bed's rate term is unchanged (lambda_rate = 3, rho = 9.6619373279 Hz on the original-audio denominator) and is the standing guard. (b) The G4a.4 abort clause is RE-ARMED at the on-set: expected phone rate < 0.6 rho for 5 consecutive sub-epochs after the on-set reads FAIL (collapse). (c) New monitor `lexlat_expected_phones_per_word` (posterior-expected phones between word closes) must stay in [0.7, 1.4] x the lexicon's token-weighted mean pronunciation length -- a resource constant printed by the build job before any arm runs, not a result; the survey banks only max_len = 33, not the mean, so the number is filled from the build job and recorded before launch.
- No re-smoothing against uniform. Klejch's alpha = 0.9 and Nuhn and Ney's identical lambda = 0.9 exist because a LEARNED lexical table can hit zero and never recover. Nothing here is learned on the lexicon side, so the device has no target. Disclosed as a deliberate omission.

### 5. Arms (one pack, one arm per GPU, one exclusive 4-GPU node)

| arm | delta against ctrl_20 | why |
|---|---|---|
| `lexlat_20` | lexicon inside the DP, on-set sub-epoch 8, C = 1024, lam_lex 0 -> 1 over 8-10 | the treatment |
| `lexshuf_20` | identical, pronunciations permuted across words (fixed derangement, seed 0) | the null, and the prior-weight control |
| `lexlat_20_e5` | identical to `lexlat_20` with on-set sub-epoch 5, ramp 5-7 | the one extra knob |
| `lexlat_20_s1` | identical to `lexlat_20` with flat_seed 1 / random_seed 1 / random_seed_offset 1000 (the ctrl_20_s1 seed set) | the treatment's replicate; its ep4 identity check pairs against ctrl_20_s1 (ruling 4) |

Four arms fill the exclusive node. The extra on-set arm is justified and bounded: the on-set is the single constant the one speech precedent says decides the outcome, our own probe brackets it between "flat at ep4" and "awake at ep10", and it costs one GPU of a node that is exclusive anyway. It is the only second knob; if one arm must go, `lexlat_20_e5` goes, never the null.

Every arm keeps ctrl_20's seeds and data order (the second seed keeps ctrl_20_s1's), so the sub-epochs before each on-set are lexicon-free. Before the on-set every arm runs the BANKED `lattice.py` path, not the augmented DP at lam_lex = 0 (design review amendment 2: bit-equality across packs is not achievable, a forward-only rebuild already flipped 8 of 300 decodes, `SAE_4A_prior.md` falsifier (ii)). Pre-registered implementation checks: (a) pre-launch, the step-1 loss of `lexlat_20` matches ctrl_20's logged step-1 loss to 1e-4; (b) at ep4 the lexicon-free arms in the new pack (`lexlat_20`, `lexshuf_20`; `lexlat_20_e5` is already ramping at ep4 and is excluded) must agree with each other to within 0.001 paired PER, and their common paired offset from ctrl_20 is the cross-pack floor F entering M (Gate); disagreement between them is an implementation fault: the pack is stopped and the cause found before ep10 / ep20 are read. `lexlat_20_s1` is checked the same way against ctrl_20_s1.

### 6. Nulls and probes

**Shuffled-pronunciation null, as a PARALLEL ARM, not a pre-funding probe.** The permutation is a fixed-seed derangement of the word -> pronunciation assignment: the same 132,049 pronunciation strings, so trie topology and segmentable set are bit-identical, and the same word LM, so word n-gram marginals are unchanged; only the phone-string-to-word-identity correspondence is destroyed. It targets exactly the 2.17 of the 2.32 nats the Step 0 audit attributes to lexical routing. Three reasons it must be an arm: (i) the quantity being nulled is a paired PER delta at sub-epoch 20, which no probe can produce -- there is no gain to test before an arm runs; (ii) it doubles as the prior-weight control the ADD mixing needs, adding a term of comparable magnitude through the identical code path with no lexical content; (iii) it costs one GPU of an already-exclusive node, i.e. no marginal allocation. Magnitude match is measured, not assumed (`lexlat_term_mean`, reported for both arms). Disclosed limitation: with the trie unchanged the null preserves the "must decompose into dictionary pronunciations" constraint and isolates word-identity routing only; a null destroying segmentability too (random pronunciations at matched length) is named in Open decision 4 and is not funded here.

**E-1, pre-funding, CPU, about 10 min** (Step 0's whole job was 302.85 s on 4 CPUs / 16 GB). (a) The G2P question: re-run the Step 0 lexicon ESCAPE row with the trie restricted to bliss-lexicon entries only, reporting word count and gold-minus-private gap against the banked 151,731 / 2.32. (b) Equivalence: the new GPU trie DP in max-plus mode on the banked Step 0 strings must reproduce `prior_gap.best_segmentation` to 1e-4 nats per token on the ESCAPE row (gold -2.19, private -4.46). A failure of (b) stops the phase.

**E0, state-space census, GPU, about 1 h.** 100 fixed dev-other utterances at the banked ctrl_20 ep10 checkpoint; the trie DP at C in {256, 1024, 4096} and unpruned on the 20 shortest utterances, each at lam_lex in {1/3, 2/3, 1} (design review amendment 4: the ramp passes through these strengths and the pruning behaviour is read at each). Reported as the cost/quality curve the literature asks for (Shinozaki's beam-table analogue): per-frame pruned forward mass (median, p95, max), log Z = NEG_INF count and log Z per retained frame against C and against seconds per utterance. Funding rule for C = 1024, fixed now: median per-frame pruned mass < 0.05 at every lam_lex AND |log Z(1024) - log Z(4096)| < 0.05 nats per retained frame at lam_lex = 1 (about 3 % of the bed's lattice term, which sits at 1.73-1.85) AND zero NEG_INF utterances. If C = 1024 misses the rule, C is re-declared at the smallest measured value meeting it, the change recorded before launch, and E1 measured at that C. If no measured C meets the rule and also passes E1, the phase stops and reports the cost.

E0 also carries two kill reads on the same 100 utterances at ctrl_20 ep10, rules fixed now (amendment 6; these are the cheapest checks of the whole phase and run before any arm): (a) the augmented lattice's max-plus (Viterbi) decode at lam_lex = 1, C = 1024 against the plain bed's greedy decode, scored as PER against the MFA gold strings (disclosed label-using read): if the lexicon-constrained decode is NOT closer to gold than the plain decode (paired delta > -0.010), the constraint does not point toward gold at this operating point and the pack is not funded; (b) the posterior-expected phone count E[N] per utterance under the augmented lattice against the plain lattice: a drop of more than 10 % is confirmed deletion pressure from ADD's added prior weight, and the pack is not funded under ADD (the recorded fallback is the weight-preserving blend, a new pre-registration, not a silent switch).

### 7. Efficiency gate E1, BEFORE the pack is funded

Measured: seconds per step and peak GPU memory at the RUN shape (batch 88,000 padded frames, max_seqs 128, one GH200, lam_lex at full strength, C as declared by E0, parameters pinned to the banked ctrl_20 ep10 checkpoint so the posterior has the shape of a live run; amendment 8), over 100 steps sampled at four evenly spaced points of a sub-epoch under the schedule's own laplace ordering with a fixed recorded seed, plus the batch with the largest padded T of the sub-epoch, which sets the memory peak -- not the first 100 steps, since laplace sorting makes early batches short and the step rate periodic (standing user requirement, 2026-09-18).

Bar, fixed now: extrapolated **<= 1202 s per sub-epoch, i.e. <= 2.00 x the bed's 601 s**, and peak GPU memory **<= 80 GiB** against the per-arm marker of 96. Fail -> the pack is not funded at that C. Declared fallbacks, in order, each re-measured against the SAME bar: (1) C = 512; (2) the word BIGRAM CSR (151,731 states instead of about 3.45M, survey s4), which the banked word-bigram ESCAPE row prices at 0.03 nats per token of lost discrimination (2.287 against the word-trigram row's 2.321, `SAE_4A_prior.md` falsifier (i)); (3) stop and report. The bar is never moved to fit a measurement.

**User amendment 2026-09-21, second acceptance tier (the bar above is unchanged and stays the primary gate):** if a probe fails the 1202 s bar AND no further cost reduction is possible (every declared fallback and every registered implementation change measured and exhausted), the user accepts a pack whose 20 sub-epochs take up to 2-3 days of wall time. At 57 batches per sub-epoch that is **<= 8,640-12,960 s per sub-epoch, i.e. about 150-227 s per step**, memory clause unchanged (<= 80 GiB). Funding under this tier requires, before launch: (a) the exhaustion of the cost fallbacks recorded in Results with the measured number of each; (b) a resume test across the 11.5 h wall clamp with a real kill of the running arm (the pack would span 5-7 walls); (c) the tier and the measured step time named in the arm's launch entry. The DP core at 856 s per step (48,800 s per sub-epoch, about 11 days for 20) is outside this tier too; the k2 route is what it applies to.

### 8. Code plan

New modules only; no edit to `lattice.py`, `blankfree.py`, `blankfree_train_jobs.py` or any other banked blank-free module while the prepro pack runs.

- `sae/emc/lexlat.py`: flat int32 trie (`child[node, phone]`, `is_word`, `word_id`) built from `prior_gap.Lexicon`, the CSR back-off word trigram with integer state ids replacing `kenlm.State`, the escape price vector, and the augmented forward / manual backward. Mirrors `lattice.py` throughout: same NEG_INF floor, `_exact_fp32_matmul` with TF32 off, float64 accumulation, checkpoint stride 32, manual backward on detached tables.
- `sae/emc/lexlat_jobs.py`: `LexiconTrieBuildJob` (CPU; reuses `prior_gap.load_phonemization_lexicon` / `restrict_to_word_lm` verbatim so the word set matches the banked rows, rebuilds the word trigram with the identical `lmplz -o 3 --interpolate_unigrams True --discount_fallback` call, asserts |V| = 151,731 and 3,302,936 bigram types, and prints the trie node count and the token-weighted mean pronunciation length, neither of which the survey banks); `LexlatCensusJob` (E0); `LexlatEfficiencyProbeJob` (E1). Arms go through the existing `blankfree_pack_jobs` packer unchanged.
- `sae/emc/test_lexlat.py`: (a) lam_lex = 0 with pruning off reproduces `lattice.py`'s log Z to 1e-9 in float64 on a toy shape; (b) max-plus mode reproduces `best_segmentation` on the `test_prior_gap.TINY_LEXICON` fixtures AND on 50 banked Step 0 strings through the REAL trie and the real KenLM (a fixture test cannot catch a wrong call into a shared primitive); (c) the escape price identities ported from `test_prior_gap.py:158` (one `<unk>` per contiguous span, SIL never escaped, SIL closes an escape); (d) finiteness and rows-sum-to-1 on the posterior at the production dtype and shape (float32 tables, float64 accumulation), not only in fp64; (e) the permutation is a true derangement and leaves the trie arrays bit-identical.

Compute-heavy work is sisyphus GPU jobs only, no login-node script; launches are verified by the on-disk job dir, since `add_alias` does not put a job in the graph.

### 9. Disclosed differences from the literature precedent

1. Klejch et al. 2022 compose a GRAPHEME (spelling) lexicon over a LEARNED phone-to-grapheme table; ours is a pronunciation lexicon with nothing learned on the lexicon side. Their alpha = 0.9 re-smoothing, and Nuhn and Ney's identical lambda = 0.9, exist only because a learned table can hit zero. Not carried (Design 4).
2. Their curriculum has intermediate character 5-gram stages; ours is two-stage (phone trigram -> phone trigram + lexicon). Reason: Step 0 prices phone order alone at +0.27 nats per token (4-gram) against the lexicon's +0.93.
3. Their deletion penalty sits at DECODE; our anti-deletion guard sits in training, and the read is plain greedy PER with no decode-side penalty.
4. Their training LM was the 100k most frequent words on 20 minutes of speech with 50 random restarts, and the method failed outright on 3 of 7 languages (Swahili WER 105.3, Swedish 93.5, Hausa 70.8). We run 151,731 words on the 100 h bed with one seed. This is the main extrapolation risk, it is not mitigated here, and it is why a PASS needs a second seed.
5. Nuhn and Ney's preselection budget is not carried (Design 2): the trie does the preselection. Klejch's silence-as-word-boundary anchor IS adopted, and is already what `prior_gap.py` does.
6. Yang, Barkoczi, Schlueter and Ney (arXiv 2603.02285) propose exactly this loss shape, with no ASR experiments and a simulation at |X| = 4; it is not treated as evidence.
7. The strongest published argument AGAINST: wav2vec-U 2.0 attributes the disease to the generator "consistently producing the most common n-grams" and answered it with an AUDIO-side anchor, and Ni et al. 2025 measured a word-level prior concentrating error onto rare words (error counts exceeding occurrence counts in the low-frequency bins). Hence the frequency-stratified read is pre-registered, not optional.
8. Nobody has run the trigram-only against trigram-plus-marginalised-lexicon ablation with the same acoustic model and the same DP. That ablation is this phase.

### Design amendment 9, DRAFT (2026-09-21; user: "please already prepare for the code for final k2 arm jobs"): the k2 lexicon leg as a second graph

Status: pre-registered as a draft so the arm code can be written to it; it becomes binding only after the k2 probe PASSES the E1 bar on some graph and a design review of this amendment; no arm is launched before both.

1. **Term.** Per utterance, `L_lex = log Z_HLG(e / tau) - log Z_H(e / tau)`: the log-semiring total score of the pruned intersection of the recognizer's dense per-frame phone log-probs `e` (the same emissions the probe feeds, at the arm's own tau) with the priced HLG, minus the UNPRUNED total under `H` alone (the bed's min-duration phone topology, verified against `transcript_logprob` to 1e-4). `L_lex <= 0` is the log posterior mass of word-decomposable phone strings under the recognizer's tempered frame-wise posterior, ESCAPE included; the `Z_H` denominator makes it a probability and removes the emissions' free scale. Gradient to the recognizer only, through k2's autograd (the probe's backward). The bed's lattice term (recognizer + reverse + P3) is unchanged; the two graphs are ADDED, `loss += lam_lex x (-L_lex) / n_unit_frames`, reduced as the lattice loss is.
2. **Curriculum.** Design 3's `lam_lex` schedule verbatim (0 before the on-set, linear ramp, 1 after; both on-sets of the pack). Scale note: in the DP form lam_lex multiplied the word-LM increments per transition, here it multiplies a log-mass; both are log-probabilities of the same word constraint, so lam_lex = 1 is "the full constraint" in both, disclosed as a different scale.
3. **Pruning.** `max_active` = the smallest probe rung that PASSES the E1 bar AND E0's stability rule (|log Z(ma) - log Z(10k)| <= 0.05 nats per retained frame, median); search beam 20, output beam 8, min_active 30 as in the probe. Fixed before launch from the probe's summary, written into the config's constants block.
4. **Graph.** The graph the probe passed on: our 1M-line trigram, or an official model if only that one passes; the official-LM benchmark and perplexity table (State) inform the choice; the lexicon word set of the chosen graph is the arm's word set, disclosed. Escape HLG, never determinized (ruling above).
5. **Over-count bound, measured before launch.** The epsilon back-off double count (Results, k2 fixture: 0.064 nats) is measured on real strings: `LexlatK2OvercountJob` intersects the HLG with linear FSAs of (a) the gold and (b) ctrl_50 ep10 decoded phone strings of dev-other and compares the log-semiring string score with the exact CSR score of `lexlat.py` (E-1's path); reported per token, both string sets. Pre-registered acceptance: median over-count <= 0.05 nats per token; above it the term is read as an approximate word constraint and the arm is not funded without a further amendment.
6. **Empty lattices.** With pruning an utterance can lose every word-decomposable path (`Z_HLG = -inf`). Such utterances get no lexicon term for that step and are COUNTED (`lexlat_k2_n_empty`); more than 10 % of a batch empty raises (analogue of amendment 3), a sub-epoch mean above 2 % reads the arm UNINFORMATIVE at that rung.
7. **Rate term.** Unchanged and through the banked `lattice.py` passes: the k2 term is outside the DP, so `E[N]` is the expectation under the bed's lattice posterior, not a lexicon-shaped one; a disclosed difference from the DP form's reading of Design 4 (a).
8. **Null arm.** The shuffled-lexicon HLG (`LexlatHLGBuildJob(shuffled=True)`, Design 6's derangement) prices the null arm with the same term and the same constants.
9. **Monitors** per step: `lexlat_k2_lam`, `_term_mean` (per unit frame), `_n_empty`, `_lattice_arcs_per_frame`, `_lattice_states_per_frame`, `_expected_escape_words` and `_expected_words` from the pruned lattice's arc posteriors, `_sec` (CUDA-synchronised leg time). Gate reads are those of Design 4 / the Gate section, the DP-only monitors (`_pruned_mass`, `_contexts_per_frame`) replaced by the k2 ones named here.
10. **Environment.** The arm's train job runs under the k2-capable python (`returnn_python_exe` = the scratch env's python, a hashed job parameter); the scratch lease (90 days) is a launch-time hazard, and a project1-resident install of the same k2 build is required before any arm longer than a probe is funded.
11. **Reuse.** Pack arms, seeds, ADD mixing, pairing to the frozen ctrl decodes, `LexlatSelectionStatJob`, the `PriorGapAnalysisJob`s and the paired reads are those of round 2, unchanged; only the term's computation and the null graph differ.

### Amendment 9 BINDING and the launch round (2026-09-21, after the design review `reports/design_review_k2_amendment9_2026-09-21.md`; over-count 9.5 PASS audited, E1 PASS audited, both on `LexlatHLGBuildJob.cdcxYJMjiYj5`)

The draft above becomes binding with these changes, all registered before any arm job exists:

1. **Graph and constants.** Treatment graph = `cdcxYJMjiYj5` (word-boundary `#0` loops, escape, sil_prob 0.5, theta 0.0), named by job id; `MAX_ACTIVE = 1000`; `PROBE_SEC_PER_SUBEPOCH = 181.5` (probe `by7UYEjtYdel`). The null graph is the same compile with `shuffled=True` and the SAME placement, escape, sil_prob and theta; the runtime asserts these four fields of both graphs' `build.json` against the config's stated expectation (`shuffled` is the only field allowed to differ).
2. **Bar reading (9.3 amended).** E1's 1202 s is the COMPLETE update: bed 601 s + lexicon leg <= 1202 s per sub-epoch. In-house rung 1000: 783 s, rung 3000: 819 s, PASS. Under this reading the official 3-gram 3e-7 (1321 s) and 4-gram theta-5 (1582 s) FAIL; only the official 3-gram 1e-7 (1056 s) passes the time clause and sits at 59.7 GiB for the leg alone.
3. **Launcher.** The training job runs under the project1 copy `/e/project1/spell/wu24/envs/sae_k2/bin/python` (byte-identical to the scratch env, torch 2.7.1 / k2 1.24.4.dev20260921+cuda12.6; `reports/impl_k2_env_project1_2026-09-21.md`) as a pack-config constant separate from the build env; the k2 shared objects carry scratch RPATH entries, so the GPU check under that interpreter must show no `/e/scratch` mapping in `/proc/self/maps` before any pack is submitted. Frozen graph pins and the work tree resolve to scratch (90-day lease): disclosed; a PASS is reproducible only while the lease holds unless the graphs are copied and re-pinned.
4. **Pre-launch step probe (`LexlatK2StepProbeJob`, one GPU hour, replaces the DP route's E0 kill reads for this term).** The real train step at the run shape on the pinned ctrl_50 ep10 checkpoint over the 9 E1 batches with the arm's model args (chunk lattices held to the outer backward, Z_H and monitors inside the timed leg). Rules fixed now: (a) peak reserved <= 80 GiB, else per-chunk backward is implemented before launch; (b) full step: 601 + 57 x leg seconds <= 1202 s; (c) kill read, disclosed label-using: the pruned lattice's best path at rung 1000 against the bed's greedy decode, paired PER on the 100 E0 census utterances against MFA gold, delta <= -0.010 or the pack is not funded at this operating point; (d) E[N] under the HLG posterior is not measurable pre-launch (no token-start tags); the rate abort is the anti-deletion clause.
5. **Runtime monitors, hash-neutral:** `lexlat_k2_peak_reserved_gib`, `lexlat_k2_sec` (existing), `lexlat_k2_stability` (per sub-epoch, first chunk of the first batch, |log Z(1000) - log Z(10000)| per retained frame under no_grad), `lexlat_k2_empty_frac`, `lexlat_k2_expected_escape_words / lexlat_k2_expected_words`, `lexlat_k2_term_mean`. Abort on the 10 % empty-batch raise writes `lexlat_k2_ABORT.json` into the arm's work dir and exits 0, so the sibling arms' reads run and the aborted arm reads UNINFORMATIVE from its marker.
6. **Gate mapping (G4a.9 engagement clauses for the k2 form):** `lexlat_pruned_mass` -> `lexlat_k2_stability` median > 0.05 for 3 consecutive sub-epochs from 11 -> CANNOT_TELL at that rung; sub-epoch mean `lexlat_k2_empty_frac` > 0.02 -> UNINFORMATIVE at that rung; `lexlat_escape_phone_frac` -> escaped share of expected words, same 0.90 ceiling, disclosed as a different quantity; `lexlat_expected_phones_per_word` -> bed E[N] / `lexlat_k2_expected_words`, same band, disclosed cross-posterior proxy; the rate abort is binding. On-set-5 arms are lexicon-free at ep4 (lambda is 0 for epoch < on-set), so they ENTER the ep4 agreement check and F.
7. **Scale note with a number:** log Z_HLG per retained frame about -0.35 at the ep10 operating point, Z_H at most 0.61 per unit frame, so -L_lex per unit frame is 0.5-0.9 against the lattice term's 1.73-1.85: lam_lex = 1 adds a term of the DP form's order, as ruling 2 assumed.
8. **Bands and M (G4a.9 pre-registration, can only enlarge M):** B_ctrl = 0.001 (prepro State), B_k2 = |k2lat_20 - k2lat_20_s1|, B_null = |k2shuf_20 - k2shuf_20_s1| at ep20; M per comparison = max(bands of its two arms, |F|, 0.010); F per pack = the common ep4 offset of that pack's arms from ctrl_20; cross-pack comparisons take the ep4 paired delta between the packs' same-seed lexicon-free arms as F. PASS also requires the s1 pair to clear -M.
9. **VERDICT writer** of the probe corrected to the registered rung rule before any official probe is re-read.

**Launch round (user instruction 2026-09-21: 2-3 exclusive 4-GPU nodes).** Node 1, registered pack, graph `cdcxYJMjiYj5`, rung 1000: `k2lat_20`, `k2shuf_20`, `k2lat_20_e5`, `k2lat_20_s1`. Node 2, same graph and objective, a second pack config: `k2shuf_20_s1` (null replicate: B_null and the s1 PASS pair), `k2shuf_20_e5` (the e5 arm's null), `k2lat_20_ma3000` + `k2shuf_20_ma3000` (gate pair at rung 3000, 217.9 s leg; turns the pruning CANNOT_TELL into a paired read). Node 3, the OFFICIAL resources as a second objective. USER RULING 2026-09-21 (overrides the review's item 2 for this node): the official 4-gram is wanted regardless of the E1 primary bar; the second acceptance tier (Design 7 amendment: up to 2-3 days for 20 sub-epochs, memory clause <= 80 GiB unchanged) applies to it by the user's explicit instruction, the cost-fallback exhaustion precondition (a) waived by that instruction and disclosed; precondition (c) (tier and measured step time in the launch entry) stands, and (b) (resume test across the wall clamp) applies only if the step probe's projection exceeds one 11.5 h wall. Arms: `off4_k2lat_20` (official lexicon + official 4-gram, theta 5.0 pruned as built, DISCLOSED DEVIATION from the unpruned 4-gram, which the size guard declined at 391 GiB), `off4_k2shuf_20` (its derangement null), `off4_k2lat_20_s1` (second seed), `off3_k2lat_20` (official 3-gram pruned 1e-7 treatment: the word-LM order comparison, paired against `off4_k2lat_20` and ctrl_20). Projected from the all-states probes: 4-gram leg 981 s + bed 601 s = 1582 s per sub-epoch, 8.8 h for 20 sub-epochs, 69.7 GiB leg-only at rung 1000; the word-boundary rebuild cut the in-house leg 1.7x in time and 2x in memory, so the rebuilt official graphs are re-probed before launch. Pre-launch chain, launched before nodes 1-2 finish: word-boundary rebuilds of both official graphs, derangement null of the 4-gram, official trie + CSR-LM npz for the exact reference, over-count exact on both rebuilt graphs (acceptance 0.05 as registered; a FAIL on the official graph is disclosed, not a stop, under the user ruling), re-probe at rung 1000, step probe (memory clause binding: 80 GiB). Not funded: lam_lex arms (ruling 2), the random-pronunciation null (follow-up), rung 10000. Per-arm wall: 4.4 h (in-house 1000), 4.6 h (3000), about 8.8 h (official 4-gram, tier 2) and 6 h (official 3-gram 1e-7), each inside one 11.5 h wall on the probe projections.

**Implementation controls with a known answer (user request 2026-09-21: "which test did we run to prove the k2 implementation is without any error? can we maybe try a supervised init"; both DISCLOSED LABEL-USING DIAGNOSTICS that enter no gate and initialise no main-line arm).**

(i) Emission-level positive control, `LexlatK2PositiveControlJob` (1 GPU, < 1 h, before any node is funded): on the 100 E0 census utterances, gold-aligned emissions (MFA, mass 0.98 on the gold phone), the cold ctrl_50 ep10 posterior, and label-permuted gold emissions, each on the treatment graph `cdcxYJMjiYj5` and the null graph `UGE5MH3v5lyT`, at tau 2.0, rung 1000. Expectations fixed now: gold/treatment gives -L_lex < 0.3 nats per gold token, escape share < 0.10, zero empty lattices; gold/null and permuted/treatment give at least 3x that -L_lex and escape share > 0.5; cold in between; gradient of -L_lex on 0.7 gold + 0.3 uniform emissions pushes the gold phone up on > 80 % of frames on the treatment graph and on < 60 % on the null graph. PASS requires all; a FAIL stops the launch round for a cause.

(ii) Training-level positive control from a NEW supervised blank-free recognizer (user ruling 2026-09-21: train a fresh one, never convert the S2 bed's blank-symbol checkpoint `65NNK8Bwxdtd`). Supervised training of the bed's own recognizer (`SaeBlankfreeModelV1`'s recognizer, the bed's blank-free min-duration topology, loss = -transcript_logprob of the gold phone sequence) on the same labelled 10 h seed the S2g initializer used (`SeedGoldPhonesJob.zii9E9tvr51e`, `CvHoldoutSplitJob.sD7U6CYs8ACM` manifests, 2,821 train / 28 held-out), about 30 sub-epochs of 10 h, one GPU, the checkpoint with the best held-out loss selected (label-using selection, disclosed); its dev-other greedy PER is the diagnostic's starting point p0. Diagnostic pack (one exclusive node, 8 sub-epochs of the 100 h bed, kept 1 / 2 / 4 / 8, the lexicon term ON from sub-epoch 1 with ramp 1-3, everything else ctrl_20's constants): `sup_plain` (bed objective alone; precedent `SAE_4A.md` oracle diagnostic: the plain objective degrades an oracle recognizer 3x in PER by sub-epoch 4), `sup_k2lat` (+ k2 term, treatment graph), `sup_k2shuf` (+ k2 term, null graph), `sup_off4` (+ k2 term, official 4-gram word-boundary graph, once built). Expectations fixed now, paired dev-other PER at sub-epoch 8 (and 4): `sup_k2lat` - `sup_plain` <= -0.010 AND `sup_k2lat` - `sup_k2shuf` <= -0.010 reads the implementation and the term's direction CONFIRMED (the lexicon holds a good recognizer against the bed's own drift); `sup_k2lat` >= `sup_plain` reads the term inert or wrong (a cause is found before the main-line reads are interpreted); `sup_k2shuf` < `sup_plain` - 0.010 reads the added weight, not the lexicon, as the protector (the null is doing the work). `sup_off4` is read against `sup_plain` the same way. Cost: about 1 GPU-h for the supervised training, one node for about 2 h for the pack.

## Gate

**G4a.9** (pre-registered 2026-09-21, before any number exists; per arm, dev-other, read at sub-epoch 20, never best-PER over the kept set).

Form of G4a.4 (design review amendment 1: every cold arm of this bed sits at PER 0.82–0.91, `SAE_4A_budget.md` Results, so PER < 0.50 is part of the PASS condition, never a precondition for reading FAIL). Health clause: speaker-matched derangement gap > 0 (point estimate; the gap job emits no CI and the gap is positive in every content-free arm, so it cannot carry the decision).

Primary read: the utterance-paired PER delta (`PairedPerDeltaJob`) `lexlat_20` - `ctrl_20` at sub-epoch 20 on dev-other, plain PER, with S / D / I reported per arm at ep10 and ep20 (amendment 7). Band B = the paired delta `ctrl_20` - `ctrl_20_s1` from the prepro pack (full replicate band: init, order, sub-epoch composition). Cross-pack floor F = the common ep4 offset of the lexicon-free arms from ctrl_20 (Design 5, amendment 2). Margin M = max(|B|, |F|, 0.010 absolute PER); the 0.010 floor is fixed now so a band that comes back implausibly tight cannot manufacture a PASS.

- **PASS**: greedy PER < 0.50 AND greedy emitted rate in [5.80, 14.49] per second for `lexlat_20` AND `lexlat_20` - `ctrl_20` <= -M AND `lexlat_20` - `lexshuf_20` <= -M AND the health clause holds for `lexlat_20`. Both paired comparisons are required: beating ctrl alone is consistent with added prior weight, and the null is what separates the lexicon from the weight.
- **FAIL**: the health clause holds, the engagement monitors show the term was live, and any PASS condition is missed (including `lexlat_20` remaining at the cold band's PER). Licenses not funding the marginalised lexicon further at this bed; licenses nothing about a different pruning budget, on-set or bed.
- **CANNOT_TELL**: the health clause fails in BOTH `lexlat_20` and `ctrl_20`; or any engagement clause below fires; or E0 / E1 had to be re-declared in a way not recorded before launch; or the ep4 lexicon-free arms disagree with each other (Design 5).

Engagement clauses (label-free, per sub-epoch, in the training log), ceilings fixed now:

- `lexlat_pruned_mass` (median over utterances of forward mass discarded per frame by the C-truncation; p95 / max and the NEG_INF count are logged beside it) above 0.05 for 3 consecutive sub-epochs counted from sub-epoch 11 (full ramp; amendment 4) -> CANNOT_TELL at that budget; the next arm is a larger C, not a different conclusion.
- `lexlat_escape_phone_frac` (posterior-expected fraction of emitted phones inside an escape span) above 0.90 for 3 consecutive sub-epochs at full ramp -> the lexicon is inactive because every path escapes; UNINFORMATIVE, and the next arm raises the escape price.
- `lexlat_expected_phones_per_word` outside [0.7, 1.4] x the build job's token-weighted mean pronunciation length for 3 consecutive sub-epochs at full ramp -> the anti-deletion guard has fired; UNINFORMATIVE.
- `lexlat_term_mean` reported for `lexlat_20` and `lexshuf_20`; the magnitude match is read over sub-epochs 8–11 only (amendment 5): if the two differ by more than a factor of 2 in that window the null is not a magnitude-matched weight control, and the PASS clause's second comparison is reported as disclosed rather than decisive. Divergence after sub-epoch 11 is the expected signature of the lexicon taking hold and never demotes the null.

Abort rule, per arm, as G4a.4 and re-armed at the on-set: NaN, or |trained surrogate| > 100 in any sub-epoch, or expected phone rate < 0.6 rho for 5 consecutive sub-epochs after the on-set. An aborted arm reads FAIL (collapse) and is not restarted with a different constant.

Disclosed label-using reads at kept epochs 10 and 20, entering nothing:

- **Frequency-stratified PER.** Each gold phone inherits the word-frequency decile of its MFA word (deciles from the window text's word unigram counts, boundaries fixed before the read); substitutions and deletions attributed to the gold phone's decile, insertions to the preceding gold phone's decile; per-decile PER for `lexlat_20`, `lexshuf_20` and `ctrl_20`. This is what makes the frequent-word collapse visible in the first result.
- **Step 0 prior-gap rerun** (`PriorGapAnalysisJob` with each arm's decode as the private-code input), like-for-like pairing: the lexicon ESCAPE gap should shrink from 2.32 and the decode's strict segmentable fraction rise from 0.237 if the arm moved the code toward lexical structure.

Label-free reference read, as G4a.8: the wav2vec-U 2.0 selection statistic (4-gram phone-LM perplexity divided by the squared vocabulary-seen fraction, SIL stripped) per kept epoch.

A PASS is audited from a fresh context and needs a second seed before it is claimed (standing rule). A negative at n = 1 reads "no take-off in this seed".

## Cost

Per arm: 20 sub-epochs at the bed's 601 s = 3.34 h if the trie DP costs the bed's rate; 6.68 h at the E1 bar of 2.00x. Arms run one per GPU in one exclusive 4-GPU allocation (`blankfree_pack_jobs`: 4 arms max, per-arm cpu 16 / mem 64 GB / gpu_mem 96, `--exclusive`), so pack wall time is the longest arm: 3.3 h to 6.7 h, inside the 11.5 h clamp with no resume path.

| item | work | charged (exclusive 4-GPU node) |
|---|---|---|
| E-1 (CPU: G2P read + max-plus equivalence) | ~0.2 h CPU | 0 GPU-h |
| E0 census (C curve) | ~1 h on 1 GPU | 4 GPU-h |
| E1 efficiency probe (100 sampled steps) | ~0.5 h on 1 GPU | 2 GPU-h |
| pack of 4 arms (one exclusive node, arms in parallel) | 4 x 3.3 h .. 4 x 6.7 h of GPU work, 3.3 .. 6.7 h wall | 13.4 .. 26.7 GPU-h (the node is charged whole) |
| registered reads (PER with S/D/I, rate, gap, paired delta, prior-gap rerun, selection statistic; 4 arms x 4 kept epochs) | ~2.5 h | ~10 GPU-h |
| **phase total** | | **about 29 GPU-h at the bed's rate, about 43 GPU-h at the E1 bar** |

The second seed sits in the fourth slot (ruling 4), so a PASS needs no second round.

## Orchestrator rulings (2026-09-21, on the draft's five open points, before design review)

1. **G2P in the trie: keep the Step 0 trie verbatim** (bliss lexicon + Sequitur G2P fallback). The bed's own prior text is phonemised through the same chain, so G2P-derived pronunciations are already a text-side resource of the live trigram prior; `SAE_ref.md` "Live reward: no G2P anywhere" is the source trace of the retired grapheme scorer, not a rule for this bed. The G2P-derived subset is disclosed as a resource; E-1(a) prices a G2P-free trie as a disclosed read, not a stop.
2. **Mixing: ADD** (`beta log P3 + lam_lex x lexical increment`, lam_lex = 1, no sweep). The shuffled-pronunciation arm is the prior-weight control; a blend would remove part of the trigram, a second change.
3. **Word-LM order: trigram primary, bigram as the declared E1 fallback** (banked price 0.03 nats per token). The standing "prior order above bigram" rule holds either way because the phone trigram stays in the objective.
4. **Fourth GPU slot: the second seed of the treatment, `lexlat_20_s1`** (flat_seed 1 / random_seed 1 / random_seed_offset 1000, mirroring ctrl_20_s1), so a PASS arrives with its replicate. The segmentability-destroying null is named as a follow-up, not funded here.
5. **On-set: both arms kept** (8 primary, 5 as the one extra knob). If a slot must go, `lexlat_20_e5` goes, never `lexshuf_20` or the second seed.

Rulings after the round-2 review (2026-09-21):
6. **Prior-gap rerun at kept epochs 10 and 20 only**, as the Gate's read section writes it (the review's "every kept epoch" was the orchestrator's brief, not the phase file); 12 `PriorGapAnalysisJob`s, not 24.
7. **Selection statistic uses the banked Step 0 4-gram KenLM MKN model pinned by content sha** from a surviving byte-identical copy (the model lives only in cleanup-eligible work dirs); the job raises rather than refits if every copy vanishes, since a new fit would be a different reference. Any refit needs its own registration from the unbiased window.
8. **E1 runs in parallel with E0 at the pre-registered C = 1024** rather than after E0's verdict: it costs about one GPU-hour and gates nothing by itself; if E0 declares a different C or a fallback, E1 is re-measured at that C before any funding.

## Results

### Resource constants (`LexiconTrieBuildJob.rlMsnTBSZXsB`, FINISHED 2026-09-21, `output/.../sae_4a_lexlat/resource/summary.txt`; the review fixes left this job's hash unmoved)

| constant | value | reference |
|---|---|---|
| words in the trie / distinct pronunciations / max length | 151,731 / 132,049 / 33 | survey s1, all matched |
| trie nodes | 291,476 (151,731 word ends) | new |
| mean pronunciation length, token-weighted (type-weighted) | 3.579 phones (6.486) | new; fixes the Design 4 monitor band `lexlat_expected_phones_per_word` in [2.505, 5.010] |
| word trigram | 151,734 unigrams, 3,393,577 bigrams, 10,419,405 trigrams, 3,545,312 CSR states, 274,266,991-byte binary | survey s4 (binary size matched) |
| distinct bigram types | 3,302,936 in-line (banked; with BOS 3,335,328, with BOS+EOS 3,393,577) | survey s4, matched by the in-line convention |
| shuffled-pronunciation null (seed 0, single-cycle derangement) | 0 fixed points; 1 word of 151,731 keeps a homophone of its own pronunciation; trie arrays bit-identical | Design 6 |

The monitor band is now a number written before any arm runs, as Design 4 requires.

### E-1, CPU equivalence probe (`LexlatEquivalenceProbeJob.lq61PSAg1DcC`, FINISHED 2026-09-21, Slurm 1922783, 42 min; `output/.../sae_4a_lexlat/equivalence/summary.txt`; executor hand-back of 2026-09-21)

Operating point: banked ctrl_50 ep10 greedy decode of dev-other, all 2,864 utterances, ESCAPE
convention, same word trigram as the banked Step 0 row.

| read | value | rule / reference | verdict |
|---|---|---|---|
| (b) max-plus vs `prior_gap.best_segmentation`, worst per-token disagreement | 2.3e-07 nats | <= 1e-4, else phase stops | PASS |
| gold, per token (lexlat / prior_gap) | −2.1945 / −2.1945 | banked −2.19 | reproduced |
| private, per token (lexlat / prior_gap) | −4.4613 / −4.4613 | banked −4.46 | reproduced |
| gold − private, pooled per token (lexlat / prior_gap) | 2.2667 / 2.2667 | banked like-for-like column 2.32 (per-utterance aggregation, `SAE_4A_prior.md` Step 0 table); the pooled difference of the banked per-token means is 2.27, so no discrepancy | same aggregation agrees |
| (a) G2P-free trie (Bliss pronunciations only): words | 133,942 | full trie 151,731 (17,789 G2P-only words) | disclosed |
| (a) G2P-free trie: gold / private / gap | −2.1965 / −4.4670 / 2.2706 | full trie on the same run 2.2667 | +0.004 nats per token, no loss |

Reading: the GPU-side trie, CSR word trigram and escape pricing are the banked Step 0 scorer to
float precision, so every E0 and training number is on the banked scale. The G2P-free trie
discriminates gold from the private code no worse than the full trie (its gap is 0.004 larger),
so a G2P-free variant would cost nothing on this read; the phase keeps the full trie as
pre-registered (the difference is below the seed spread of anything downstream). The probe's own
"delta −0.0494" line compares against the like-for-like 2.32 and is not a like comparison.

### E0, state-space census (`LexlatCensusJob.RDMTvt4ngAPB`, FINISHED 2026-09-21, Slurm 1922956, 36 min; `output/.../sae_4a_lexlat/e0_census/summary.txt`, `census.json`, `per_utt.json`; executor hand-back `reports/exec_lexlat_e0_check_2026-09-21.md`)

Operating point: banked ctrl_50 ep10, 100 fixed dev-other utterances, tau 2.0; the unpruned
ceiling (max_contexts 16384) on the 20 shortest utterances only, so its log Z column is not
comparable to the 100-utterance cells. Third run after two memory fixes (State); values are
bit-identical to the unchunked DP by construction.

| cell (lam_lex = 1) | pruned mass median / p95 / max | log Z per retained frame | s per utt (B = 1) | contexts per frame | NEG_INF |
|---|---|---|---|---|---|
| C = 256 | 0.0000 / 0.0006 / 0.0644 | −2.6507 | 0.69 | 253 | 0 |
| C = 1024 | 0.0000 / 0.0001 / 0.0179 | −2.6095 | 0.77 | 1009 | 0 |
| C = 4096 | 0.0000 / 0.0000 / 0.0021 | −2.5594 | 2.07 | 4032 | 0 |
| ceiling 16384 (20 shortest) | 0.0000 / 0.0000 / 0.0000 | −2.5652 | 8.78 | 15646 | 0 |

At lam_lex 1/3 and 2/3 the medians are 0.0000 in every cell and the p95 stays below 0.005
(summary table). Funding rule as written (Design 6): median pruned mass < 0.05 at every lam_lex
holds for every C and NEG_INF is 0 everywhere; the log Z gap to C = 4096 at lam_lex = 1 is
0.0913 (C = 256) and **0.0512 (C = 1024), above the 0.05 bar**. Verdict: **C = 1024 misses the
rule; C is re-declared at 4096**, the smallest measured value meeting it (recorded here before any
arm launches, as Design 6 requires), and E1 is measured at C = 4096 (ruling 8: the C = 1024 E1
already queued stays as the disclosed lower point). The gap at 1024 is 0.0012 above the bar and
the rule is not moved to fit it.

Kill read (a), disclosed label-using read: augmented max-plus decode at lam_lex = 1, C = 1024
against the plain greedy decode, paired PER delta −0.0509 (sd 0.0646, median −0.0381, 76 better /
9 worse; pooled 0.9095 → 0.8604). Rule: delta ≤ −0.010. **PASS.** Control row (disclosed, not the
rule): the same Viterbi decode at lam_lex = 0 gives −0.0330 (65 / 13, pooled 0.8760), so 0.033 of
the 0.051 is the decode-side trigram Viterbi over the same lattice and 0.018 is what the lexicon
adds on top; the lexicon-specific part alone would also clear the 0.010 magnitude, but that
subtraction was not the pre-registered rule and is recorded as interpretation only.

Kill read (b): E[N] 54.06 → 48.63 under the augmented lattice, mean drop 9.66 % (non-SIL 9.99 %,
max 19.69 %). Rule: drop > 10 % refuses ADD. **PASS, marginal** (0.34 points under the bar): the
deletion pressure the design anticipated is real and sits at the edge; the training-side
anti-deletion guard (Design 4) and the per-arm E[N] monitor carry it, and the ep10 / ep20 S / D / I
read (amendment 7) is where it will show if it grows.

Monitors at the primary operating point (Design 4 bands): phones per word 2.630 (band
[2.505, 5.010], near the low edge), escape phone fraction 0.649 (clause > 0.90 not tripped: 65 %
of the ctrl_50 ep10 decode's phones lie in escape spans, i.e. the code is mostly non-lexical at
this point, as Step 0's 0.237 strict-segmentable fraction already said), term mean −224.2 nats per
utterance (−0.896 per retained frame).

Reading: the pruned DP is well-behaved (no NEG_INF, negligible pruned mass at every C) and the
augmented lattice's mode is closer to gold than the greedy decode, so the term points the right
way at this operating point; both kill reads pass, one of them at the margin. The cost of the
declared C is the open question: 4096 contexts cost 2.7× the per-utterance time of 1024 at B = 1,
and the E1 bar (≤ 1202 s per sub-epoch, ≤ 80 GiB) decides whether the phase is affordable there.
No audit yet: E0 declares an operating point and gates spend; the audited claim is G4a.9.

### E1, efficiency probe at the run shape (`LexlatEfficiencyProbeJob`, speech-llm d2f6a5e; bar Design 7: <= 1202 s per sub-epoch on the sum of full-step times, <= 80 GiB)

Run shape in both rows: 88,000 padded frames, max_seqs 128 (B = 114 to 127 per batch), one GH200, lam_lex at full strength, parameters pinned to ctrl_20 ep10 (amendment 8), timed set = largest-T batch + 8 sampled batches (max_timed_batches = 9, disclosed deviation from the 100 of Design 7), STEP_ABORT_SEC = 1200.

| C | job / Slurm | outcome | time clause | memory clause | source |
|---|---|---|---|---|---|
| 4096 (declared by E0) | `fY18YN7LVdAe` / 1923324, 2026-09-21 07:56 to 08:26 | CUDA OOM on batch 0 in the forward gather (`lexlat.py:1256 _step`, b = 114, n_new = 4096, m_max = 6503): 72.68 GiB held, 22.62 GiB more requested on a 95 GiB device; no step timed, no partial.json | not measurable | **FAIL** (> 95 GiB needed, bar 80) | `reports/exec_lexlat_e1_c4096_read_2026-09-21.md` |
| 1024 (disclosed lower point, misses E0's log Z rule by 0.0012) | `x3LaY6KlB6I4` / 1923286, cancelled by the user's pause after 5 of 9 timed steps | step 0 (T 770, B 114, m_max 3840) 867.0 s; step 1 (T 689, B 127) 911.6 s; step 14 (T 687, B 128) 874.7 s; step 15 (T 764, B 113) 826.2 s; step 28 (T 615, B 128) 802.3 s; alloc 38.7 / 37.9 GiB, reserved 41.1 GiB; partial.json n_measured = 5, aborted_at null; no summary written | **FAIL on the measured sample** (incomplete statistic, disclosed): 802–912 s per step, mean 856 s, against a budget of about 21 s per step; 57 batches extrapolate to about 48,800 s per sub-epoch, 41× the 1202 s bar | met (41 GiB) | `reports/exec_lexlat_e1_c4096_read_2026-09-21.md` section 6; `reports/exec_pause_lexlat_kill_budget_2026-09-21.md` |

Reads. The bar is not met at the declared C on memory alone, and the disclosed lower point misses the time clause by roughly 40x per step even before its statistic is complete; the earlier truncated wall clock (>= 556 s per step at C = 1024, first attempt) is confirmed by the clean timers. Fallback (1), C = 512, is skipped: E0's rule already rejects C = 1024. Fallback (2), the word-bigram CSR, is read provisionally as unable to pass (per-step cost is set by C x m_max x O per frame, which the LM order does not change); this reading goes to audit before the phase is stopped. Per the standing rule, an E1 FAIL licenses "not funding the pack at this cost", not "the lexicon would not have helped": E0's kill reads (max-plus decode -0.051 PER against greedy, lexicon share -0.018) remain the phase's only evidence on the lexicon's value, and they are read-side, not training evidence.

### Cost analysis: why the step costs 850 s and what the literature says closes it (2026-09-21, `reports/lit_gpu_lexicon_search_2026-09-21.md`; profile job pending)

Findings that change the plan (full texts read unless marked):
- **The gap is the arc count, not the kernels.** Our step touches about C × m_max × O ≈ 2e8 arcs per utterance-frame at C = 1024. The published exact forward-backward with gradients on GPU runs at about 4.4e9 arc-updates per second (Ondel et al., ICASSP 2022, arXiv 2112.00709: 11.6 µs per utterance-frame at 51k arcs, batch 128 × 700 frames); at that rate our arc count predicts a step of the order of the 850 s measured. Re-chunking or float32 alone cannot close a 40× gap; only fewer arcs can.
- **Pruned forward-backward WITH gradients at a bounded state count exists as tooling, unpublished at our scale.** k2's `intersect_dense_pruned` is an autograd function over a shared HLG with a dynamic beam targeting `max_active_states` (icefall MMI: 10,000 states per utterance-frame, search beam 20), followed by `get_tot_scores(log_semiring=True)`; the search itself is max-plus pruned, the marginal is over the retained lattice ("not exact due to pruning"). Nobody publishes its per-frame cost at beams of 1k–10k; it is measurable.
- **Max-plus (beam) search over the same space is 40–75× INSIDE our budget.** GPU WFST decoders run at 3.3–6.8 µs per utterance-frame at max-active 10,000 (Braun et al., ICASSP 2020, arXiv 1910.10032) against our budget of about 250 µs per utterance-frame (21 s per step over ~120 × 700). No gradients; this is the decode-and-self-train use of the lexicon, i.e. what wav2vec-U 2.0 actually does with its lexicon and word LM.
- **Techniques the published systems rely on that this design lacks:** arc-level pruning AFTER expansion with a dynamic beam targeting a state count (ours prunes contexts BEFORE expansion and then materialises every candidate × symbol); LM look-ahead / weight pushing; float32; a batched CSR LM query; a forward-backward that is O(states) in memory rather than materialising O(arcs) (LAST, arXiv 2304.13134: 1e6 states / 33e6 arcs per utterance at 148 µs per utterance-frame in 278 MB). Not read: Klejch 2022 full text, Ortmanns and Ney look-ahead, GPU CTC prefix beam search, the flashlight decoder paper.

Consequence for the phase: the current DP core is not repairable to the bar by implementation changes. Two routes remain, each needing its own registration before any arm: (A) a pruned marginal with gradients over a standard H·L·G (min-duration phone topology · pronunciation trie · word trigram) through k2 at max_active 1k–10k, with the lexicon leg measured against the SAME E1 bar (≤ 21 s per step, ≤ 80 GiB) on the same E1 batches; this changes the objective's mixing (a second graph's log Z instead of ADD inside one lattice), so a design amendment is required if the probe passes; (B) the wav2vec-U route: max-plus lexicon + word-LM decoding of the current policy and a self-training stage on the pseudo-labels, no marginal gradient. Neither is funded yet; the profile job first confirms where the 850 s go.

### Official-LM benchmark, first reads (2026-09-21, `config/sae_4a_lexlat_k2_official.py`, manager 857002; `output/.../sae_4a_lexlat_k2_official/`)

Word perplexity of the dev REFERENCE transcripts (`WordLmPerplexityJob.ddxHefQ0vl2D`, FINISHED; text-only, disclosed reference use; conventions in `perplexity/summary.txt`). Descriptive, no gate: this is the LM-quality gap between our G and the standard. Column = ppl over the common in-vocabulary subset (99.45 % / 99.23 % of running words), OOV terms excluded; OOV rate over running words.

| LM | order | dev-clean ppl | dev-clean OOV % | dev-other ppl | dev-other OOV % |
|---|---|---|---|---|---|
| ours, 1M-line word trigram | 3 | 244.85 | 0.531 | 220.65 | 0.764 |
| official 3-gram.pruned.1e-7 | 3 | 244.27 | 0.349 | 222.92 | 0.579 |
| official 3-gram.pruned.3e-7 | 3 | 301.58 | 0.349 | 274.01 | 0.579 |
| official 4-gram, full | 4 | 148.42 | 0.349 | 137.34 | 0.579 |

Reading: our trigram matches the official pruned.1e-7 trigram in perplexity (within 1 %) and is 22 % better than the pruned.3e-7 model; the full 4-gram is the only substantially stronger LM (about 38 % lower perplexity). Our OOV rate is 0.2 points higher (151,731-word vocabulary against 200,000).

HLG on the official 3-gram.pruned.3e-7 (`LexlatOfficialHLGBuildJob.YJsdBTcEQJz9`, FINISHED in under 3 min): full LM at theta = 0, escape priced with the phase's own prices, determinized False; L 1.22 M states / 2.84 M arcs, G 1.22 M states / 3.92 M arcs, HLG 9.78 M states / 81.6 M arcs (against 24.7 M / 143.9 M for our trigram's HLG). Its probe `LexlatK2ProbeJob.R7QzD6vYBLD3` (Slurm 1932516) was submitted by the manager. The 1e-7 and 4-gram builds are running.

### Profile of one lexicalised step at C = 1024 (`LexlatStepProfileJob.ItFHebFGK6nb`, FINISHED 2026-09-21, Slurm 1929752; `work/.../LexlatStepProfileJob.ItFHebFGK6nb/output/summary.txt`, `profile_top_kernels.txt`, `trace.json.gz`)

Value-identical to E1 (loss rel delta 1.7e-16 against the reference, both batches); the timers cost nothing (873 s phase-timed vs 877 s un-instrumented). Batch 0 (B = 114, T = 770, 87,780 padded frames) and batch 1 (B = 127, T = 689) agree on every share.

| quantity | batch 0 | batch 1 |
|---|---|---|
| lexicalised step | 873 s | 930 s |
| banked trigram-only step | 12.3 s | 12.3 s |
| ratio | 71x | 76x |
| `lexlat_forward_backward` calls per step | 3 × 291 s (loss DP + the rate term's two central-FD tilted passes, each through the augmented DP) | 3 × 310 s |
| forward leg per DP call (frame loop + top-C prune + trie expand) | ~125 s | ~133 s |
| checkpoint replay in the backward (a second forward) | ~125 s | ~133 s |
| backward proper (logmm 53 s, slot gathers 7 s) | ~40 s | ~45 s |
| peak reserved | 41.1 GiB | 41.6 GiB |

Where the time goes (phase table, exclusive seconds, batch 0): `slot_gather` 192 s in the forward and 192 s again in the replay (22 % each), the per-frame `frame_step` body 157 s each (18 % each), `logmm` in the backward 53 s (6 %), top-C pruning 14 s each (1.6 %), trie expansion 5 s each, the CSR word-LM query under 1 s. Everything else is below 1 %; UNACCOUNTED 0.2 s. torch.profiler over one step: GPU busy 96.7 %, 3.88 M kernel launches (2,518 per frame step), 860 s of self CUDA time; by kernel, `where` 30.5 %, float64 `add` 18.2 %, `gather` 12.4 %, `exp` 7.8 %, `fill` 6.0 %, `amax` 4.5 %, `sum` 3.5 %; by op, `logsumexp` 53 % and `where` 37 %; the GEMMs (`_logmm`) under 1.5 %.

Reading. The step is not launch-bound (GPU busy 97 %) and not GEMM-bound; it is memory-bandwidth-bound elementwise work (masking, log-sum-exp, gathers) over the expanded [B, C, m_max, O] float64 candidate tensor, exactly the arc count the Cost analysis predicted. Three DP passes per step are structural (the rate term's central finite differences must go through the same augmented DP; Design 4), so the lexicon leg's cost is 3 × (2 forward-equivalents + backward). The implementation levers that keep the value identical or nearly so (float32, ≤ 2x; a fused where/log-sum-exp kernel, ≤ 2–3x; dropping the checkpoint replay by holding the forward tables, 1.5x at more memory) multiply to well under the 41x the bar requires (873 s against 21 s), and the one-sided FD mode would remove only one of three passes. This settles the E1 question for the DP core: the bar is unreachable by implementation changes, as the Cost analysis concluded; the live cost test is route A (the k2 probes, our trigram and the official LMs).

### Round chronology (moved from State 2026-09-21)

Facts that lived only in State before it was shortened; all dated 2026-09-21. Numbers already in the sections above are not repeated here.

- **Phase opened** on the closure of `SAE_4A_prior.md` Step 0b (no phone LM trained on this text reaches the Step 0b bar: gaps 1.71 / 1.861 / 1.849 / 1.809 against the bar 2.01) and of its score-function arm, which falsifier (ii) left unfunded (r(gold) exceeds every one of G = 8 posterior draws in 100 / 100 / 97 % of utterances at ep1 / ep4 / ep10, above the pre-registered 95 % rule; `SAE_4A_prior.md` Results, audited in `reports/audit_sf_probe_2026-09-21.md`). Design drafted in `reports/draft_lexlat_phase_2026-09-21.md`; design review `reports/design_review_lexlat_2026-09-21.md` returned APPROVE_WITH_AMENDMENTS with no ruling overturned, and all eight amendments were applied in place before any job.
- **Round 1** (speech-llm 319ce90; new modules only: `sae/emc/lexlat.py`, `lexlat_jobs.py`, `test_lexlat.py`, `configs/config_sae_4a_lexlat_probes_v1.py`, shim `config/sae_4a_lexlat_probes.py`; `reports/impl_lexlat_r1_2026-09-21.md`): 31 tests pass, lam_lex = 0 reproduces `lattice.py` log Z to 4e-16, string mode reproduces `prior_gap` to 3e-7 per token, manual backward vs autograd 3e-16. Code review `reports/review_lexlat_r1_2026-09-21.md` (spec-faithful; escape parity, CSR back-off, reservation, finality and plumbing verified) found a crash in the E0 unpruned cell (arc guard below the 16384-ceiling candidate count), a wrong `exact` flag and a missing lam_lex = 0 Viterbi control row for kill read (a); fixed in speech-llm 4c451a3 (`reports/impl_lexlat_r1_fixes_2026-09-21.md`), only E0 re-hashed (to `RDMTvt4ngAPB`). Deferred to round 2: a pruned-path autograd test.
- **E0 memory fixes** before the census ran (`reports/debug_lexlat_e0_oom_2026-09-21.md`, `reports/exec_lexlat_probes_relaunch_2026-09-21.md`): the unpruned ceiling cell held 112 GiB in the `torch.where` expansion of `lexlat._slot_u` on a 95 GiB device while the guard bounded a proxy four times smaller; speech-llm a05ab16 chunked the expansion over the destination axis and 299922f made the block width adaptive per frame, both bit-identical and hash-neutral (`lexlat.py` is imported at run time, not hashed). The debugger's note stands: the ceiling cell is a reference, not an exact log Z.
- **Round 2** (speech-llm f0ab314 + 6cbb3f9; `reports/impl_lexlat_r2_2026-09-21.md`): `sae/emc/lexlat_train.py`, `lexlat_train_jobs.py` (E1), `test_lexlat_train.py`, `configs/config_sae_4a_lexlat_pack_v1.py`; a default-off `lexlat_*` block in the blank-free model definition and train step, pre-on-set loss and gradients `torch.equal` to the banked path at both on-sets, NEG_INF after on-set ends that arm (amendment 3), live census unchanged; pack `PackedBlankfreeTrainJob.XaDOVLQZkzgh`, E1 `LexlatEfficiencyProbeJob.r4Iaa72mU27T`. Review `reports/review_lexlat_r2_2026-09-21.md` (DONE_WITH_CONCERNS) found an E1 shim exporting no `py` (empty graph), a destination block width ignoring B (~60 GiB at the training shape), an E1 statistic on the median and two unregistered reads; fixed in speech-llm b39ce3c (8 GiB byte-budget block width, bit-identical; the sum-of-full-step-times rule; `LexlatSelectionStatJob.3HXluQmd65EE` and the 12 `PriorGapAnalysisJob`s added to the pack graph; hashes unchanged).
- **E1's first attempt** OOMed in the manual backward (`reports/debug_lexlat_e1_oom_2026-09-21.md`: five per-frame [B, K, m_max, O] float64 tensors, 64 GiB at B = 114 and m_max = 7416, on top of ~19 GiB resident; E0's fix had covered the forward only). Orchestrator position, fixed before the diagnosis arrived and kept: a value-identical re-chunking of the same computation at the same run shape is an implementation change and may be re-measured, the bar and the fallback list are not moved, and a non-identical change would need its own registration. One clean measurement was funded and built in speech-llm d2f6a5e (`reports/exec_lexlat_e1_relaunch_2026-09-21.md`). Orphaned hashes carrying error markers, not live failures: `LexlatEfficiencyProbeJob.r4Iaa72mU27T` and `.eHrOFiwUqKAf`, `LexlatWordCountsJob.X2YnVYfqN7aV` (which also had a phonemisation mismatch between the phone and word window lines it pairs, a wiring bug against the uniform-sample rule; refixed as `1ZJy5dFbOAHD`, FINISHED). The DP pack `DPiivOfTWAdM` was never funded.
- **Paused, then resumed for cost work.** User "Let's pause it for now" cancelled the C = 1024 probe after 5 of its 9 timed steps (`reports/exec_pause_lexlat_kill_budget_2026-09-21.md`); user "for lexical prefix tree search, please keep trying: 1. add time profiling in the probe; 2. is it possible to implement lexical prefix tree search on GPU efficiently?" resumed the phase for cost work only (no arm, no pack, no bar moved). Still open from the pause: the Design 7 fallback reading (the word-bigram CSR provisionally cannot close a ~40x per-step gap, because per-step cost is set by C and the per-frame candidate count m_max, which the LM order does not shrink) owes its fresh-context audit before the phase is stopped.
- **Profile job's first run** FAILED after 1 h 14 min in its own identity self-check: the per-key loss comparison included the throughput monitor `blankfree_frames_per_sec`, wall-clock by construction, and the assertion fired before the report was written. A check-scope bug, not a result about the lexicon step; fixed in speech-llm 10ccb37 at the same hash (`reports/impl_lexlat_profile_2026-09-21.md`, `reports/impl_lexlat_profile_fix1_2026-09-21.md`, `reports/exec_lexlat_profile_rerun_2026-09-21.md`). Known gap in the banked phase table: the backward's straight-line sub-steps appear as one UNACCOUNTED share of the backward leg.
- **k2 built with CUDA** (`reports/impl_k2_feasibility_2026-09-21.md`, `reports/impl_k2_build_2026-09-21.md`): no GPU wheel exists for aarch64, so k2 1.24.4.dev20260921+cuda12.6.torch2.7.1 (master ec31d2c, with_cuda True) was built from source against the env's torch 2.7.1 / CUDA 12.6 / sm_90 in the conda clone `/e/scratch/spell/wu24/envs/sae_k2` (build 523 s; 9.9 GB, on scratch only, nothing under project1; 90-day lease, RPATH absolute so not movable); CPU `intersect_dense_pruned` + log-semiring total score + backward verified, GPU check script `/e/scratch/spell/wu24/envs/k2_gpu_check.py`. User approval for the route: "I approve the whole k2 thing, sounds like a good plan" (build and settling probe approved; an arm still needs design amendment 9 and its review).
- **k2 probe built and fixed before any launch.** Build (`reports/impl_k2_probe_2026-09-21.md`, DONE_WITH_CONCERNS): `lexlat_k2.py` / `lexlat_k2_jobs.py`, shim `config/sae_4a_lexlat_k2.py`, 22 tests in the scratch env (H matches the bed's `transcript_logprob` to < 1e-4, HLG max-plus equals `string_best_segmentation` up to the SIL constant, G vs KenLM < 1e-4, prune 0 bit-identical); disclosed deviations: G is built from the CSR automaton in `lexlat_resources.npz` (the banked ARPA was cleaned) with the kaldilm cross-check done by hand only, and the prune ladder is the implementer's choice. Review `reports/review_k2_probe_2026-09-21.md` (DONE_WITH_CONCERNS) confirmed H, tau, the unpruned trigram with backoff, the bar's constants and the absence of labels, and named five defects to fix first: `int(FsaVec.shape[1])` = `int(None)` killing the GPU child at HLG load; no partial write and no step abort (probe.json written once after 27 cells in a 2 h clock, the failure mode E1's ruling forbids); an asynchronous max-plus total-score pass inflating t_scores and peak memory inside the timed window; `<unk>` arcs dropped, making the priced graph the strict lexicon while the Design fixes ESCAPE inside the marginal, so a PASS would not have licensed route A; n_neg_inf double-counted. All five fixed in speech-llm 05f425f and re-checked PASS (`reports/impl_k2_probe_fix1_2026-09-21.md`, `reports/review_k2_probe_fix1_2026-09-21.md`); dead hashes `LexlatHLGBuildJob.BoWs93v20S6x` / `LexlatK2ProbeJob.7eYgCXeFxMDQ` moved to the live `rtX44PBJFNy1` / `Tkl94t85j4pV`. Residual concern: the step abort fires between cells, with no wall guard inside a single k2 call.
- **Determinization ruling** (applied in speech-llm 1194506, hashes unchanged; `reports/impl_k2_probe_fix2_2026-09-21.md`, `reports/review_k2_probe_fix1_2026-09-21.md`): k2's determinization does not terminate on the escape LG (twins property violated), and k2's determinize is tropical-only, so a determinized graph would under-count the log-semiring total. Ruling, extended to every priced graph of this phase, escape or strict fallback: none is determinized. The undeterminized escape HLG is the correct object, not a compromise -- the probe's objective is the log-semiring total score, invariant under a language-preserving transform, while a tropical determinization would have merged parses by max; the cost of the larger graph is exactly what the probe measures.
- **Disclosed defect of the k2 graph** (found by the log-semiring fixture test, kept as an xfail with this reason): the HLG's log-semiring total exceeds the exact sum over parses, -2.739 against -2.803 nats (determinized -2.736), an over-count of 0.064 nats. The word LM enters k2 as an automaton with epsilon back-off arcs, so an explicit n-gram path and the back-off path for the same word pair both carry mass -- the usual back-off-as-epsilon approximation, exact in max-plus, which is why E-1 and the max-plus tests agree to 1e-4 and the bed's own CSR implementation, matching longest suffixes, does not double count. Consequence: the k2 probe stays a COST settling test (time, memory, pruning stability), which the over-count does not affect; a PASS licenses design work in which the objective's log Z must either accept this approximation with a measured bound or use failure arcs, which k2's pruned intersection does not support. Amendment 9 item 5 registers the measured bound; its `LexlatK2OvercountJob` excludes and counts 772 of the 2,864 gold strings, which carry an adjacent repeat the run-collapse topology cannot emit.
- **HLG build on our banked trigram**, `LexlatHLGBuildJob.rtX44PBJFNy1` FINISHED (Slurm 1926867, `output/summary.txt`): the FULL banked trigram compiled at theta = 0 with the prune ladder untouched, 173.5 s, peak RSS 26.0 GiB against the 6 h / 200 GB budget; L 893,218 states / 2.09 M arcs, G 3,545,313 states / 20.6 M arcs, HLG 24,736,960 states / 143.9 M arcs; escape priced in [-8.562, -3.125] nats per escaped phone; determinized False.
- **Official-LM benchmark built**, on the user's "maybe benchmark the official lbs 4gram as well since that is the standard" (speech-llm 8d31839; `reports/impl_k2_official_lm_2026-09-21.md`; new modules `lexlat_k2_official.py`, `lexlat_k2_official_jobs.py`, order-n G in `lexlat_k2.py` with the order-3 npz path bit-identical, live census unchanged, the live k2 graph untouched): three further HLGs on the official LibriSpeech lexicon (stress stripped onto the bed's 39 + SIL, ESCAPE and SIL_PROB as now, never determinized) with G = official 3-gram.pruned.1e-7 (icefall's standard search graph), 3-gram.pruned.3e-7 (Galvez et al. 2023) and the full 4-gram, each priced by the unchanged `LexlatK2ProbeJob` at max_active 1k / 3k / 10k on the same batches, checkpoint and tau, plus `WordLmPerplexityJob` (CPU, text-only, disclosed reference-transcript use). Reading rules fixed before the reads: the E1 bar applies to every graph as written; a PASS on one graph licenses the amendment for THAT graph only; the perplexity table is descriptive. Review `reports/review_k2_official_lm_2026-09-21.md`: the trigram path is bit-identical and the live hashes unmovable; three 4-gram-path defects were fixed in speech-llm 2ced584 -- an order-4 back-off hole (a listed 3-gram whose 2-gram prefix is unlisted) now handled as KenLM does by creating the missing context (the review's arc-dropping fix alone still missed kenlm by 0.46 nats on a fixture), G compacted after pruning (pruning drops arcs and never states, and 84.37 M states made the ladder shrink G only 1.8x), and a pre-compose size guard from the reference build's 190 bytes per HLG arc that declines a rung predicted above 0.8 x 200 GB instead of letting Slurm kill the ladder -- with ladder 0 / 0.5 / 2 / 5 / 8 / 12 / 16 nats, one attempt hour per rung, the clock started after the parse and an 8 h wall for the 4-gram (parse ~20 min, ~145.3 M n-grams / 84.4 M states before compaction). Only the 4-gram hashes moved: build `UMLMzgsG4aF4` to `NrghE6fnf5hc`, probe `zeXxiYy0VBnD` to `DtWYToXTPh6w`.
- **Arm backend for amendment 9 built and reviewed** (speech-llm b19f2ba + 9c8b22d; `reports/impl_k2_arm_backend_2026-09-21.md`, `reports/review_k2_arm_backend_2026-09-21.md`: the term matches `run_probe` line for line, enumeration test to 1e-6, gradient to the emissions only, lam_lex = 0 bit-identical on loss and gradients, both model-source edits hash-neutral by census, rate term untouched): `lexlat_k2_train.py` (`LexlatK2Runtime`), `lexlat_k2_arm_jobs.py` (`LexlatK2OvercountJob`), a default-off `lexlat_k2_*` block in the model definition and train step, and pack config `config_sae_4a_lexlat_k2_pack_v1.py` (shim `config/sae_4a_lexlat_k2_pack.py`) with the HLG constants filled from `rtX44PBJFNy1` and `MAX_ACTIVE` / `PROBE_SEC_PER_SUBEPOCH` left unfilled by design -- `py()` refuses without them and asserts the measured lexicon leg <= 1202 s per sub-epoch before the weaker DP-side factor clause. Test-only build: 201 jobs, pack `PackedBlankfreeTrainJob.WbNATSBGkXjG`, shuffled HLG `zUCMVmtrEigZ`; nothing launched. Monitor note: `lexlat_k2_expected_words` includes the escape word, so the Gate read is escape / words. Side fix (ruling 7): the DP pack config's three `phones_o4.bin` sources had been auto-cleaned and were replaced by the surviving byte-identical copy `PriorGapAnalysisJob.l0p0srBryKrs` (md5 61eae5...), pack `DPiivOfTWAdM` unchanged.

### Over-count read, amendment 9.5 (`LexlatK2OvercountJob.f5Ljn6twbc4b`, FINISHED 2026-09-21, Slurm 1931848, 77 min on 16 CPUs; `work/.../LexlatK2OvercountJob.f5Ljn6twbc4b/output/summary.txt`, `overcount.json`)

Graph: the banked-trigram escape HLG `rtX44PBJFNy1` (undeterminized, sil_prob 0.5). Reference as registered: `string_best_segmentation` (E-1's exact CSR Viterbi, MAX-PLUS) plus the silence model's constant. Strings: E-1's own sets; adjacent-repeat strings excluded and counted (gold 772 of 2,864, private 22).

| set | strings / tokens | over_count per token: median / p95 / max | tropical_residual median / max abs | sum_minus_max median / p95 | read (median <= 0.05) |
|---|---|---|---|---|---|
| gold (disclosed label-using diagnostic) | 2,091 / 110,919 | +0.287 / +0.374 / +0.503 | 0.000 / 0.058 | +0.286 / +0.374 | FAIL |
| private (ctrl_50 ep10 decode) | 2,842 / 165,532 | +0.162 / +0.432 / +0.600 | 0.000 / 0.260 | +0.143 / +0.415 | FAIL |

As registered: VERDICT FAIL on both sets, which licenses not funding the k2 arm without a further amendment; it says nothing about the lexicon's value.

Orchestrator reading, to be audited before it is acted on: the job decomposes `over_count` into `tropical_residual` (the graph's max-plus against the same max-plus reference, median 0 on both sets: the E-1 equivalence holds in the graph at full scale) and `sum_minus_max` (the graph's log-sum minus its own max-plus). The second column carries the whole FAIL, and it is NOT the back-off double count alone: it also holds every legitimate reading the design's own objective sums over (alternative word segmentations of the phone string, and the escape reading of every span, priced inside the marginal by Design). The amendment's sentence names "the exact CSR score" as the reference for a log-semiring string score; the exact object is the log-semiring sum over parses under the CSR back-off (longest-suffix, no double count), which the fixture test computed by enumeration (0.064 nats on the fixture) and which `string_best_segmentation` does not compute. The registered column is therefore an upper bound on the double count, the acceptance (median <= 0.05 nats per token) stands unchanged, and the read has to be repeated against the exact log-semiring CSR reference before amendment 9 is decided either way. The pre-registered read is preserved above as FAIL and is not overturned by this note.

Audit of that reading (fresh context, `reports/audit_k2_overcount_2026-09-21.md`): the FAIL is CONFIRMED and the orchestrator's attribution is overturned in its main part. Re-derived from `overcount.json`: gold +0.287, private +0.162, the identity holds to 1e-16, and 100 % / 91 % of strings individually exceed 0.05. A read-only CPU decomposition on 60 strings per set (the auditor's own DP with a switchable semiring, whose max reproduces the graph's max-plus to 9e-9 per token) splits the column: legitimate multi-parse mass (segmentations, homophones, escape spans; SIL contributes nothing, no string or pronunciation carries it) only +0.013 gold (4 %) / +0.019 private (14 %); the plain back-off route sum (explicit arc plus back-off routes per word transition) +0.033 / +0.017; the remainder +0.240 / +0.110 is positional multiplicity of the `#0` back-off loops: `_add_self_loops` puts a `#0` self-loop on every L state, so one G back-off route is consumed at several positions inside a word and, with no determinization or epsilon removal (both tropical-only in k2), every position is a separate summed path. Against the exact CSR log-sum the over-count is still +0.274 / +0.117 (5.5x / 2.3x the tolerance). Two further findings: the amendment's sentence fixes the reference as max-plus while its cited fixture figure (0.064) is a sum-against-sum quantity; and a non-zero `tropical_residual` (up to +0.081 per token on some strings) is slack of the max-plus reference against the graph's best route, not a graph defect.

Consequence and decision (2026-09-21): amendment 9.5 stays FAILED for the graph as built. The dominant term is a construction choice, not the back-off approximation, so it is repaired before amendment 9 is decided: the `#0` loops move to the word-boundary states of L as a new build option (the old placement keeps every existing hash), the fixture test becomes a decomposition test, and the over-count read is repeated on the rebuilt graph against BOTH the registered max-plus reference and the exact CSR log-sum (implementer reports `reports/impl_k2_hlg_loops_2026-09-21.md`, `reports/impl_k2_overcount_exact_2026-09-21.md`). Acceptance unchanged: median <= 0.05 nats per token per set; the read that decides is over_count_exact on the rebuilt graph, with the registered column reported beside it. If the plain back-off route sum alone (about 0.03 / 0.02 on the sample) is what remains, the graph passes; if the k2 cost probe then passes on the rebuilt graph too, amendment 9 goes to design review with the residual approximation disclosed.

**Deciding read, rebuilt word-boundary graph `cdcxYJMjiYj5`, exact reference (`LexlatK2OvercountExactJob.vNM59xVEnC2G`, FINISHED 2026-09-21, Slurm 1938195, 1915 s on 2 CPUs; `work/speech_llm/sae/emc/lexlat_k2_overcount_exact_jobs/LexlatK2OvercountExactJob.vNM59xVEnC2G/output/summary.txt`, `overcount_exact.json`).** Same strings and exclusions as the registered read (gold 2,091 / 110,919 tokens; private 2,842 / 165,532); reference `lexlat.string_log_sum` (max-semiring parity on 50 strings 0.00 nats, 0 decomposition violations).

| set | over_count_exact per token median / p95 / max | legit_multi_parse median | over_count as registered (max-plus reference) median | read (median <= 0.05) |
|---|---|---|---|---|
| gold (disclosed label-using diagnostic) | +0.0357 / +0.0744 / +0.1726 | +0.0109 | +0.0500 | PASS |
| private (ctrl_50 ep10 decode) | +0.0173 / +0.0552 / +0.2389 | +0.0323 | +0.0535 | PASS |

VERDICT PASS on both sets: **amendment 9.5 is met on the word-boundary graph** (down from 0.274 / 0.122 exact on the all-states graph); the residual is the plain back-off route sum the audit predicted (about 0.03 / 0.02). The registered max-plus column on the same graph reads FAIL on private by 0.0035 and is reported beside it as the amendment requires; the deciding column is the exact one, as the 2026-09-21 decision paragraph above fixed before this job ran. AUDITED CONFIRMED (`reports/audit_k2_overcount_exact_wb_2026-09-21.md`, fresh context): medians re-derived from the json (gold +0.0357, private +0.0173, robust to token-weighted and pooled conventions), rows and exclusions identical to the registered read, decomposition identity to 5.6e-17, registered column byte-identical to x4a6MX6ZgJ5n, the exact reference reproduced by the auditor's own parse enumeration on 10 strings to 7e-15 nats, graph confirmed as the one probe by7UYEjtYdel priced. Auditor's qualifications, disclosed: (i) the deciding column was re-specified (max-plus reference to exact reference) after the max-plus criterion had FAILED on the old graph, though before the rebuilt graph and both of its reads existed, and the max-plus column on this graph still reads FAIL on private by 0.0035; (ii) the 0.05 bar is anchored to neither scale; (iii) p95 exceeds 0.05 on both sets; (iv) 27 % of gold strings are excluded (adjacent repeats); (v) the read bounds an unpruned linear-FSA intersection, not the arm's pruned dense-lattice log Z. Orchestrator reading: the re-specification is the decision paragraph above, recorded before the deciding job ran, on the audited finding that the registered column charged legitimate multi-parse mass to the graph; the term is disclosed as an approximate word constraint with the double count bounded at 0.02-0.04 nats per token (median).

Official HLG builds, all three FINISHED (2026-09-21; `output/.../sae_4a_lexlat_k2_official/<g>/hlg/summary.txt`): 3-gram.pruned.1e-7 (`GxCkDk90bQpT`) full LM at theta 0, G 2.65 M states / 8.95 M arcs, HLG 20.4 M states / 169.4 M arcs, 119 s, peak 20.8 GiB; 4-gram (`NrghE6fnf5hc`) DISCLOSED DEVIATION theta = 5.0 nats (the ladder's 0 / 0.5 / 2 rungs were declined by the size guard at predicted 391 / 374 / 192 GiB against the 160 GiB budget), 145.3 M n-gram arcs pruned to 7.6 M, G 3.82 M states / 11.3 M arcs, HLG 33.4 M states / 181.1 M arcs, 347 s, peak 31.1 GiB. All three official probes (`R7QzD6vYBLD3`, `5dP7W2NMFq4T`, `DtWYToXTPh6w`) died on the k2 int32 arc-count fault (Results "k2 probe fault" pending) and were cancelled with manager 857002 (`reports/exec_k2_probe_stop_2026-09-21.md`); their logs are kept in `output_failed_k2fault/`; rerun after the chunking fix.

### k2 settling probes against E1, first complete reads (2026-09-21; `LexlatK2ProbeJob`, chunked intersection speech-llm 3454206 + verdict line 0c812b1; `reports/exec_k2_probe_rerun2_2026-09-21.md`; audit pending `reports/audit_k2_probe_e1_2026-09-21.md`)

All five probes measured 27 of 27 cells (9 planned batches of sub-epoch 10 x max_active 1000 / 3000 / 10000) at the run shape on the banked ctrl_50 ep10 posterior at tau 2.0. Bar (Design 7, unchanged): <= 1202 s per sub-epoch extrapolated from the mean batch time x 57, <= 80 GiB reserved; rung choice (Design, k2 route item 3): smallest rung passing the bar with median |log Z(ma) - log Z(10000)| <= 0.05 nats per retained frame. Seconds are the lexicon leg alone (forward, log-semiring total, backward), not the full training step.

| graph (HLG states / arcs) | max_active 1000: s per sub-epoch / peak GiB / median stability | 3000 | 10000 | VERDICT |
|---|---|---|---|---|
| in-house, all-states loops `Tkl94t85j4pV` (24.7 M / 143.9 M) | 309 / 43.3 / 0.034 | 353 / 46.3 / 0.014 | 476 / 62.3 / 0 | PASS |
| in-house, word-boundary loops `by7UYEjtYdel` on `cdcxYJMjiYj5` (23.9 M / 98.6 M) | 182 / 23.4 / 0.028 | 218 / 27.2 / 0.013 | 310 / 38.1 / 0 | PASS |
| official 3-gram 3e-7 `R7QzD6vYBLD3` (9.8 M / 81.6 M) | 720 / 62.4 / 0.029 | 781 / 67.8 / 0.013 | 953 / 83.3 (memory FAIL) / 0 | FAIL at 10000 only |
| official 3-gram 1e-7 `5dP7W2NMFq4T` (20.4 M / 169.4 M) | 455 / 59.7 / 0.030 | 527 / 70.6 / 0.013 | 704 / 88.9 (memory FAIL) / 0 | FAIL at 10000 only |
| official 4-gram theta 5 `DtWYToXTPh6w` (33.4 M / 181.1 M) | 981 / 69.7 / 0.032 | 1031 / 72.6 / 0.014 | 1189 / 88.5 (memory FAIL) / 0 | FAIL at 10000 only |

Reading, pending audit: the k2 route clears E1 on the in-house graphs by 4-7x on time and by 2-3x on memory; the word-boundary rebuild is 1.7x faster and half the memory of the all-states graph at every rung. The rung rule selects max_active 1000 on every graph (stability medians 0.028-0.034, all under 0.05; the max over utterances reaches 0.09-0.13, disclosed). The official graphs pass at 1000 and 3000 and fail only the memory clause at 10000, so they are usable at the selected rung; the 4-gram sits at 0.82 of the time bar. One non-finite total score at rung 1000 on the all-states graph (1 of 1070 utterances) is in the audit's brief. The earlier CUDA 700 / 717 fault did not recur under the <= 16-sequence chunking; the k2 GPU check passed in every job.

Over-count on the rebuilt word-boundary graph, registered column (max-plus reference; `LexlatK2OvercountJob.x4a6MX6ZgJ5n`, FINISHED): gold median +0.0500 (p95 0.096), private +0.0535 (p95 0.154), VERDICT FAIL as registered (gold PASS at the bar, private 0.0035 over), down from 0.287 / 0.162 on the all-states graph. The exact-reference decomposition on the all-states graph (`LexlatK2OvercountExactJob.4yKkB64ZDvJ4`, FINISHED) reads over_count_exact median 0.274 / 0.122 and legit multi-parse mass 0.011 / 0.032 (gold / private), confirming the audit's attribution. The exact read on the rebuilt graph (`vNM59xVEnC2G`, running) is the amendment-9.5 decision: median over_count_exact <= 0.05.

**Official 4-gram word-boundary graph (`LexlatOfficialHLGBuildJob.a3vpyag6WCBy`), over-count read 2026-09-22 (`reports/extract_k2_official_overcount_probes_2026-09-22.md`), node 3's precondition.** Exact decomposition `LexlatK2OvercountExactJob.pkDzxP1kCCoH`, FINISHED: over_count_exact median gold +0.0221, private +0.0159, both under the 0.05 bar: **PASS under the amendment-9.5 rule; node 3's pack read is not voided.** Registered column `LexlatK2OvercountJob.XRbHuFzSqvS3`, FINISHED: gold median +0.0327 (PASS), private +0.0976 (FAIL as registered), so the registered column's private excess is legitimate multi-parse mass (about 0.08 nats per token on decoded strings, against 0.03 on the in-house graph), not back-off double counting; disclosed, not a gate quantity. Not separately audited; it applies the rule audited for `vNM59xVEnC2G`.

**Step probes, the real train step (`LexlatK2StepProbeJob.7dn3tJ9GS0tg` rung 1000, `hPiGpdJ84fvI` rung 3000, FINISHED 2026-09-22 09:0x; `output/summary.txt`, `step_probe.json`).** Whole train step of k2lat_20 at sub-epoch 10 on the in-house word-boundary graph, 9 of 9 planned batches, CUDA-synchronised, backward through the recognizer, no optimizer step: rung 1000 peak reserved 31.22 GiB, mean leg 3.302 s, 601 + 57 x 3.302 = 789.2 s per sub-epoch; rung 3000 31.77 GiB, 3.955 s, 826.5 s. Both PASS (<= 80 GiB, <= 1202 s). The settling probe's 23 GiB did not bound the real step (it ran without backward); the packs' on-set reads (17-22 GiB, 1.9-6.5 s per step) are consistent. The E1 memory question is closed at these rungs with a 2.5x margin; rung 10000 remains untested with backward.

**Official probes on the word-boundary graphs, fresh GPU runs 2026-09-22 08:23-08:27** (same table protocol as above; rungs 1000 / 3000 only; Slurm 1948814 / 1948815): 4-gram word-boundary `LexlatK2ProbeJob.iz6A6PiEs1xd`: rung 1000 289.8 s per sub-epoch / 37.96 GiB, rung 3000 330.3 s / 39.59 GiB, PASS, rung rule selects 1000; 3-gram 1e-7 word-boundary re-probe `AgHdqPhhjIDz`: rung 1000 319.5 s / 39.16 GiB, rung 3000 371.2 s / 40.13 GiB, PASS, selects 1000. Under the 9.3 bar reading (bed 601 s + leg <= 1202 s) both official word-boundary graphs PASS E1 with a wide margin, where their all-states compiles (rows above, 720-981 s at rung 1000) did not. Node 3 ran on the conservative 981.1 s bound; the measured 4-gram leg is 289.8 s. The pack shim keeps 981.1 (changing `PROBE_SEC_PER_SUBEPOCH` would re-hash the FINISHED pack `QepNaSM1CDmN`); the measured constant governs any new pack on this graph.

AUDITED (`reports/audit_k2_probe_e1_2026-09-21.md`, fresh context, re-derived from per_batch.json): both in-house PASS verdicts CONFIRMED, 27 of 27 cells real and distinct, timed region correct (intersection + total score + backward, CUDA-synchronised, chunk loop inside, peak reserved reset per cell and maxed over chunks), emissions the real ctrl_50 ep10 posterior at tau 2.0, run shape and longest-T batch confirmed. The three official-LM FAIL lines are arithmetically right but OVERTURNED as gate reads: the registered rule (smallest rung passing the bar with median stability <= 0.05) selects max_active 1000 on all five graphs, where every graph passes both clauses; the summary writer's VERDICT demands every rung pass, which is a stricter rule this orchestrator wrote into the verdict brief, not the gate (writer to be corrected with the next code change; the registered rule governs). Disclosed underestimate: the arm additionally computes log Z_H and the arc-posterior monitors (untimed here) and, calling the helper with backward=False, holds all chunk autograd graphs until the outer backward, so the probe's peak GiB does not bound the arm's; the sampling of 9 batches is conservative. The one non-finite total (all-states graph, rung 1000, batch 15 utterance 95: empty pruned lattice, log Z -inf against -218.08 at rung 10000) was dropped from that median (1070 of 1071); the design's NEG_INF abort rule makes this a live hazard for the arm at rung 1000 on that graph, and no such utterance occurred on the word-boundary graph.

### Supervised blank-free recognizer, the diagnostic pack's starting point (`BoundedBlankfreeTrainingJob.l2yr1vfnVoo6`, FINISHED 2026-09-22 01:37, Slurm 1944216, 15 min on one GPU; `GetBestPtCheckpointJob.6ey6iE6KtZrw`; `output/.../sae_4a_supervised_blankfree/`)

Trained as registered in "Implementation controls" (ii): the bed's own recognizer, blank-free min-duration topology, loss = -transcript_logprob of the gold phone string, 30 passes over the labelled 10 h seed (2,821 train / 28 held-out utterances, `CvHoldoutSplitJob.sD7U6CYs8ACM`), one GPU. Labels used for training and selection, disclosed; the recognizer is a diagnostic input only, never a main-line arm. The first job (`PItwGyntiGL7`) died at batch 1 because the MetaDataset ordered by the 28,539-utterance feature set while the targets HDF holds the 2,849 seed utterances; fixed by ordering on the targets dataset (`reports/debug_supervised_targets_missing_2026-09-22.md`, commit d136c38d; the unlaunched theta seed config carries the same latent wiring).

| kept pass | held-out loss (nats per unit, `dev_loss_blankfree_supervised`) | dev-other greedy PER (`BlankfreeGreedyPerJob`) |
|---|---|---|
| 1 (selected: min held-out loss) | 0.489 | 0.1894 (S 8678 / D 17885 / I 7021, N 177275) |
| 5 | 0.584 | 0.2207 |
| 10 | 0.644 | 0.2381 |
| 20 | 0.675 | 0.2340 |
| 30 | 0.590 | 0.2067 |

Train loss falls monotonically (1.09 at pass 1 to 0.23 at pass 30) while held-out loss is lowest after the FIRST pass and rises to 0.70 by pass 19 before easing: the recognizer overfits the 10 h seed from pass 2 on. p0 for the diagnostic pack is therefore the pass-1 checkpoint at 0.1894 dev-other PER (selected by the registered rule, min held-out loss). The pack's expectations are paired deltas between sup_* arms, so p0's absolute value gates nothing; it is recorded because a positive control whose recognizer is itself at 19 % PER holds the lexicon against drift from a weaker start than a converged supervised model would, and the sub-epoch-4 / -8 reads must be interpreted with that in mind.

### Positive control, first read (`LexlatK2PositiveControlJob.H8HfOnGCeOd8`, FINISHED 2026-09-22 02:30, Slurm 1945324; `output/summary.txt`, `control.json`; "Implementation controls" (i))

Setup as registered: ctrl_50 sub-epoch 10 bed, tau 2.0, max_active 1000, 100 dev-other utterances, gold = frame-wise posterior on the MFA gold phone (6,034 segments, 509 lengthened to H's min duration); treatment graph `cdcxYJMjiYj5`, null graph `UGE5MH3v5lyT` (shuffled pronunciations, theta 0.0, word-boundary loops). The summary notes the bed divides log-probabilities by tau, so the constructed 1 - eps posterior is worth 0.529 per frame at the operating point.

| cell | -L_lex nats per gold token | escape share | E[words] | empty |
|---|---|---|---|---|
| gold / treatment | 1.948 | 0.000 | 16.2 | 0 |
| cold / treatment | 2.127 | 0.003 | 15.9 | 0 |
| noise / treatment | 3.550 | 0.021 | 17.3 | 0 |
| gold_noisy / treatment | 3.285 | 0.000 | 16.0 | 0 |
| gold / null | 2.811 | 0.000 | 12.1 | 0 |
| cold / null | 2.470 | 0.006 | 11.3 | 0 |
| noise / null | 3.741 | 0.063 | 11.2 | 0 |

Gradient sign agreement with "push the gold phone up" on gold+noise: treatment 0.976, null 0.859.

Clauses: OK (2) gold/treatment escape share 0.000 < 0.1, (3) no empty lattice, (8) cold sits between gold and null, (9) treatment gradient agreement 0.976 > 0.8. MISS (1) gold/treatment 1.948 nats per token, expected < 0.3; (4) gold/null 2.811, expected >= 3 x clause 1; (5) gold/null escape share 0.0004, expected > 0.5; (6) noise/treatment 3.550, expected >= 3 x clause 1; (7) noise/treatment escape share 0.021, expected > 0.5; (10) null gradient agreement 0.859, expected < 0.6. **VERDICT FAIL (6 of 10)**, which by the registered rule licenses "do not fund the packs on this term until this is understood" and nothing about the lexicon's value.

Reading, provisional: every ordering the control asked for holds (gold < cold < noise on both graphs; treatment < null for gold and for noise; the treatment graph's gradient pushes the gold phone up at 98 % of frames), but the absolute and ratio clauses miss, and the ESCAPE word is essentially never chosen even for noise on the treatment graph or gold on the shuffled null. Whether that is the term (an implementation defect in the escape pricing or the lattice) or the control (tempered emissions plus the word 3-gram's own cost per phone would put a correct gold/treatment value near 1-2 nats per token, and short in-vocabulary words may tile noise more cheaply than the escape price) is the debugger's question above. The packs launched before this read (user ruling: launch overnight); their reads stand or fall with that verdict.

Debugger read (`reports/debug_k2_positive_control_fail_2026-09-22.md`, fresh context, 03:1x): EXPECTATIONS MISCALIBRATED, no implementation defect. A k2-free reference (`lexlat.string_log_sum` on the argmax string through the banked resources, LM tempered as the train step does) bounds -L_lex per utterance from above; measured gold/treatment 1.948 per token against the single-string bound 2.201 = 0.982 (tempering floor: eps 0.02 at tau 2 gives a 0.529 peak, a property of the control's emissions) + 1.219 (the transcript's 3-gram + silence cost, 8.05 untempered nats per word); measured <= bound on 99 of 100 utterances; gold/null 2.811 vs 3.025, noise/treatment 3.550 vs 4.009; log Z_H equals the closed form to 2e-5 on all 100. Escape: 3,120 <unk> arcs per graph, reachable, priced unk log-prob + per-phone price (4.9 per phone + 15 per span), so the derangement null, which keeps the full pronunciation inventory, tiles gold at 3.8 nats per phone and never escapes; clause 1 is unreachable even at eps -> 0 (about 2.4 per token), clauses 4 / 6 (5.84) exceed the escape cap (about 4.0 per token), clause 10 follows from the null sharing H's run-collapse. Proposed fix: amend the control's expectations only. AUDIT PENDING from a second fresh context (`reports/audit_k2_positive_control_2026-09-22.md`, criteria: log Z_H within 1e-3 of a k2-free forward; -L_lex <= single-string bound on >= 95 of 100 gold/treatment utterances; escape reachability); until it lands the original ten clauses stand as registered and the verdict stays FAIL.

### Diagnostic pack ("Implementation controls" (ii)), amendment before any number: temperature held at 2.0

The pack's first two launches produced no training-level number. `PackedBlankfreeTrainJob.v8nCMBb919HV` died at model load (checkpoint key prefix, fixed by conversion, speech-llm 7aef9ed2). Its replacement `5o9P5uidnoWv` (Slurm 1954697) ran `sup_plain` to completion, but `sup_k2lat`, `sup_k2shuf` and `sup_off4` all aborted at the first stability read of sub-epoch 1, about 2.5 min after model load, with k2's `Array1<T>::Init Check failed: size >= 0 (-1951946753 vs. 0)` inside `intersect_dense_pruned` (`reports/exec_k2_diag_pack_fail2_2026-09-22.md`). Cause, verified from the written configs and logs (`reports/impl_k2_diag_tau_fix_2026-09-22.md`): the pack inherited the bed's cold-start temperature anneal 8.0 / 5.04 / 3.17 / 2.0, and with the lexicon term on from sub-epoch 1 the first intersection ran at tau 8.0, where the flattened posterior overflows k2's int32 (the same overflow that withdrew D9's flat-init row); every main-line arm first intersects at its on-set (sub-epoch 8 or 5) at tau 2.0, where the read succeeds (k2lat_20: 0.0254 nats per retained frame). Amendment (speech-llm bc67634a, pre-registered here before the relaunch): the temperature is held at 2.0 for sub-epochs 1-8 in all four arms, `sup_plain` included, so the paired deltas keep their meaning; the anneal is a cold-start device the supervised init does not need. Everything else stays as registered (on-set 1, ramp 1-3, kept 1 / 2 / 4 / 8, ctrl_20's other constants). The finished `sup_plain` of `5o9P5uidnoWv` ran under the annealed schedule and is not paired with the new arms. New pack `PackedBlankfreeTrainJob.5XNGdxndoY1Y`; its expectations and reads are those of "Implementation controls" (ii), unchanged.

**Read (`5XNGdxndoY1Y` FINISHED 2026-09-22 21:07, Slurm 1957754; all four arms 8 sub-epochs, kept 1 / 2 / 4 / 8; `PairedPerDeltaJob` x8 under `work/speech_llm/sae/emc/eval_jobs/`, outputs `output/exp2025_11_06_speech_llms/librispeech/sae_4a_supervised_k2_diag/`; `reports/extract_k2_diag_pack_2026-09-22.md`; temperature 2.0 x 8, on-set 1, ramp 3, init `SupervisedRecognizerExportJob.YtuVg6ZYuK0P`).** Starting point p0 = 0.1894 dev-other greedy PER (the supervised recognizer, `sae_4a_supervised_blankfree/best/dev-other/per.txt`). Plain PER at ep4 / ep8: sup_plain 0.8915 / 0.8915; sup_k2lat 0.8124 / 0.8139; sup_k2shuf 0.8199 / 0.8190; sup_off4 0.8059 / 0.8107. Paired deltas (candidate minus baseline, negative = candidate better, 95 % speaker-clustered bootstrap): sup_k2lat - sup_plain -0.079 [-0.087, -0.072] at ep4, -0.078 [-0.085, -0.071] at ep8; sup_k2lat - sup_k2shuf -0.0075 [-0.0103, -0.0045] at ep4, -0.0050 [-0.0081, -0.0015] at ep8; sup_k2shuf - sup_plain -0.072 [-0.078, -0.065] / -0.072 [-0.080, -0.066]; sup_off4 - sup_plain -0.086 [-0.094, -0.078] / -0.081 [-0.087, -0.075]. Against the expectations fixed in "Implementation controls" (ii): clause 1 (treatment - plain <= -0.010) holds by eight times the margin; clause 2 (treatment - null <= -0.010) FAILS at both epochs (the interval excludes zero but not -0.010), so the reading "implementation and direction CONFIRMED" is NOT reached; the third expectation, sup_k2shuf < sup_plain - 0.010, holds by seven times the margin, which is the registered reading "the added weight, not the lexicon, is the protector (the null is doing the work)". The term is therefore not inert and not wrong-signed: it changes training strongly, but almost all of its effect is lexicon-agnostic. The larger fact is the plain row: the bed's own objective takes a 0.19 PER recognizer to 0.89 within four sub-epochs (the SAE_4A oracle precedent was 3x; here 4.7x) and no lexicon graph holds it above 0.81; the k2 term slows the collapse by a tenth and the true lexicon adds 0.005-0.008 on top. This is the same ordering as the main line at ep20 (any lexicon graph 0.03-0.06 below ctrl_20, treatment minus null within 0.01, D2 table), now with a known-good starting point, so the ordering is a property of the objective, not of the cold start.

### Packs, in-run kill reads at the lexicon on-set (2026-09-22 01:09-02:18; `PackedBlankfreeTrainJob.euWIAvH0xO5p` node 1, `2j2bZo1TrkC0` node 2; `reports/exec_k2_packs_onset_read_2026-09-22.md`)

Design 5 (a), step-1 loss equality: k2lat_20, k2shuf_20, k2lat_20_e5, k2shuf_20_e5, k2lat_20_ma3000, k2shuf_20_ma3000 reproduce ctrl_20's first step exactly (l_tau -0.350, prior per token -5.657, expected tokens 63.821); k2lat_20_s1 / k2shuf_20_s1 reproduce ctrl_20_s1's (l_tau -0.347, -5.671, 57.498). The pre-on-set path is bit-identical to the controls, as amendment 2 requires.

First on-set step, all eight arms (bars from Design 7 / amendment 9): peak reserved 17.4-21.9 GiB (bar <= 80 GiB, PASS); lexlat_k2_sec 1.87-6.51 s per step (bar <= 10.5 s, i.e. 601 + 57 x leg <= 1202 s per sub-epoch, PASS); empty-lattice fraction 0.000 in every arm (abort at > 0.10, amendment 9.6, not tripped); escape share 0.03-1.25 %; term_mean -0.418 to -0.513 nats; stability 0.014-0.071. No arm wrote an abort marker (the marker goes to the arm's own work dir; the `lexlat_k2_ABORT.json` in the setup-dir root is a pytest artifact of 2026-09-21 23:05 on no code path). The step probes were therefore not needed for the memory read; the runs are within budget with a 4x memory margin.

AUDITED (`reports/audit_k2_positive_control_2026-09-22.md`, fresh context, criteria fixed in the brief without the debugger's numbers): CONFIRMED, the k2 code computes -L_lex correctly at this operating point. Amendment 9.7 as code (`LexlatK2Control97VerdictJob`, new module `lexlat_k2_control_verdict_jobs.py`, `config/sae_4a_lexlat_k2_control97.py`, speech-llm commit 92748461, a reader over the FINISHED control's outputs, control hash untouched; `reports/impl_k2_queue4_2026-09-22.md`): (a) PASS 1.9e-12, (b) PASS 99 of 100 with worst excess +1.50, (e) PASS, (d) CANNOT_TELL (the escape arcs' count and price were never dumped; an HLG scan would be needed), (c) FAIL as written: on the NULL graph the cold string scores 2.470 against gold's 2.811 nats per token (5 of 6 orderings hold on nats per token, 6 of 6 on escape share). Ruling: (c) is read on -L_lex per token, the quantity the clause's other orderings are stated in; the null-graph "gold < cold" ordering was over-claimed when 9.7 was drafted (a deranged lexicon has no reason to prefer the gold string), so the amended control reads CONTROL_FAIL_9_7 by the letter with the failing ordering identified; the implementation conclusion above (audited from the raw quantities) stands and 9.7 is not redrafted after this read. log Z_H recomputed k2-free two ways (closed form; explicit DP over the H topology) agrees with the job to 1.9e-12 nats on all 100 gold utterances; -L_lex respects the exact single-string bound (A + string_log_sum / tau) on 99 of 100 gold/treatment utterances (worst excess +1.5 nats), 86 of 100 gold/null, 95 of 100 noise/treatment, every violation in the direction max_active 1000 pruning moves it, median headroom 9.9 nats; 3,120 <unk> arcs per graph scored -28.4..-18.1 = per-phone escape price + the single <unk> LM arc (-14.97), carried by the start state; in the delta-emission limit -L_lex -> 1.219 nats per gold token, 4x clause 1's bar, so a correct implementation meets exactly the four clauses this run met. The amended control is amendment 9.7; the original FAIL stands as the record of the mis-specified gate.

### Packs, registered paired PER reads at the kept epochs (2026-09-22 morning; `PairedPerDeltaJob` under `work/speech_llm/sae/emc/eval_jobs/`, `output/summary.txt` + `paired_per.json`; dev-other, 2864 utterances, delta = candidate minus baseline, negative = candidate better, 95 % speaker-clustered bootstrap over 2000 resamples; `reports/extract_k2_packs_morning_2026-09-22.md` for the per-arm plain PER, `reports/exec_k2_round_restart_2026-09-22.md` for the finished graph)

All twelve arms ran 20 / 20 sub-epochs, no `lexlat_k2_ABORT.json`, every kept-epoch PER read FINISHED (node 1 4:39 h, node 2 4:58 h, node 3 4:16 h wall). Plain dev-other PER at ep20: ctrl_20 0.8746, ctrl_20_s1 0.8751; treatments 0.819 (k2lat_20_ma3000) to 0.843 (k2lat_20); nulls 0.814 (k2shuf_20_ma3000) to 0.843 (k2shuf_20_e5). No arm is near the PASS clause's PER < 0.50.

**Clause 1, treatment against ctrl at ep20 (all clear M, see bands):**

| pair | delta [95 % CI] | job |
|---|---|---|
| k2lat_20 - ctrl_20 | -0.0311 [-0.0363, -0.0262] | `HYTZwM2ZMkmW` |
| k2lat_20_s1 - ctrl_20_s1 | -0.0416 [-0.0486, -0.0349] | `2aO397B0qOSq` |
| k2lat_20_e5 - ctrl_20 | -0.0437 [-0.0500, -0.0379] | `ElD71PVrHu7w` |
| k2lat_20_ma3000 - ctrl_20 | -0.0560 [-0.0628, -0.0499] | `r6nMVuNKn3gz` |
| off3_k2lat_20 - ctrl_20 | -0.0475 [-0.0532, -0.0423] | `n3CDpl7FyD6R` |
| off4_k2lat_20 - ctrl_20 | -0.0399 [-0.0464, -0.0340] | `8mJoYN5ICe4f` |
| off4_k2lat_20_s1 - ctrl_20_s1 | -0.0349 [-0.0409, -0.0293] | `pJkioJZd1Gbx` |

**The nulls against ctrl at ep20 gain as much or more:** k2shuf_20 -0.0464 [-0.0541, -0.0393] (`PPd2BhNgfUI3`); k2shuf_20_s1 -0.0613 [-0.0684, -0.0547] (`xc8FawKHZvXj`); k2shuf_20_e5 -0.0316 [-0.0377, -0.0262] (`WLLxkbUKKzk0`); k2shuf_20_ma3000 -0.0604 [-0.0672, -0.0540] (`xMqZwlEa2tMC`); off4_k2shuf_20 -0.0456 [-0.0534, -0.0387] (`xwpY4KOmRKxC`).

**Clause 2, treatment against its shuffled-pronunciation null at ep20 (the clause that separates lexical content from prior weight; PASS needs <= -M):**

| pair | ep20 delta [95 % CI] | ep10 delta | job (ep20) |
|---|---|---|---|
| k2lat_20 - k2shuf_20 | **+0.0153** [+0.0120, +0.0186] | +0.0154 | `7Mde7BZzISJQ` |
| k2lat_20_s1 - k2shuf_20_s1 | **+0.0197** [+0.0174, +0.0220] | +0.0147 | `xiFdglACcimO` |
| k2lat_20_ma3000 - k2shuf_20_ma3000 | **+0.0044** [+0.0007, +0.0080] | -0.0004 [-0.0036, +0.0026] | `YNbYKNpt0Ths` |
| off4_k2lat_20 - off4_k2shuf_20 | **+0.0057** [+0.0033, +0.0084] | -0.0072 [-0.0094, -0.0048] | `LEcmjtdd1qyl` |

In every registered pair, both seeds, both rungs and the official 4-gram, the null is at least as good as the treatment at ep20; the node 3 pair had the treatment ahead at ep10 and lost it by ep20. The only place the treatment leads a null at ep20 is the cross-pack e5 pair (k2lat_20_e5 -0.0437 vs k2shuf_20_e5 -0.0316 against ctrl_20, about 0.012 apart), which is not a registered paired read, crosses packs (its F is the ep4 difference of the two e5 arms, 0.0054) and does not clear M.

**Bands at ep20 (amendment 9 item 8):** B_ctrl 0.001 (prepro); B_k2 = |k2lat_20_s1 - k2lat_20| = 0.0092 [0.0069, 0.0120] (`BXSZA2yWPo0A`); B_null = |k2shuf_20 - k2shuf_20_s1| = 0.0136 [0.0118, 0.0154] (`NIwHpRkXSRW5`); B_off4 = |off4_k2lat_20_s1 - off4_k2lat_20| = 0.0062 [0.0032, 0.0092] (`84WDmbEMYuCF`). M for the node 1 pairs = max(0.0092, 0.0136, |F|, 0.010) = 0.0136 before F.

**Design 5 (b), the ep4 agreement of the lexicon-free arms (on-set 8 for every arm but the e5 pair, on-set 5), pre-registered tolerance 0.001, disagreement = CANNOT_TELL clause of the gate:** k2lat_20 - k2shuf_20 at ep4 = +0.0111 [+0.0081, +0.0148] (`usEfLapwXFeS`), eleven times the tolerance. Offsets from ctrl_20 at ep4: k2lat_20 +0.0126, k2shuf_20 +0.0015, k2lat_20_e5 +0.0039, k2lat_20_ma3000 -0.0131, k2shuf_20_ma3000 -0.0098, k2shuf_20_e5 +0.0093, off3_k2lat_20 +0.0063, off4_k2lat_20 -0.0149, off4_k2shuf_20 -0.0003; the s1 identity checks k2lat_20_s1 - ctrl_20_s1 -0.0009 [-0.0037, +0.0020] (agrees), off4_k2lat_20_s1 - ctrl_20_s1 +0.0080 (does not). For scale the full-seed band ctrl_20 - ctrl_20_s1 at ep4 is +0.0021 [+0.0002, +0.0039] (`0HBfQbqoUxld`). At ep1 every arm is within 0.0007 of ctrl_20 (k2shuf_20 +0.000000, off4_k2shuf_20 -0.000006, k2lat_20 -0.00036 [-0.00066, -0.00006], k2lat_20_e5 -0.00055, off4_k2lat_20 -0.00065); a zero paired delta is a net zero over changed utterances, not bit identity (debugger: 71-627 utterances change per arm at ep1, 210 for k2shuf_20). Debugger dispatched on this (`reports/debug_k2_preonset_divergence_2026-09-22.md`); the engagement monitors, health clause (derangement gap), S / D / I and emitted rate are being extracted (`reports/extract_k2_gate_inputs_2026-09-22.md`).

Secondary contrasts at ep20: rung 3000 beats rung 1000 for the treatment (k2lat_20_ma3000 - k2lat_20 -0.0249 [-0.0279, -0.0219], `Jp2KOte10s3R`) and its null gains the same; the official 4-gram (pruned theta 5.0) is worse than the official 3-gram 1e-7 (off4 - off3 +0.0077 [+0.0052, +0.0100], `8HafzKjk0CvR`) and better than the in-house trigram (off4_k2lat_20 - k2lat_20 -0.0088 [-0.0113, -0.0064], `KFAyl7hxIPIx`).

Reading before the monitors land (not the verdict): no arm can PASS G4a.9 on these numbers. The lexicon term lowers dev-other PER by 0.03-0.06 against ctrl_20 in every pack and seed, and the shuffled-pronunciation null lowers it by the same amount or more, i.e. the gain is the added prior weight, the alternative the gate's second clause was written to separate. Whether the formal read is FAIL or CANNOT_TELL (Design 5 (b) clause; engagement clauses) is decided on the debugger's and extractor's reports, then audited. The official 4-gram over-count (`XRbHuFzSqvS3` / `pkDzxP1kCCoH`, loop b5fxcfz1n) still gates node 3's read.

**Engagement, health and rate clauses (`reports/extract_k2_gate_inputs_2026-09-22.md`; monitors logged from each arm's on-set sub-epoch, `train_loss_lexlat_k2_*` in `learning_rates`):** `lexlat_k2_stability` is below the 0.05 ceiling in every arm from sub-epoch 11 on (largest 0.044, k2shuf_20 at 12; the only values above 0.05 are k2shuf_20 / k2shuf_20_s1 at sub-epochs 8 and 10, before the clause counts), so no rung reads CANNOT_TELL. `lexlat_k2_empty_frac` at most 0.0010 in any sub-epoch (ceiling 0.02). Escape share (expected escape words over expected words) at most 0.24 (off4_k2shuf_20; treatments 0.0001 in-house, 0.05 official 4-gram, 0.20 official 3-gram; ceiling 0.90). Magnitude match of `lexlat_k2_term_mean` over sub-epochs 8-11: treatment -0.39 to -0.32, null -0.48 to -0.39 per unit frame, ratio 1.2 (limit 2), so the null is a magnitude-matched weight control and clause 2 is decisive. Phones per word, the disclosed cross-posterior proxy (bed E[N] over `lexlat_k2_expected_words`, band [2.51, 5.01] from the token-weighted mean 3.579): treatments 3.4-3.5 (k2lat_20 34.0 expected words at sub-epoch 11, 32.7 at 16, over 114.5 / 114.7 expected tokens; off4_k2lat_20 3.4-3.6) inside the band; every null 5.4-6.9 (about 20 then 17-19 expected words per utterance) above it from sub-epoch 8 on. As the clause is written it is not arm-specific, so by the letter it fires for every NULL arm (UNINFORMATIVE) and for no treatment; the audit rejected an orchestrator narrowing of the clause to the treatment as post hoc. Substantively: the shuffled-pronunciation null holds the term magnitude (1.2x) but not the word-level statistics fixed (40 % fewer expected words per utterance), a limit of this null disclosed with the read. Health clause: dev-other derangement gap at ep20 3.04-3.61 for the eight arms with a registered ep20 dev-other read (k2lat_20 3.42, k2shuf_20 3.33, k2lat_20_ma3000 3.61, k2shuf_20_ma3000 3.12, k2lat_20_e5 3.32, k2shuf_20_e5 3.40, k2shuf_20_s1 3.04, off3_k2lat_20 3.45); k2lat_20_s1, off4_k2lat_20_s1 and off4_k2shuf_20 carry only a dev-clean ep20 read (3.69 / 3.96 / 3.82) and off4_k2lat_20 only ep10 dev-other, a registration gap in the pack configs (queue). Emitted phone rate at ep20 (`decode_stats.json`, `phone_rate_original_hz`) 7.3-8.1 Hz in every arm, inside [5.80, 14.49]. Every arm's plain PER is 0.81-0.86, so the PER < 0.50 PASS condition is missed by every arm as well.

**Design 5 (b) disagreement, diagnosis (`reports/debug_k2_preonset_divergence_2026-09-22.md`, read-only, before the audit):** the ep4 scatter is run-to-run GPU nondeterminism of the banked bed amplified by the cold phase, not a pre-on-set code difference. Evidence: the k2 leg is guarded by `active()` (`train_steps/sae_blankfree.py:157`) and the graphs load lazily inside `step()`, the first HLG load appears after "start epoch 8" in k2lat_20's log (epoch 5 for the e5 arms); k2lat_20's `returnn.config` differs from ctrl_20's only in the `lexlat_k2_*` block; same torch 2.7.1 build, no deterministic mode, cuDNN Conv1d and gather backward on the bed. All 57 ep1 step lines agree with ctrl_20 to three decimals and the ep1 sub-epoch means to 2e-7 in all twelve arms; the first divergence is at ep2 steps 2-4 (learning rate 1e-5 -> 1e-4, temperature 8 -> 5.04), 1e-3 by ep2 and 1e-2 to 1e-1 by ep4 (l_tau -0.38 -> +2.40). The pairs with IDENTICAL pre-on-set configs differ most at ep4 (k2lat_20_ma3000 - k2lat_20 -0.0257, off4 - off3 -0.0213), above every treatment-vs-null pair, so the ep4 spread is the bed's own. Consequence, as amendment 9 item 8 allows (M only grows): F is the identical-config ep4 spread, 0.013-0.015 (s1 pack 0.008), M = 0.015 for the node 1 pairs; clause 2's sign is positive in all four registered pairs, so no admissible M changes the reading. The ep4 offsets do not predict the ep20 treatment-null gaps (r about 0.4 over four pairs) and a persistence to ep20 is not measurable from these runs. Verdict written after the fresh-context audit (`reports/audit_k2_gate_read_2026-09-22.md`).

**VERDICT G4a.9, k2 form, all three packs: CANNOT_TELL as the gate is written, with PASS unreachable under any admissible margin** (audited from a fresh context, `reports/audit_k2_gate_read_2026-09-22.md`: Q1 CONFIRMED, Q2 CONFIRMED, four non-decisive transcription defects corrected above). The deciding clause is Design 5 (b): the ep4 paired disagreement between each pack's lexicon-free arms is +0.0111 (node 1, `usEfLapwXFeS`), -0.0033 (node 2, `fruH5gZCS6lC`) and -0.0146 (node 3, `KpPo3SVRhxA5`) against the pre-registered 0.001, and the letter of the Gate makes that CANNOT_TELL. The audit confirms the cause is the bed's own run-to-run nondeterminism (the k2 leg returns 0 before the on-set, `lexlat_train.py:109`, and two arms that differ only in a constant read inside `step()` sit -0.0257 apart at ep4), so the tolerance was set for a determinism this bed does not have; the gate is left as written and the tolerance is not rewritten after the fact. Independently of that clause, no arm can PASS: clause 2 (treatment - shuffled null <= -M, M = 0.0136 from B_null 0.0136, B_k2 0.0092, |F| <= 0.0126, floor 0.010) is positive with its whole confidence interval above zero in all four registered pairs (+0.0153, +0.0197, +0.0044, +0.0057), and every arm's PER is 0.81-0.84 against the < 0.50 condition, while clause 1, the rate (8.1 Hz), the health clause (gap 3.42) and every engagement monitor for the treatment are met. What the round licenses: not funding the marginalised k2 lexicon term further at this operating point on this bed (rung 1000 / 3000, in-house trigram, official 3-gram and pruned 4-gram, on-set 5 / 8, two seeds); it says nothing about whether the term could work with a different null, escape price, bed or pruning (gate-decision-vs-measurement). What the round found: the term reliably lowers dev-other PER by 0.03-0.06 against ctrl_20 at ep20 in every pack and seed, and a magnitude-matched shuffled-pronunciation null lowers it by the same or more, so the gain is prior weight and sharper phone posteriors, not lexical content; the null's own word posterior is coarser (5.4-6.9 phones per expected word against the treatment's 3.4-3.6 and the lexicon's 3.58), so the treatment does engage the lexicon without that engagement reaching the PER. Follow-up arms (random-pronunciation null, escape price, lam_lex, larger rung) are the user's call and are not launched on this read.

## Diagnosis: why the k2 form failed (user instruction 2026-09-22, "analyze why this failed")

Descriptive reads, no gate; each carries its interpretation rule written before the number. Arms unless stated: k2lat_20, k2shuf_20 (node 1, rung 1000), k2lat_20_ma3000, k2shuf_20_ma3000 (node 2), off4_k2lat_20, off4_k2shuf_20 (node 3), ctrl_20 (trigram-only control), gold (`GoldPhonesJob.ZGSp0hxyd2YP`), GAN (wav2vec-U 2.0 s0, update 148000, the attrib phase's row). Epochs ep4 (before the on-set), ep10, ep20. Split dev-other (2864 utterances) greedy decodes, `output/exp2025_11_06_speech_llms/librispeech/sae_4a_lexlat_k2_pack{,2,3}/<arm>/ep<N>/dev-other/greedy_raw.json`; "private code" here means these decodes, as in the prior phase's JSD read (the training-set decode is not dumped; disclosed). Conventions of the prior phase JSD read (SIL stripped, unbiased window `orN768ARKwlt`, trigram `RtzbESkOedsT`, rows subsampled to the smallest row, 1000 utterance-block resamples). Configs: `config/sae_4a_lexlat_k2_why.py` (CPU readers) and `config/sae_4a_lexlat_k2_why_gpu.py` (model-forward jobs); outputs under `output/sae/4a/lexlat_k2_why/`.

- **D1, n-gram JSD to the text.** `NgramModeSeekingJob` rows: all nine arms above at ep4 / ep10 / ep20 (gold, GAN once), orders 1-4, plus mean log P3 per phone. Interpretation: if the treatments' JSD3/4 sit at ctrl_20's level (about 0.50 / 0.74 at ep10) the lexicon term did not move sequence structure; if they move toward the GAN (0.08 / 0.25) but the nulls move as far, the movement is prior weight; a treatment-only movement is lexical content that the PER read did not see.
- **D2, loss curves.** Reader over each arm's `train/learning_rates` (bed terms, total, `lexlat_k2_term_mean`, `_lam`, `_expected_words`, `_expected_escape_words`, `_stability`, `_empty_frac`) per sub-epoch 1-20, with dev-other PER at ep4 / 10 / 20 from `per.json`; one figure per quantity (treatment, null, ctrl overlaid, both seeds where they exist) and a table of the last-5-sub-epoch slope. Interpretation: total loss and PER still falling at ep20 by more than the run-to-run band (0.009-0.014 PER) means not converged (D6 decides the follow-up); a flat PER with a still-falling lexicon term means the term is being satisfied without changing the argmax.
- **D3, word structure of the decodes.** New reader: best lexicon segmentation of every decoded string (`lexlat.string_best_segmentation` on the trie `LexiconTrieBuildJob.rlMsnTBSZXsB`, escape allowed at the registered price): per row the fraction of phones covered by in-lexicon words, escape rate per parsed word, words per utterance, phones per word, distinct words used, mass of the 100 most frequent words, and the fraction of parsed words that are also in the utterance's gold word sequence (word recall against gold, order-free). Rows: all nine arms, three epochs. Interpretation: gold sets the ceiling; if treatments parse into more in-lexicon words than ctrl_20 but with no word-recall gain over the nulls, the output is word-shaped without being the right words (structure learned, content not); if coverage does not exceed ctrl_20's, the model did not follow the word structure at all.
  D3 addendum (after the frequency-stratified read below): word recall is also reported per word-frequency decile (the reader's convention: decile 0 rarest, 9 most frequent, equal token mass, unseen = -1) with a paired utterance-block bootstrap of treatment minus null per decile.
- **D0, frequency-stratified PER (registered disclosed read, `LexlatFreqStratPerJob`, six jobs FINISHED 2026-09-22 11:xx, `reports/extract_k2_freqstrat_2026-09-22.md`; dev-other ep20, phones attributed to their gold word's training-text frequency decile, no CI column).** Deciles 0-7 (rare to mid-frequency words, 80 % of the tokens): the null is at or below the treatment in nearly every cell, as in the pooled read. Decile 9 (the most frequent types, 10,475 tokens): ctrl_20 0.877; k2lat_20 0.741 against k2shuf_20 0.784; k2lat_20_s1 0.758 against k2shuf_20_s1 0.793; k2lat_20_e5 0.794 against k2shuf_20_e5 0.835; off4_k2lat_20 0.692 against off4_k2shuf_20 0.757; but k2lat_20_ma3000 0.784 against k2shuf_20_ma3000 0.714 and off4_k2lat_20_s1 0.772 against 0.757. Reading (no interval, so suggestive): on the highest-frequency words the true lexicon beats its derangement by 0.04-0.07 in four of six pairs while losing everywhere else, the signature of lexical content confined to the few most frequent words (or of mode-seeking onto them); D3's per-decile word recall with a paired interval decides which, and whether the rung-3000 reversal is noise.
- **D4, reverse-model gap, gold / shuffled / decoded.** `BlankfreeDerangementGapJob` already gives gold minus deranged gold per utterance for every arm and epoch. New GPU job (same reverse model and scoring path): gold minus DECODED per utterance, and deranged-gold minus decoded, paired over utterances with a speaker-clustered bootstrap. Interpretation: a decoded string that the reverse model scores above gold (negative gap) means the recognizer's output is what the generative side prefers, i.e. the lexicon term is fighting the reverse model, not the audio; a gap far above the derangement gap (3.0-3.6) means the decodes are worse than shuffled gold for the reverse model, i.e. the recogniser is not near the gold basin at all.
- **D5, word-LM conformance of the private code.** `LexlatK2OvercountJob`'s scoring path (HLG intersection with the string's linear FSA, `lm_score` levels) on the decodes: per token log P under the HLG (word LM through the lexicon, escape included), escape share of the best parse, and the same under the shuffled HLG; rows: gold, GAN, ctrl_20, the six k2 arms, ep20 (ep10 for the node 1 pair). Interpretation: treatments' per-token HLG score at gold's level with PER 0.83 means the term is met by a fluent non-transcription (the mode-seeking outcome); at ctrl_20's level means the term never reached the argmax.
- **D6, optimisation adequacy.** Table from the configs: recognizer 1-layer conv 1024 -> 40, 20 sub-epochs x 57 steps = 1,140 updates at learning rate 1e-4, against the GAN's 150,000 updates with the same architecture; D2's slopes. Follow-up EXPERIMENT (not a read; registered here, funded only by the user): extend k2lat_20 / k2shuf_20 / ctrl_20 to 60 sub-epochs from the ep20 checkpoints on one 4-GPU node (about 3 x 4.5 h), reads as the packs. Interpretation of the read: 1,140 updates is 0.8 % of the reference's; if D2 shows both PER and the lexicon term still moving, "too short" is the leading explanation and the extension is the next arm.
- **D7, pruning mass.** New GPU probe on the trained ep20 checkpoints of k2lat_20 and k2shuf_20 (and ctrl_20's ep20 posterior through the same graph), 100 fixed dev-other utterances, rungs 1000 / 3000 / 10000: log Z per rung, |log Z(ma) - log Z(10000)| per frame (the stability monitor, now on the trained model), the exact gold-string score under the graph (`string_log_sum`), gold posterior mass exp(score_gold - log Z(ma)), gold survival (gold linear FSA intersected with the pruned lattice non-empty), tropical best-path score and its posterior mass (top-1 share), and the lattice's expected number of distinct words per utterance. Interpretation: gold survival under 50 % or gold mass under 1e-6 at rung 1000 with survival at 10000 means the pruning starves the gold path; top-1 mass above 0.5 means the lattice collapsed onto one parse and the gradient updates few paths; a stable log Z across rungs with low gold mass means the loss is dominated by non-gold paths regardless of pruning, i.e. the objective, not the approximation.
- **D9, lattice oracle PER (user instruction 2026-09-22: "run oracle PER on the k2 arm experiments, PER_oracle(L) = min over y in L of EditDistance(y, gold) / |gold|, L the lattice similar to what we use in training, so we know whether the lattice still contains something closer to ground truth"; registered before any number).** Job `LexlatK2OracleJob` (GPU, new module, config `config/sae_4a_lexlat_k2_why4.py`, own manager), the D7 lattice build verbatim (arm checkpoint in eval mode, temperature of that sub-epoch, `k2.intersect_dense_pruned` with the arm's HLG, search beam 20, output beam 8, min_active 30, one utterance per intersect) on the D4 utterance set (500 seed-0 dev-other utterances), then per utterance the minimum phone edit distance over all lattice paths (`k2.levenshtein_graph` on the gold, `k2.levenshtein_alignment` against the lattice with its arcs relabelled to run-collapsed phone tokens, SIL mapped to epsilon on both sides, the plain-PER convention) divided by the gold length. Rows: k2lat_20_ma3000 at ep4 / ep10 / ep20, k2shuf_20_ma3000 ep20, k2lat_20 ep20, k2shuf_20 ep20, ctrl_20 ep20 through the treatment graph, and the flat init (sub-epoch 0) through the treatment graph, each at the arm's own rung (1000 or 3000) and at 10000; for k2lat_20_ma3000 ep20 additionally the SAME posterior through H alone (the phone topology without lexicon or word LM, same beams and rungs), the lexicon-free reference. Columns per row: corpus oracle PER (sum of minimum edits over sum of gold tokens, as the plain PER) with a utterance-bootstrap CI, median per-utterance oracle PER, the lattice 1-best path's PER under the graph's own scores, the banked greedy PER of the same utterances, lattice arcs / states (median), and the fraction of golds not admissible through H (an adjacent repeat; the oracle needs no admissibility, the fraction is context for D7's 0/73 survival). Interpretation, written now: an oracle PER of the pruned lattice far below the greedy 0.81-0.86 (<= 0.50) means the marginalised lattice still holds paths much closer to the truth and the failure is one of preference (the objective does not favour them); an oracle within 0.10 of the greedy PER means the lattice, pruned by the recognizer's own posterior, has already lost the truth, and the rung-10000 row says whether pruning or the posterior did it. The H-only reference at matched rung bounds the lexicon's part: an HLG oracle worse than the H-only oracle means the lexicon graph excludes the closer paths. The flat-init row is the oracle of an uninformed posterior at the same rung, the chance level for this graph and beam. Caveat fixed now: a larger lattice has a lower oracle by construction, so rows compare only at matched rung, with arc counts beside them. Checks the job must pass before a number is read: a hand-built lattice with known paths returns the known minimum; a utterance whose gold survives in the lattice (D7's survival check) returns exactly 0; oracle <= 1-best PER on every utterance. Amendments before launch (implementer `reports/impl_k2_why4_oracle_2026-09-22.md`): (a) the ctrl_20 and flat-init rows run D7's full ladder 1000 / 3000 / 10000 so every row has a matched rung; (b) the flat init at the sub-epoch-1 temperature 8.0 is not computable (a near-uniform posterior at tau 8 gives 53-65 M states / 211-254 M arcs per utterance against about 2 k / 3.5 k for a trained arm, overflowing k2's int32), so the flat-init row is read at tau 2.0, the temperature every trained row is read at; it remains the uninformed-posterior calibration and is labelled with its temperature. (c) WITHDRAWN before launch (`reports/impl_k2_why4_flat_tau_2026-09-22.md`): even at tau 2.0 the flat-init lattices are 0.9-1.6 M states per utterance, about 300 s per utterance (41 h for the 500), and two of six test cells still overflow k2's int32 with a length bias, so the row is not a matched-set reference; the chance-level calibration is not read, disclosed. The ctrl_20 row (a trained but lexicon-free posterior through the treatment graph) and the H-only row remain the references.

**Literature (`reports/lit_k2_lexicon_prior_failure_2026-09-22.md`, full texts read), what changes the plan.** (i) For a marginalised LM-prior loss of exactly our form, identifiability of the recognizer rests on the position-dependent phone-unigram matrix P_C (row n = the prior's marginal over phones at position n) having full column rank: Yang, Schlüter, Ney (arXiv 2603.02285, 2026, theorem 1, sigma_min(P_C) about 3e-4 on LibriSpeech text), Wang, Hasegawa-Johnson, Yoo (ACL 2023, theorem 1), and ESPUM (ICASSP 2024, table 3: bigram-only 71.6 PER, plus positional unigram 39.2, unigram plus 5-gram 77.9). A pronunciation derangement changes the word-to-phone map but leaves a positional-unigram matrix of the same kind, so a null gaining as much as the treatment is the predicted outcome, not a defect. (ii) A stronger LM inside a sum-over-paths criterion is a reproduced negative in supervised MMI (Woodland and Povey, ASR 2000, table 4; Michel and Ney, Interspeech 2019: bigram 13.4 beats 4-gram 13.9 WER), and there the LM scale is inert while the acoustic scale (our tau) is decisive (0.2 / 1 / 10 LM scale gives 14.2 / 14.2 / 14.3; AM scale 0.1 / 0.2 / 1 gives 26.0 / 14.7 / 14.2). (iii) Forward-only (alpha) beam pruning "reduces the benefit of MMIE" where alpha-beta pruning does not (Qin and Rudnicky, ICASSP 2010; Kuang and Povey, Interspeech 2022); k2's `intersect_dense_pruned` prunes on the forward score alone, so D7's kept-mass read is on the unfavourable variant by construction. (iv) An LM score of the decode is no witness of content (REBORN, table 5: PER 14.7 to 12.9 while LM perplexity rises 10.0 to 11.2); Lin et al. (ICASSP 2022) find the phone 4-gram JSD the only order with a success threshold (about 0.27), and PER in the 0.81-0.87 band is outside the range where PER is informative. Consequences: D1 is read on JSD4 against 0.27; D5 is a conformance read, not a content read; the follow-up arm, if any, is tau (the posterior temperature), not lam_lex; and D8 below is the closing read.

- **D8, identifiability channel, CPU only.** New reader: the position-dependent phone-unigram matrix P_C (positions 1..N, N = the 95th percentile of the phonemised text length; phones 40, SIL handling stated) induced by (a) the true lexicon on the text (the same unbiased text window phonemised word by word with the HLG's lexicon, first pronunciation, as the treatment HLG was built), (b) the deranged lexicon on the same text (the exact derangement of the node 1 null HLG `LexlatHLGBuildJob` shuffled build, recovered from its build inputs / seed), (c) the bed's phone-trigram prior `RtzbESkOedsT` (exact forward recursion over positions, length distribution of the text); report sigma_min(P_C), the condition number and ||P_C^+||_1 for each, with a utterance-block bootstrap on (a) and (b). Interpretation (fixed before the number): if (a) and (b) agree within the bootstrap spread, the theory's identifiability channel is the same for treatment and null and the round measured the predicted outcome (mechanism closed at this bed); if (a) is materially better conditioned than (b) (sigma_min ratio above 2 with non-overlapping intervals), the objective had a channel it did not use, and D7's pruning read becomes the live suspect.

  D2 read (`LexlatK2LossCurvesJob.PcVAskOT57HB` FINISHED 2026-09-22, `output/summary.md`, `curves.json`, 64 figures; ten arms). (i) Schedule: the effective learning rate is 1e-5 at sub-epoch 20, decaying 1.1e-5 per sub-epoch over the last five, i.e. the runs end annealed to a tenth of the 1e-4 peak after 1,140 updates; every "plateau" at ep20 is under a vanishing step size, so it is not evidence of convergence. (ii) PER: ctrl_20 is flat, 0.875 / 0.869 / 0.875 at ep4 / 10 / 20 (ctrl_20_s1 0.873 / 0.880 / 0.876), while every k2 arm drops about 0.04 between ep4 and ep10 (k2lat_20 0.888 -> 0.849, k2shuf_20 0.877 -> 0.834, k2lat_20_ma3000 0.862 -> 0.825, off4_k2lat_20 0.860 -> 0.834) and at most 0.015 more to ep20. (iii) The lexicon term (train_loss_lexlat_k2, per unit frame) is still falling at ep20 but slowly (slopes -0.001 to -0.005 per sub-epoch) and is LOWER for the treatments (0.285-0.306) than for their nulls (0.315-0.359): the true lexicon is easier to satisfy, and does it with 27-32 expected words per utterance (3.5 phones per word) against the nulls' 16-17. (iv) The bed's own prior-matching loss falls with any lexicon graph: agg 1.52 (ctrl) to 1.03-1.47, its bigram-KL part 1.38 to 0.95-1.25, with the reverse-model term and the acoustic term l_tau unchanged (1.81-1.87); the k2 arms also lower the phone rate (8.8 -> 8.2-8.5 Hz) toward the reference. Reading: the lexicon term's effect on the recognizer runs through the phone-level statistics (bigram KL, rate), where treatment and null act alike; the two arms differ only in how the term itself is satisfied (word count, term value). Convergence (user question 2): not established; the schedule anneals out before the term stops moving, so a longer run with the learning rate held is the experiment that separates "too short" from "the objective is satisfied", which is D6's registered follow-up.
  D6 read (`LexlatK2OptimisationTableJob.ZabTTvf9FK9J` FINISHED, `output/summary.md`, every cell names its source file): ctrl_20 and k2lat_20 ran 20 sub-epochs x 57 steps = 1,140 updates each (batch 88,000 feature frames, max 128 sequences, no gradient accumulation, Adam betas 0.5 / 0.98, peak learning rate 1e-4 in a 20-value schedule from 1e-5 back to 1e-5); the bed recognizer has 1.42 M parameters in 8 tensors (in_proj 1024x1024, batch norm, conv 40x1024x9) against the wav2vec-U 2.0 generator's 1.46 M in the same 8 shapes (proj 44x1024x9), which took 148,000 selected of 150,000 configured updates. Same architecture, 0.77 % of the reference's updates, under a schedule that anneals by ep20. In data seen (`reports/extract_gan_batch_2026-09-22.md`, from the GAN job's train.log config dump and manifests): the GAN reference trained at batch_size 160 utterances, max_tokens None, update_freq 1, on the same 28,539-utterance bed, so 148,000 updates are about 830 passes; the k2 round's 1,140 updates at about 125 utterances each (88,000 frames, max_seqs 128) over the bed's 28,254 training segments are 5 passes (user's reading 2026-09-22, confirmed). The update-count comparison is therefore not flattering to the reference: per pass over the data the gap is 830 to 5, about 0.6 %. Not read: the GAN's distributed world size (if above 1 the reference's effective batch and passes are larger still). With D2 (the lexicon term and the bed's prior term still moving at ep20, PER moved only in ep4-10), "too short" is the leading explanation for the optimisation question and the registered extension (learning rate held, 60 sub-epochs, k2lat_20 / k2shuf_20 / ctrl_20) is the experiment that tests it; it is the user's to fund.
  D8 first read (in-process on the full window, `LexlatPositionalUnigramJob.F4m7uZm4hRRH` registered, speech-llm commit 15776380, `reports/impl_k2_why_pc_2026-09-22.md`; the sisyphus run is the record): 1,000,000 sentences, N = 179, 39 SIL-free phones, rows sum to 1; derangement reproduced against the null build's dumped `shuffled_lexicon.json.gz` on all 151,731 words. sigma_min: (a) true 7.05e-4 [8.0e-4, 1.05e-3 bootstrap], (b) deranged 3.48e-4 [4.3e-4, 5.3e-4], ratio 2.03 [1.65, 2.25]; (c) trigram prior 2.6e-18, rank-deficient at fp64 (||P_C^+||_1 6.6e14). By the rule as written: CANNOT_TELL (the per-row intervals are disjoint, the ratio interval straddles 2). Disclosed confound found by the implementer: deranged sentences are 1.83x longer, and at its own 95th-percentile length (N = 321) the deranged matrix reads 7.9e-4, above the true lexicon's, so the factor 2 at common N is a length effect, not a channel difference. The registered run FINISHED 2026-09-22 (`work/speech_llm/sae/emc/lexlat_positional_unigram_jobs/LexlatPositionalUnigramJob.F4m7uZm4hRRH/output/summary.md`, `P_C.npz`) reproduces every number above (sigma_min 7.0508e-4 / 3.48151e-4, ratio 1.948 [1.651, 2.249] over 200 resamples, derangement 0 mismatches); it is the record. Reading (to be audited with the campaign): the true and deranged lexica induce comparably conditioned positional-unigram matrices (Yang et al.'s 3e-4 order reproduced), while the phone-trigram prior alone has essentially none (its positional marginals reach stationarity within a few positions), which is a candidate mechanism for the round's result: any word-lexicon graph, true or deranged, adds the identifiability channel the trigram-only control lacks, and the PER gain of 0.03-0.06 is that channel, not the lexical content.

  D2 addendum, the reverse model's own curve (same job, `curves.json`, `reports/extract_k2_why_reverse_curve_2026-09-22.md`; `dev_loss_blankfree_reverse_per_frame` = the posterior-expected reverse log-likelihood per retained frame, E[sum_u G] / frames with G = log p_phi(segment | unit, duration) + log p(d | k), lattice.py:61 and :410, so HIGHER = the reverse model explains the held-out audio better given the recognizer's own units; it is a fit statistic, not a loss). Dev, sub-epochs 1 / 2 / 4 / 8 / 10 / 15 / 20: ctrl_20 -6.94 / -6.62 / -4.09 / -3.47 / -3.45 / -3.29 / -3.26; k2lat_20 -6.94 / -6.62 / -4.22 / -3.50 / -3.52 / -3.39 / -3.35; k2shuf_20 -6.94 / -6.62 / -4.06 / -3.46 / -3.43 / -3.31 / -3.28; k2lat_20_ma3000 ... -3.28 at ep20, k2shuf_20_ma3000 -3.32, off4_k2lat_20 -3.41, off4_k2shuf_20 -3.27; seed twins ctrl_20_s1 -3.11, k2lat_20_s1 -3.29, k2shuf_20_s1 -3.29. Last-5 slopes +0.009 to +0.017 per sub-epoch in every arm. Reads: (1) the reverse model is identical across arms through sub-epoch 2 (the lexicon term is off until its on-set), gains 3.5 nats per frame by sub-epoch 8 and is still climbing at ep20 in every arm, i.e. NOT converged; the curve is the same shape for treatment, null and control. (2) Treatments sit 0.02-0.15 nats per frame BELOW their nulls and the control at ep20 (the lexicon constraint pulls the units slightly away from what the reverse model fits most easily), but the seed spread on the control alone is 0.15 (ctrl_20 -3.26 vs ctrl_20_s1 -3.11), so no treatment-vs-null difference in this statistic is readable at n = 1-2 seeds. (3) Together with D4 (gold-vs-decoded preference growing ep4 to ep20) this is the self-consistency loop still tightening at the end of the run: the reverse model keeps fitting the recognizer's units and neither the lexicon term nor the derangement changes that.

  D1 read (`NgramModeSeekingJob` ep4 `FVXXwgk1BNNu` / ep10 `HmwYyqhEt1oH` / ep20 `lpf6rkrlMSPC`, plus the three k2shuf-control-slot twins, all FINISHED 2026-09-22; `reports/extract_k2_why_jsd_2026-09-22.md`): decode sets against the unbiased 1 M-line text window (70.2 M phones, SIL-stripped, Witten-Bell interpolated, JSD in bits, count-matched to the smallest row of each job: 167,588 / 142,428 / 134,710 phones at ep4 / ep10 / ep20, so ACROSS-epoch movement is confounded with budget until the pooled why2 job `qfpxgx1o7u3m` reads; WITHIN one job the rows are comparable). JSD3 / JSD4 at ep20: gold 0.073 / 0.248, GAN 0.084 / 0.277, ctrl_20 0.506 / 0.748, k2lat_20 0.498 / 0.714, k2shuf_20 0.456 / 0.695, k2lat_20_ma3000 0.466 / 0.705, k2shuf_20_ma3000 0.501 / 0.730, off4_k2lat_20 0.521 / 0.734, off4_k2shuf_20 0.513 / 0.751. At ep4: ctrl 0.554 / 0.771, k2lat_20 0.532 / 0.751, k2shuf_20 0.524 / 0.750; at ep10: ctrl 0.503 / 0.744, k2lat_20 0.485 / 0.711, k2shuf_20 0.450 / 0.697. Reads: (1) the GAN sits at gold's level (its JSD4 0.277 against gold's own 0.248, the domain gap between dev-other transcripts and the LM text), every k2 arm and the control sit at 6-7x gold's JSD3 and about 3x its JSD4, a different regime altogether: the k2 decodes' n-gram statistics are as far from text as the control's. (2) The lexicon term moves the decode statistics by at most 0.01-0.05 bits toward text against the control, and the DERANGED null moves them as much or more (k2shuf_20 below k2lat_20 at ep10 and ep20; the ma3000 and off4 pairs split), so the n-gram read agrees with the PER read that the lexical content is not what the term delivers. (3) Lin et al.'s 0.27 is a descriptive line in the job, not a gate; the job's own pre-registered clauses ((a) >= -0.10 nats, (b)/(c) >= +0.05 bits with CI excluding zero, (d) within +-0.10 bits) are read in the extraction report: the treatment-vs-null clause (c) gives k2lat_20 minus k2shuf_20 JSD4 = +0.0006 / +0.0135 / +0.0192 at ep4 / ep10 / ep20 (positive = the null is closer to text), FAIL against the +0.05 margin every time; (a) and (b) PASS trivially, (d) FAILs because the control is 0.50 bits from gold. The user's item 1 is answered: the k2 arms are at the control's distance from text, not the GAN's.

  D7 read (`LexlatK2MassProbeJob` x5 FINISHED 2026-09-22, `work/speech_llm/sae/emc/lexlat_k2_mass_jobs/`, `output/summary.txt` + `by_max_active.json`; `reports/extract_k2_why_mass_2026-09-22.md`, summaries quoted verbatim): 100 fixed dev-other utterances, of which 73 carry a gold string admissible through H (27 excluded for an adjacent repeat, the same 27 in every job); search beam 20, output beam 8, min_active 30, one utterance per intersect; gold convention PRIMARY = the bed's SIL-inclusive edge-silence form, SECONDARY = SIL-stripped, both exact forced scores. Per arm at rung 1000 (kept-mass gap |log Z(1000) - log Z(10000)| per retained frame; median gold posterior mass exp(score_gold - log Z)): k2lat_20 ep4 0.0195 nats/frame, 1.9e-122; ep10 0.0128, 6.8e-185; ep20 0.0092, 1.2e-165; k2shuf_20 ep20 (null graph) 0.0240, 1.4e-153; ctrl_20 ep20 posterior through the treatment graph 0.0227, 3.4e-119. At rung 3000 the gap is 0.004-0.008 nats/frame. Gold survival 0/73 at EVERY rung including 10000, both conventions, every job; the lattice's own best path survives (method check ok). Top-1 share median 0.0000 at every rung (k2lat_20 ep4 rung 1000: 1.8e-7); expected distinct words per utterance 12.5-13.6 on the treatment graph, 6.1 on the null graph; distinct word support 216-422 at rung 1000, 460-1076 at 10000. Reading against the registered interpretation: the pruning keeps the mass (the rung-1000 lattice is within 0.01-0.024 nats per frame of the rung-10000 one, i.e. the two-stage approximation loses about 1-2 % of the retained log-mass, not a decisive amount); the gold path is not starved by pruning but absent from the model altogether (its exact score sits 270-430 nats below log Z, so it would not survive any beam); and the lattice has not collapsed onto one parse (top-1 share about 0), so the gradient is spread over hundreds of word hypotheses per utterance rather than concentrated on a few. Answer to the user's third question: no, the approximation did not lose the mass; the loss is dominated by non-gold paths regardless of pruning, which is the "objective, not the approximation" branch of the pre-registered interpretation. Code review of the GPU jobs (`reports/review_k2_why_gpu_2026-09-22.md`): APPROVED_WITH_FIXES; the one defect (the survival fraction's denominator excludes empty lattices while the line prints it over all scorable golds, lexlat_k2_mass_jobs.py:646-695) moves nothing here because every job has 0 empty lattices and 0/73 survival; two notes with no number moved: the gold graph score is string_log_sum / tau (a lower bound within 1 nat of the tempered score, the campaign's convention), and the summary's claim that the PER read's gold is SIL-inclusive is wrong (blankfree_eval_jobs.py:85,89 strips it), so the PRIMARY / SECONDARY labels stand as two disclosed conventions, not as "the PER read's". D4's BlankfreeDecodeGapJob reproduces the banked gap column by construction (17 triples verified on disk, asserted to 1e-3). Caveat: the gold-mass numbers are per utterance and their magnitude is mostly the recognizer's PER 0.85 (the per-frame gap is not computed by the job), so they cannot distinguish the k2 objective from the bed's own drift; the ctrl_20 row (bed without the term) has the same order of magnitude.

  D4 read (`BlankfreeDecodeGapJob` x17 FINISHED 2026-09-22, `work/speech_llm/sae/emc/blankfree_decode_gap_jobs/`, arm and epoch from each output json's `name` field; `reports/extract_k2_why_gap_2026-09-22.md`): 500 seed-0 dev-other utterances per job, the arm's own reverse model scores the utterance's frames given (i) the SIL-inclusive gold phone string, (ii) a same-speaker derangement of the gold strings, (iii) the arm's greedy run-collapsed decode; per-utterance sums in nats, positive gold-minus-X = the reverse model prefers gold; speaker-clustered bootstrap, 2000 resamples, seed 0. Every job's registered decoded-vs-deranged-decoded gap per frame reproduced its banked value to 0.0 (the D4 job is the same primitive as the training-time gap). Reads: (1) gold minus decoded is about -800 to -1190 nats per utterance in every arm and epoch (ctrl_20 ep20 -1187 [-1297, -1091]; k2lat_20 ep20 -1016 [-1106, -934]), i.e. the reverse model reconstructs the audio far better from the recognizer's own decode than from the true transcript, and the preference grows with training (k2lat_20 ep4 -827, ep10 -1022, ep20 -1016; ctrl_20 -796, -1148, -1187): the reverse model has learned the recognizer's private unit-to-audio map, not the phone-to-audio map. (2) gold minus deranged gold is small but positive everywhere, 15 to 74 nats per utterance (0.5 to 3.5 % of the gold score), so the reverse model carries only a weak preference for real phone order over a shuffled one; against it the training-time decoded-vs-deranged-decoded gap is 2.7-4.3 nats PER FRAME, hundreds of nats per utterance, so the objective's gap term is almost entirely about the decode's own consistency, not about phone structure. (3) Unregistered contrast, to be read paired before it is claimed: at ep20 the treatment arms show a larger gold-vs-deranged margin than their nulls in all three pairs (k2lat_20 40.6 [30.4, 55.3] vs k2shuf_20 22.9 [12.8, 36.4]; k2lat_20_ma3000 68.5 [57.2, 82.9] vs k2shuf_20_ma3000 21.3 [10.4, 35.5]; off4_k2lat_20 73.9 [59.2, 92.0] vs off4_k2shuf_20 27.7 [15.8, 42.0]; ctrl_20 35.7 [24.5, 50.6]), the ma3000 and off4 pairs with disjoint unpaired intervals; a paired reader over the banked per-utterance rows is added to the why2 config. If it holds paired, the true lexicon does move the reverse model toward real phone order, a signal the PER read at 0.85 cannot show, of the order of 1-2 % of the utterance score.

  D3 read (`LexlatDecodeWordStructureJob` ep4 `bstM0OiMFKq1` / ep10 `5o7poiMU1u87` / ep20 `80Lypk0oLUVt` FINISHED 2026-09-22, `output/summary.md`; `reports/extract_k2_why_wordstruct_2026-09-22.md`; dev-other greedy decodes, best segmentation on the banked trie with escape, order-free word recall against gold, 1000 utterance-block resamples, rows paired inside each resample). Coverage (fraction of phones inside in-lexicon words) / word recall, ep4 -> ep10 -> ep20: k2lat_20 0.548 -> 0.729 -> 0.816 / 0.043 -> 0.064 -> 0.075; k2shuf_20 0.423 -> 0.514 -> 0.572 / 0.032 -> 0.023 -> 0.028; ctrl_20 0.461 -> 0.449 -> 0.517 / 0.038 -> 0.031 -> 0.036. ep20 also: k2lat_20_ma3000 0.708 / 0.032 vs k2shuf_20_ma3000 0.542 / 0.016; off4_k2lat_20 0.891 / 0.069 vs off4_k2shuf_20 0.579 / 0.041; GAN 0.922 / 0.617; gold 0.989 / 0.810. At ep20 the treatments parse at 3.1-3.2 phones per word, 16-17 words per utterance and escape rate 0.03-0.04 (gold 3.4 / 18.2 / 0.01) against the nulls' 4.5-5.2 / 9-11 / 0.08 and ctrl_20's 4.9 / 11.9 / 0.08; the 100 most frequent word types carry 0.61-0.63 of the treatments' in-lexicon tokens against 0.49 in gold and 0.45-0.53 in nulls and ctrl. Paired treatment minus null word recall, pooled: node 1 +0.047 [+0.045, +0.050], ma3000 +0.015 [+0.014, +0.017], off4 +0.028 [+0.026, +0.031]; per frequency decile the gain is zero below decile 5 and sits almost entirely in deciles 8-9 (node 1 +0.136 / +0.313, off4 +0.309 in decile 9 with deciles 7-8 NEGATIVE, -0.014 / -0.022; ma3000 spread over deciles 5-9 at +0.02 to +0.04); recall of words below decile 8 is under 0.05 in every k2 arm, against 0.35-0.73 for the GAN. Reading under the registered rule: coverage exceeds ctrl_20 in every treatment (by 0.19-0.37) and by 0.03-0.06 in the nulls, so the recogniser followed the word structure of whichever lexicon it was given, and the true lexicon takes the decodes to GAN-level word shape; word recall over the null is positive in all three paired pairs but of the order of 0.02-0.05 against a 0.6-0.8 ceiling, and confined to the most frequent word types, so the verdict is the rule's "structure learned, content not": the term is satisfied by emitting short in-lexicon words at the gold rate, drawn from the few hundred most frequent types, in the wrong places. This resolves D0's open question the same way: the decile-9 PER advantage in four of six pairs is mode-seeking onto the most frequent words, not lexical content reaching the rest of the vocabulary.

  D4 addendum, sub-epoch 1 rows and the paired phone-order contrast (`BlankfreeDecodeGapJob` x9 at kept sub-epoch 1 under `config/sae_4a_lexlat_k2_why3.py`, all FINISHED 2026-09-22, outputs `output/sae/4a/lexlat_k2_why/decode_gap/<arm>/ep1/`; `LexlatK2DecodeGapPairedJob.xYMtltVwFmJv` FINISHED; `reports/extract_k2_why_gap_ep1_paired_2026-09-22.md`). (1) At sub-epoch 1, before the lexicon term is on, gold minus decoded is already negative, -53.5 [-59.8, -47.6] nats per utterance for ctrl_20 (decode above gold on 488 of 500 utterances), identical to 1e-3 across the seven same-init arms and -0.20 vs -0.22 nats per frame for the two s1 twins, i.e. the reverse model prefers the recogniser's units over gold from the first sub-epoch and the preference then grows 15- to 20-fold by ep20 (-800 to -1190). (2) gold minus deranged gold at sub-epoch 1 is -5.6 [-8.6, -3.2] nats per utterance (median 0.0; every arm's per-frame interval crosses zero): the reverse model starts with NO preference for real phone order; the 15-74 nats per utterance seen at ep20 is learned during training. (3) The unregistered contrast of the D4 read now holds paired (positive = the first arm's reverse model separates gold from its same-speaker derangement by more, same utterances and donors in both arms, 2000 speaker-clustered resamples): treatment minus null at ep20 k2lat_20 - k2shuf_20 +17.7 [+10.7, +24.4], ma3000 pair +47.2 [+37.6, +57.4], off4 pair +46.2 [+34.7, +58.5], and at ep10 +12.7 / +54.4 / +45.6, all intervals above zero; treatment minus ctrl_20 at ep20 +4.8 [-3.4, +14.1] (node 1, crosses zero), +32.8 [+21.8, +43.8] (ma3000), +38.2 [+24.3, +52.1] (off4). Reading: the true lexicon does teach the reverse model more real phone order than its derangement does, by 1-2 % of the utterance score, and at rung 3000 and on the official 4-gram graph also more than the trigram-only control; but this is a property of the generative side (reverse model given the recogniser's units), invisible in the PER read, and small against the -1000 nats per utterance by which the same reverse model prefers the private code over gold. Taken with D3, the lexicon term moves both models toward frequent-word structure while leaving the unit-to-phone map private.

  D9 read (`LexlatK2OracleJob` x8 and `LexlatK2OracleTableJob.MpnfEz5SAjG6` FINISHED 2026-09-22 17:18, `output/table.txt` / `table.json`; the eight rows are all trained checkpoints, the flat-init row withdrawn as recorded above; every row's own greedy of the loaded checkpoint reproduced the banked decode to +0.0000; 500 utterances scored at every rung, none skipped). Oracle PER [CI95] at the arm's own rung, then at rung 10000, with median arcs, beside the banked greedy PER of the same utterances: ctrl_20 ep20 through the treatment graph 0.654 (1000, 7024 arcs) / 0.624 (3000, 11632) / 0.593 (10000, 21058), greedy 0.877; k2lat_20 ep20 0.688 (1000, 5266) / 0.649 (10000, 13738), greedy 0.846; k2shuf_20 ep20 through the shuffled graph 0.703 (1000, 3418) / 0.639 (10000, 16328), greedy 0.828; k2lat_20_ma3000 ep4 0.611 (3000, 11462) / 0.578 (10000, 21336), greedy 0.861; ep10 0.667 (3000, 4898) / 0.644 (10000, 8958), greedy 0.825; ep20 0.645 (3000, 7421) / 0.620 (10000, 13718), greedy 0.818; k2shuf_20_ma3000 ep20 0.680 (3000, 5231) / 0.646 (10000, 11729), greedy 0.817; the H-only reference (k2lat_20_ma3000 ep20 posterior through the phone topology alone, same beams) 0.424 [0.420, 0.428] at both rungs with 3777 arcs (the rung never binds, beam 20 does). The lattice 1-best under the graph's own scores is 0.80-0.83 in every HLG row, 0.02-0.06 better than greedy. Intervals are +-0.004, so every difference quoted is outside them. Reading under the registered rule: (1) neither branch of the rule fires; the HLG oracle sits 0.16-0.25 below greedy, above the 0.50 "far below" mark and outside the 0.10 "already lost" band, so the pruned training lattice still holds paths a fifth closer to the truth than the argmax, but nothing near it; rung 10000 buys only 0.03-0.06, so the posterior, not the pruning, sets the floor. (2) The H-only oracle at 0.42 with HALF the arcs of the rung-3000 HLG lattice means the recogniser's own posterior does carry paths at 0.42 PER and the lexicon graph is what excludes them: composing with L and G raises the oracle of the same posterior from 0.42 to 0.65 (rung 3000) or 0.62 (rung 10000), the registered signature "the lexicon graph excludes the closer paths", i.e. the word-decomposable paths the term rewards are systematically farther from the transcript than the best phone-topology paths. (3) Over training the treatment's lattice loses truth at matched rung: k2lat_20_ma3000 0.611 (ep4) -> 0.667 (ep10) -> 0.645 (ep20) at rung 3000 while greedy improves 0.861 -> 0.818, and the lexicon-free ctrl_20 posterior through the same treatment graph keeps a lower oracle than every trained treatment at every matched rung (0.654 vs 0.688 at 1000; 0.624 vs 0.645 at 3000; 0.593 vs 0.620-0.649 at 10000). (4) Treatment vs null at matched own rung: the true lexicon's lattice holds slightly closer paths than the shuffled one (0.688 vs 0.703 at rung 1000; 0.645 vs 0.680 at 3000) but the order reverses or vanishes at rung 10000 (0.649 vs 0.639; 0.620 vs 0.646), so the lattice content of the two graphs is the same within 0.03 once the beam is wide. Answer to the user's question: the training lattice does still contain something closer to the truth than what the model emits (about 0.62-0.69 against 0.82-0.88), but the closest paths the recogniser knows (0.42) are not word-decomposable under the lexicon and are removed by the graph before the objective ever sees them; the failure is one of preference and of admissibility together, and the objective's preference among the paths it can see moves the argmax by only 0.02-0.06 (the 1-best column).

  D5 read (`LexlatK2OvercountJob` x10 under `config/sae_4a_lexlat_k2_why.py`, `work/speech_llm/sae/emc/lexlat_k2_arm_jobs/`, and the paired reader `LexlatK2OvercountPairedJob.kC90mF8uo1YH`, all FINISHED 2026-09-22; `reports/extract_k2_why_overcount_2026-09-22.md`). The rows print the over-count statistic of amendment 9.5 (log-sum minus best path per token) rather than the per-token HLG log-probability the rule is phrased on; that quantity is in the paired reader as `hlg_log_prob_per_token` (k2 log-sum over tokens), on ONE common set of 1,737 strings scored in every row, point medians, no interval (descriptive, as the job states). Per-token HLG log P, word-boundary HLG: gold -2.36, GAN -3.21, k2lat_20 -3.93, k2lat_20_ma3000 -4.20, k2shuf_20_ma3000 -4.23, k2shuf_20 -4.31, ctrl_20 -4.35 (ep10: k2lat_20 -4.14, k2shuf_20 -4.35). Official 4-gram graph: gold -2.15, GAN -3.03, off4_k2lat_20 -3.43, off4_k2shuf_20 -3.88, ctrl_20 -3.94. Under the SHUFFLED HLG every trained arm sits together, -4.40 to -4.60 (gold -4.02, GAN -4.37), so the treatments' gain is specific to the true lexicon graph. The over-count itself is 0.08-0.10 nats per token in every trained arm (gold 0.05, GAN 0.06-0.08), no arm distinguished. Reading under the registered rule: neither branch fires cleanly. The treatments are not at ctrl_20's level (the term did reach the argmax) and not at gold's (it is not a fluent non-transcription): k2lat_20 closes 0.43 of the 2.0 nats between ctrl_20 and gold, off4_k2lat_20 0.52 of 1.8, against the GAN's 1.14 / 0.92 at a PER of 0.2; the nulls close 0.04-0.12. The private code became more word-LM-conformant by about a fifth to a quarter of the distance to gold, i.e. as far as emitting frequent short words in the wrong places can take it (D3), and no farther.

  D1 addendum, pooled budget (`NgramModeSeekingJob.qfpxgx1o7u3m` FINISHED 2026-09-22, `work/i6_experiments/users/wu/experiments/unsupervised_asr/ngram_mode_seeking/`; all 23 rows count-matched to one budget of 134,710 phones so the ep4 -> ep20 movement is readable, review defect 1 of the why config; `reports/extract_k2_why_pooled_precision_2026-09-22.md`). JSD3 / JSD4 (bits) at ep4 -> ep20: k2lat_20 0.536 / 0.757 -> 0.498 / 0.714; k2shuf_20 0.528 / 0.757 -> 0.456 / 0.695; k2lat_20_ma3000 0.535 -> 0.466; k2shuf_20_ma3000 0.512 -> 0.501; off4_k2lat_20 0.616 / 0.796 -> 0.521 / 0.734; off4_k2shuf_20 0.586 -> 0.513; ctrl_20 0.556 / 0.776 -> 0.506 / 0.748; gold 0.073 / 0.248; GAN 0.084 / 0.277. Mean log P3 per phone (unbiased-window trigram) at ep20: gold -2.54, GAN -2.73, off4_k2lat_20 -2.76, k2lat_20 -2.96, k2lat_20_ma3000 -3.16, k2shuf_20_ma3000 -3.18, k2shuf_20 -3.29, ctrl_20 -3.37, off4_k2shuf_20 -3.44. Reading: at one budget every trained arm's JSD3 sits at 0.46-0.52 against ctrl_20's 0.51 and the GAN's 0.08, the movement from ep4 is 0.04-0.09 for every arm including the control and the nulls move as far as the treatments, so the lexicon term did not move sequence structure toward the text (the registered "did not move" branch, now free of the budget confound). The trigram likelihood of the treatments' output does rise, 0.4-0.6 nats per phone above ctrl_20 and to within 0.2-0.4 of gold, while their JSD does not: the output becomes more probable under the phone trigram without becoming distributed like the text, the mode-seeking signature (D3: frequent words over-produced).

  D3 addendum, word precision (`LexlatDecodeWordPrecisionJob` ep4 `yTTNCBLxiW23` / ep10 `8tgHrL9EshMd` / ep20 `WHZ5aL4GRMBo` FINISHED 2026-09-22, why2 config; hypothesis-side share of parsed in-lexicon words that the utterance really contains, pooled over utterances, 1000 utterance-block resamples; registered as review note 2 of the why config, not in the D3 bullet above; `reports/extract_k2_why_pooled_precision_2026-09-22.md`). ep20 precision / recall: k2lat_20 0.087 [0.084, 0.090] / 0.075; k2shuf_20 0.053 / 0.028; k2lat_20_ma3000 0.046 / 0.032; k2shuf_20_ma3000 0.035 / 0.016; off4_k2lat_20 0.075 / 0.069; off4_k2shuf_20 0.074 / 0.041; ctrl_20 0.058 / 0.036; GAN 0.565 / 0.617; gold 0.803 / 0.810. k2lat_20 over epochs 0.069 -> 0.081 -> 0.087. Reading: precision is as low as recall, so the parsed words are not a small correct subset inside noise; of the 44,000 in-lexicon words k2lat_20 emits over dev-other (gold has 51,000), fewer than one in eleven is in its utterance, against more than one in two for the GAN. This closes the D3 verdict from both sides: word-shaped, wrong words.

Write-up: one report for the user (`reports/diag_k2_why_2026-09-22.md`) with the seven reads and the answer to the three questions (prior conformance, optimisation, pruning mass), audited from a fresh context before it is called a conclusion. First draft audited 2026-09-22 (`reports/audit_diag_k2_why_2026-09-22.md`, CONFIRMED_WITH_CORRECTIONS): both tables exact against the job outputs; twelve prose ranges corrected (the rung-3000 treatment had been dropped from the word-level ranges: coverage 0.71-0.89 not 0.82-0.89, top-100 mass 0.46 / 0.61 / 0.63, gold is 3.41 phones per word and 18.2 words per utterance, the ep20 private-code preference is 945-1187 nats and no longer growing in the treatments, the update deficit is 130x / 166x, the D7 treatment-graph range is 0.009-0.023 over 73 scorable of 100 utterances); one claim inverted and corrected: the k2 arms do NOT leave PER where the control has it, every k2 arm is 0.03-0.06 below ctrl_20 at ep20 (D2 table) and the treatment sits within 0.01 of its null; D3's registered branch does not fire as written (the paired recall gain over the null is above zero) and the "structure, not content" reading is a judgment on its size; D5 reads between its two branches; D8 is CANNOT_TELL and carries no weight in the mechanism. The corrected draft, with the diagnostic pack read added, supersedes the first.

## D11: official 4-gram perplexity of dev-other and of the decodes (user instruction 2026-09-22, "check official 4gram LM perplexity on dev other and decoded dev other"; registered before any number)

Word-level perplexity under the official LibriSpeech 4-gram, the exact ARPA the off4 graph was built from (pruning stated), SRILM conventions: sentence boundaries scored, OOV words excluded from the perplexity and counted (ppl over words + </s>, and ppl1 without </s>, both printed). Rows, dev-other, 2864 utterances: (a) the reference transcripts; (b) the GOLD phone strings (`GoldPhonesJob.ZGSp0hxyd2YP`) turned into words by the decode rows' own segmentation, the ceiling that prices the segmentation itself; (c) the ep20 greedy decodes of ctrl_20, k2lat_20, k2shuf_20, k2lat_20_ma3000, k2shuf_20_ma3000, off4_k2lat_20, off4_k2shuf_20, and the GAN (wav2vec-U 2.0 s0, the attrib phase's row). Phone-to-word conversion: D3's LM-independent best segmentation on the banked trie (`LexiconTrieBuildJob.rlMsnTBSZXsB`, escape at the registered price, an escaped span = one OOV token), so the segmentation is not chosen by the LM being measured. Columns: perplexity, ppl1, OOV / escape rate, words per utterance, mean log10 P per word; paired treatment minus null and treatment minus ctrl_20 of the per-utterance log P per word (speaker-clustered bootstrap, 2000 resamples). Interpretation, written now: a treatment perplexity at or below row (b) with D3's word precision 0.05-0.09 means fluent non-transcription (the lexicon term found high-probability word sequences in the wrong places, mode-seeking); a perplexity several times row (b) means word-shaped but not fluent (words chosen one at a time, no 4-gram context), consistent with D5's per-token HLG score closing only a fifth to a half of the gap to gold; treatment vs null says whether the true lexicon buys word-sequence fluency the derangement does not.

**D11 read (2026-09-22; `LexlatOfficialLmPerplexityJob.dSN7ET4bM4OO` FINISHED, 22 min; `reports/extract_d11_ppl_2026-09-22.md`).** Reference row 140.12 = the banked value. Perplexity / OOV rate / words per utterance: reference 140.12 / 0.006 / 17.8; gold phone strings through the segmentation (row b) 857.8 / 0.012 / 18.2; GAN 2478 / 0.040 / 20.1; ctrl_20 18508 / 0.091 / 11.9; k2lat_20 10811 / 0.050 / 16.2 vs k2shuf_20 21195 / 0.094 / 10.2; k2lat_20_ma3000 28305 / 0.070 / 12.9 vs k2shuf_20_ma3000 36708 / 0.108 / 9.1; off4_k2lat_20 12487 / 0.042 / 16.9 vs off4_k2shuf_20 17445 / 0.095 / 10.8. Paired per-utterance log10 P per word, treatment minus null: k2lat_20 +0.102 [+0.064, +0.138], ma3000 -0.002 [-0.044, +0.036], off4 -0.052 [-0.090, -0.013]; treatment minus ctrl_20: k2lat_20 +0.080 [+0.048, +0.111], ma3000 -0.258 [-0.296, -0.217], off4 +0.012 [-0.017, +0.041]. Reading by the registered branches: every treatment sits 13-33x above row (b) (the GAN 2.9x), so the "word-shaped but not fluent" branch fires, not "fluent non-transcription": the words are chosen without word-sequence context, even under the official 4-gram the off4 graph was built from. The true lexicon buys word-sequence fluency over its derangement in one of three pairs (k2lat_20), ties in one and loses in one (off4). Caveat: the per-word figure is confounded with length (nulls emit 9-11 words per utterance against 13-17), and macro and pooled per-token means order off4 and its null differently (-4.04 / -3.99 macro, -4.10 / -4.24 pooled).

## D10: why the supervised init collapses (user question 2026-09-22, pre-registered before any number)

User: "what is the reason for the heavily degraded supervised init of the k2 arm? did you init the ASR and the reverse model both with gold phones? what is the error pattern, the loss convergence, did they learn a private code even in that case?" Facts on record before any new job: the diagnostic pack `5XNGdxndoY1Y` loaded ONLY the recognizer from gold (`SupervisedRecognizerExportJob.YtuVg6ZYuK0P`, 8 of 25 keys; the source's `reverse` block was never trained by the supervised job, which optimises -transcript_logprob of theta only) and that recognizer is 24 Adam steps from the flat init (pass 1 of 30, `epoch = 1 step = 24`); the reverse model phi started at the bed's random init. sup_plain is already at 0.846 PER after sub-epoch 1 (57 updates inside the lr warm-up), S 120k / D 15k / I 14k of N 177k, i.e. substitutions. Precedent on the blank-symbol bed (SAE_4A.md "S2f", "S2g"): a 24-pass supervised recognizer at 0.116 degraded to 0.263 (phi trained) / 0.245 (phi frozen) in 8 sub-epochs without the direct seed-KL anchor, held and improved only with it (0.109), and a gold-fitted phi improved further only under that anchor; the anchor is label-derived and absent here by design.

Reads on the finished pack (descriptive, no gate), dev-other:
- **D10a private code**: `PrivateCodeAnalysisJob` (version 2, the SAE_4A_infomax conventions) on p0 and sup_plain ep1 / ep2 / ep8, sup_k2lat ep1 / ep8. Rule: a degraded row whose one-to-one-with-drop PER is within 0.10 of p0's own is a RELABELLING (phone information kept under other names, the objective failed to pin the names); many-to-one PER >= 0.60 with NMI(symbol, phone) falling and NMI(symbol, unit) rising is a UNIT CODE (the private code of ctrl_50: NMI(symbol, unit) 0.377, many-to-one 0.855 at ep10); anything else reads PARTIAL.
- **D10b reverse-model preference**: `BlankfreeDecodeGapJob` for sup_plain and sup_k2lat at ep1 and ep8 (gold minus decoded; gold minus same-speaker deranged gold), read against the main line (ep1 -53.5, ep20 -800 to -1190 nats per utterance).
- **D10c curves**: per-sub-epoch bed terms, lexicon term and reverse-model dev statistic for the four arms, and the per-step loss inside sub-epoch 1 from the training log.
- **D10d init continuity**: the converted init decoded through the pack's own forward path must reproduce p0 = 0.1894 within 0.005; a miss voids every D10 read of the pack until explained.

**D10e, the both-gold pack (experiment; disclosed label-using diagnostic, enters no gate, initialises no main-line arm).** One exclusive node, the pack's constants verbatim (tau 2.0 held, lr schedule, 8 sub-epochs, kept 1 / 2 / 4 / 8, seed), one delta per arm against sup_plain: `supphi_plain` theta p0 + a gold-fitted phi, both trainable (the user's case); `supphi_frz` the same with phi frozen; `supphi_k2lat` = supphi_plain + the k2 term on the treatment graph (on-set 1, ramp 3, as the pack); `sup30_plain` theta = the pass-30 supervised checkpoint (0.2067, 30 passes, large margins) with the bed's random phi, the fragility contrast. Gold phi: the S2g fit (`SupervisedReverseInitJob.4GzzIJEpK5vp`, 10 h seed gold phone strings, latent durations, no timestamps) if its reverse block loads strictly into the blank-free model with the same ReverseConfig, else a refit of the blank-free reverse class by the S2g recipe on the same seed. Precondition read before the pack: under the gold phi, gold minus same-speaker deranged gold on dev-other has its interval above zero (phi is a gold contract), else no launch. Bands at ep8 (and ep1), per arm against its own init PER p: HOLD <= p + 0.10, COLLAPSE >= 0.70, PARTIAL between. Readings: supphi_plain HOLD = the untrained phi drove the pack's collapse; supphi_plain COLLAPSE with supphi_frz HOLD = the theta-phi co-adaptation (the private-code loop) drives it; both COLLAPSE = the unanchored objective pushes theta off gold even against a fixed gold generative model (the S2f pattern at a weaker start); sup30_plain HOLD with sup_plain COLLAPSE = fragility of the 24-step init is a cause. supphi_k2lat minus supphi_plain is read paired, sign with interval, descriptive. Each arm's ep8 also gets the D10a and D10b reads.

Amendment before any job (USER RULING 2026-09-22: "start a separately supervised init k2 training NOW"): the pack launches as soon as it passes code review; the phi precondition read runs beside it and is read before any pack number is interpreted, no longer as a launch gate (a precondition FAIL voids the pack's reading, it does not stop the run). Arms: `sup30_plain` is dropped and `supphi_k2shuf` (supphi_plain + the k2 term on the shuffled null graph, exactly as sup_k2shuf) takes its slot, so the k2 control of "Implementation controls" (ii) is re-read on the both-gold start with its registered clauses: supphi_k2lat - supphi_plain <= -0.010 AND supphi_k2lat - supphi_k2shuf <= -0.010 at ep4 and ep8 = implementation and direction CONFIRMED; supphi_k2shuf < supphi_plain - 0.010 = the added weight, not the lexicon, protects. The HOLD / PARTIAL / COLLAPSE bands above apply to all four arms; the fragility contrast (sup30) is not run.

**D10a-d read (2026-09-22 late evening; `reports/extract_d10_reads_2026-09-22.md`, curves `reports/extract_d10_curves_2026-09-22.md`).**
- D10d PASS: the converted init decodes 0.18945 through the pack's forward path (`BlankfreeGreedyPerJob.6A3JiTDnBLI1`; S 8678 / D 17885 / I 7021 of N 177275), within 0.005 of p0, so the reads below stand.
- D10a, per row PER / one-to-one-with-drop / many-to-one / NMI(symbol, phone) / NMI(symbol, unit) / symbol entropy in bits / top-5 symbol frame mass: p0 0.189 / 0.189 / 0.189 / 0.906 / 0.397 / 4.86 / 0.31; sup_plain ep1 0.846 / 0.846 / 0.855 / 0.098 / 0.189 / 2.81 / 0.88; ep2 0.901 / 0.866 / 0.879 / 0.070 / 0.282 / 3.82 / 0.63; ep8 0.891 / 0.830 / 0.819 / 0.068 / 0.358 / 4.65 / 0.42; sup_k2lat ep1 0.691 / 0.691 / 0.709 / 0.309 / 0.269 / 3.79 / 0.66; ep8 0.814 / 0.795 / 0.797 / 0.115 / 0.320 / 3.93 / 0.65 (gold-phone-vs-unit NMI 0.461 is the reference ceiling). No row is a RELABELLING: one-to-one-with-drop sits 0.60-0.68 above p0's, so phone identity is lost, not renamed. The UNIT CODE clause (many-to-one >= 0.60, NMI(symbol, phone) falling, NMI(symbol, unit) rising) did not fix its reference point: along each arm's training (ep1 to ep8) both arms meet it (sup_plain phone 0.098 to 0.068, unit 0.189 to 0.358; sup_k2lat phone 0.309 to 0.115, unit 0.269 to 0.320) and read UNIT CODE; against p0 the unit NMI also falls (0.397 to 0.358 / 0.320) and they read PARTIAL. Both readings are recorded; the ambiguity is the registration's, not resolved here. Descriptively: sub-epoch 1 (57 steps at lr 1e-5) collapses the output onto a few symbols (entropy 4.86 to 2.81 bits, top-5 mass 0.31 to 0.88; 18 of 40 gold phones have no majority symbol) and destroys unit information with phone information; sub-epochs 2-8 re-spread the symbols (entropy back to 4.65) along unit lines, reaching unit NMI 0.358 against ctrl_50's private code 0.377, while phone NMI stays at 0.07. The lexicon term slows the collapse (ep1 0.691 against 0.846) but follows the same path by ep8.
- D10b (500 dev-other utterances, speaker-clustered 95% intervals): sup_plain ep1 gold minus decoded -5.1 [-10.0, -0.3], gold minus deranged gold -5.5 [-8.4, -3.0]; ep8 -1077.8 [-1175.8, -991.6] (decode preferred on 500/500), +18.5 [9.5, 30.0]. sup_k2lat ep1 +0.9 [-4.2, 5.8] / -5.1 [-8.1, -2.7]; ep8 -767.8 [-835.4, -708.7] (500/500), +79.2 [67.6, 95.6]. At ep1 the reverse model cannot tell an utterance's gold from another speaker-matched utterance's gold (it is still the random init); by ep8 it prefers the recognizer's own decode over gold by the main line's ep20 magnitude (-800 to -1190) while separating right from wrong gold by only 18-79 nats: a contract on the decode, not on phones.
- D10c (sup_plain): dev reverse per frame -6.64 (ep1) to -5.17, -4.08, ..., -3.54 (ep8) while dev prior-matching loss worsens 1.11 to 2.00 and the retained phone rate stays 10.2-10.5 Hz.
- Reading: the collapse happens in sub-epoch 1 against a still-random reverse model, before any private code exists; the code (unit-tracking symbols, decode-preferring reverse model) is built afterwards on the collapsed start. This is consistent with the user's hypothesis that the untrained reverse model's gradient wrecks the supervised recognizer, but it does not separate that gradient from the prior-matching and rate terms acting in the same steps; D10e (supphi_plain against supphi_frz) is the test.

**D10e read (2026-09-23; pack `PackedBlankfreeTrainJob.WEmvqF2yBFG8` FINISHED, gold phi = refit `BlankfreeSupervisedReverseInitJob.16v7R6ztSq1u` epoch.008.pt, cost gate passed; numbers `reports/extract_d10e_pack_2026-09-23.md`; audit CONFIRMED_WITH_CAVEATS `reports/audit_d10e_pack_2026-09-23.md`).** The auditor traced all 16 PER values and the paired rows to their arm and epoch. Each arm differs from its comparison only by its registered delta, and the batches are bit-identical to the diagnostic pack's. Every arm loaded the refit checkpoint the precondition scored. In `supphi_frz` phi stays bit-identical and is left out of the optimizer.
- Precondition PASS (`BlankfreeDecodeGapJob.tD8bSgyBTQB8`, 500 utterances): under the gold phi at p0, gold minus same-speaker deranged gold is +1000.4 [+922.8, +1090.2] nats per utterance (+16.9 per gold token). Gold minus the p0 decode is +11.4 [-11.1, +27.5]: the decode is preferred on 154 of 500 utterances.
- Dev-other greedy PER at ep1 / 2 / 4 / 8, init p = p0 = 0.1894 (`BlankfreeGreedyPerJob.kL6MFqeFIfCy`). HOLD <= 0.2894, COLLAPSE >= 0.70.

| arm | ep1 | ep2 | ep4 | ep8 | band at ep1 / ep8 |
|---|---|---|---|---|---|
| `supphi_plain` | 0.1931 | 0.2641 | 0.2936 | 0.2561 | HOLD / HOLD |
| `supphi_frz` | 0.1868 | 0.2493 | 0.2663 | 0.2169 | HOLD / HOLD |
| `supphi_k2lat` | 0.1757 | 0.1881 | 0.2010 | 0.1797 | HOLD / HOLD |
| `supphi_k2shuf` | 0.2217 | 0.2870 | 0.3723 | 0.3656 | HOLD / PARTIAL |

Against the random-phi pack on the same recognizer and batches: `sup_plain` 0.846 / 0.891, `sup_k2lat` 0.691 / 0.814 ("D10a-d read").
- Paired rows (`PairedPerDeltaJob`, dev-other, negative = first arm better), ep4 / ep8:
  - frz minus plain: -0.027 [-0.031, -0.024] / -0.039 [-0.043, -0.034].
  - k2lat minus plain: -0.093 [-0.097, -0.088] / -0.076 [-0.080, -0.073].
  - k2lat minus k2shuf: -0.171 [-0.176, -0.166] / -0.186 [-0.191, -0.180].
  - k2shuf minus plain: +0.079 [+0.071, +0.086] / +0.110 [+0.103, +0.116].
- D10a at ep8, PER / one-to-one-with-drop / many-to-one / NMI(symbol, phone) / NMI(symbol, unit):
  - plain 0.256 / 0.256 / 0.294 / 0.797 / 0.404.
  - frz 0.217 / 0.217 / 0.253 / 0.835 / 0.387.
  - k2lat 0.180 / 0.180 / 0.232 / 0.859 / 0.366.
  - k2shuf 0.366 / 0.366 / 0.389 / 0.736 / 0.215.
  - For comparison, p0 reads 0.906 on NMI(symbol, phone).
- D10b at ep8, 500 utterances, gold minus decoded per frame:
  - plain -0.455 [-0.575, -0.370], decode preferred on 450 of 500.
  - frz -0.011 [-0.094, +0.049], 208 of 500.
  - k2lat -0.353 [-0.456, -0.275], 424 of 500.
  - k2shuf +0.448 [+0.324, +0.549], 103 of 500.
  - Gold minus deranged gold stays at +3.9 to +4.4 per frame in every arm.
- D10c: the dev lexicon term falls 0.290 to 0.247 in k2lat and 0.457 to 0.394 in k2shuf. Retained rate is 10.9-11.6 Hz in every arm.

**Readings by the registered branches.**
- **Collapse cause.** `supphi_plain` HOLD at ep1 and ep8 means the untrained reverse model drove the diagnostic pack's collapse. The same recognizer with the same batches reads 0.193 against 0.846 after sub-epoch 1.
- **Co-adaptation.** Both `supphi_plain` and `supphi_frz` HOLD, so co-adaptation is not a collapse driver. Descriptively, a trainable phi still costs 0.039 by ep8, and its preference already turns toward the recognizer's own decode: 450 of 500 against 208 for the frozen phi. This is the private-code loop starting from gold, slowly.
- **k2 control.** On the both-gold start, k2lat is at least 0.010 better than both plain and k2shuf at ep4 and ep8. The k2 control of "Implementation controls" (ii) reads implementation and direction CONFIRMED here, where on the random-phi start it read CONTROL_FAIL_9_7.
- **Added weight.** "The added weight, not the lexicon, protects" is refuted: the deranged-lexicon term makes the recognizer worse (+0.110 at ep8). The gain is the lexicon's content.
- **k2lat against its init.** `supphi_k2lat` ends 0.0097 below its init PER at ep8. This is unpaired; no row against the init is registered.

**Audit caveats.**
- The phi is a refit of the blank-free reverse class by the S2g recipe, not the S2g checkpoint; the reason is recorded only in the code.
- plain's ep8 HOLD is 0.033 inside the bound on one seed, and its ep4 read 0.294, just over it.
- About half of the k2lat-minus-k2shuf gap is the null getting worse through deletions. The null's term runs about 1.6x the treatment's.
- The whole result holds on a gold start only.

**Interpretation, for the user's fork (not a gate; D10e enters no gate and initialises no main-line arm).** The k2 lexicon term carries lexicon-specific signal when the recognizer's symbols are phones: NMI(symbol, phone) 0.86 here, against 0.07 on the cold line's unit code (D10a). On the main line, where the symbols are a unit code, the treatment sat within 0.01 of its derangement. The term preserves, and perhaps refines, a phone-like start. It does not create one.

**D10e addendum, paired rows against the init (orchestrator 2026-09-23, registered before any number; descriptive, no gate).** `PairedPerDeltaJob` rows for every supphi arm minus the p0 init decode (`BlankfreeGreedyPerJob.kL6MFqeFIfCy`) at ep1 / 4 / 8, dev-other, the pack's conventions (speaker-clustered bootstrap, 2000 resamples). Reading for `supphi_k2lat` at ep8:
- REFINES: delta <= -0.010 with the interval below zero. The lexicon term improves a phone-like start beyond its init on this seed.
- PRESERVES: the interval contains zero, or the delta is between -0.010 and 0.
- DEGRADES: the interval lies above zero.

**D10e addendum read (2026-09-23).** All 12 reads finished (`PairedPerDeltaJob`, commit 978913d4, `reports/exec_d10e_vs_init_2026-09-23.md`). Every row's base PER is 0.189446, and every candidate matches the banked value. Arm minus p0, delta [95% CI]:

| arm | ep1 | ep4 | ep8 |
|---|---|---|---|
| plain | +0.0037 [-0.0016, +0.0087] | +0.1042 [+0.0971, +0.1114] | +0.0666 [+0.0604, +0.0727] |
| frz | -0.0026 [-0.0074, +0.0020] | +0.0768 [+0.0714, +0.0823] | +0.0275 [+0.0212, +0.0340] |
| k2lat | -0.0138 [-0.0192, -0.0087] | +0.0116 [+0.0058, +0.0168] | -0.0097 [-0.0151, -0.0045] |
| k2shuf | +0.0322 [+0.0259, +0.0382] | +0.1828 [+0.1761, +0.1896] | +0.1762 [+0.1689, +0.1833] |

**Reading, as registered: `supphi_k2lat` ep8 PRESERVES.** The delta of -0.0097 misses the -0.010 REFINES threshold by 0.0003, although its interval lies wholly below zero. The job prints `refines=True` by its own interval-only convention; the registered rule governs. Descriptive:
- Every arm follows the same hump, worst at ep4 and recovering by ep8.
- The k2 term shrinks that hump from +0.104 (plain) to +0.012.
- The deranged lexicon's arm does not recover (+0.176 at ep8).
- One seed only.

## Extension E60, training length (user ruling 2026-09-22, pre-registered before any job)

**Ruling.** User 2026-09-22: "given current status, one thing we would definitely do is to train longer at least to reach the PER plateau; k2lat_20_ma3000 would be a nice candidate. On other modifications, let's wait for analysis." This resolves State NEXT (1): the direction is D6's registered extension with the rung-3000 treatment in place of k2lat_20; nothing else in the objective, the graphs or the bed changes until the diagnosis write-up (`reports/diag_k2_why_2026-09-22.md`) is read.

**Why (D2 / D6).** The 20-sub-epoch schedule anneals the learning rate to 1e-5 by ep20 while the lexicon term, the bed's prior term and the reverse model are all still moving, so the ep20 "plateau" is a step-size artefact; 1,140 updates are 5 passes over the bed against the reference's 830. A run with the learning rate held separates "too short" from "the objective is satisfied".

**Arms** (one pack, one exclusive 4-GPU node, one arm per GPU; names `<parent>_x60`, parents are FINISHED packs consumed as frozen paths):
- `k2lat_20_ma3000_x60`: the user's candidate, treatment at rung 3000; parent `PackedBlankfreeTrainJob.2j2bZo1TrkC0/output/k2lat_20_ma3000`.
- `k2shuf_20_ma3000_x60`: its derangement null at the same rung (without it the ep60 read has no paired null); parent the same pack.
- `ctrl_20_x60`: the trigram-only control, the "PER plateau" reference (ctrl_20 was flat, 0.875 / 0.869 / 0.875); parent `5EIGJJ1MkcO9/output/ctrl_20`.
- `k2lat_20_x60`: the rung-1000 treatment D6 registered, the rung contrast at ep60; parent `euWIAvH0xO5p/output/k2lat_20`.
Dropped for the slot count: `k2shuf_20` (D6's rung-1000 null); `k2lat_20_x60` reads against ctrl and the rung-3000 treatment only.

**Delta from the parent.** Each arm's written config is its parent's `returnn.config` except: `num_epochs` 60; `learning_rates` = the parent's 20 values followed by 40 x 1e-4 (held at the peak, as D6 registered; the step from ep20's 1e-5 back to 1e-4 at ep21 is a warm restart, disclosed); `temperature_schedule` = the parent's 20 values followed by 40 x 2.0 (the anneal ends at 2.0 by sub-epoch 4 and is held); `cleanup_old_models.keep` = [21, 30, 40, 50, 60]; `model` the new arm directory. The lexicon curriculum (onset 8, ramp 3, full_lam 1.0) is unchanged and already past: `lexlat_lambda` = 1.0 at every sub-epoch 21-60. Mechanism: RETURNN's own resume. The new pack job seeds `output/<arm>/models/` with the parent's `epoch.020.pt` and `epoch.020.opt.pt` and copies the parent's `learning_rates` file before RETURNN starts, so training begins at sub-epoch 21 with the Adam state; graphs, resources, seeds, data and batch shape unchanged. The job class is a subclass of `PackedBlankfreeTrainJob` in a NEW module with the parent checkpoints in its hash, never an edit of `blankfree_pack_jobs.py` (its file-sha version would re-hash the finished packs).

**Reads** (dev-other, kept sub-epochs 21 / 30 / 40 / 50 / 60, the packs' own readers): plain greedy PER with S / D / I, emitted rate, derangement gap; `PairedPerDeltaJob` rows: every arm minus `ctrl_20_x60` at 30 / 40 / 50 / 60; `k2lat_20_ma3000_x60` minus `k2shuf_20_ma3000_x60` at 30 / 40 / 50 / 60; `k2lat_20_ma3000_x60` minus `k2lat_20_x60` at 60; within-arm ep60 minus ep50 and ep60 minus ep40 (plateau rows) for every arm; ep21 minus the parent's ep20 (continuity) for every arm. The per-sub-epoch monitors (`lexlat_k2_*`, bed terms, reverse-model dev loss) log as before so D2's curve reader extends to 60.

**Gate E60** (thresholds fixed now; M = 0.015, the identical-config ep4 spread of the packs):
- Continuity clause, per arm, read FIRST at ep21: the training log's first sub-epoch is 21 with the optimizer state loaded, AND |PER(ep21) - PER(parent ep20)| <= M. A miss means the arm is not a continuation: CANNOT_TELL, the executor stops the pack, debugger.
- Plateau clause, per arm, at ep60: the paired delta ep60 - ep50 within [-0.010, +0.010] (the run-to-run band's lower edge) reads PLATEAU; below -0.010 reads STILL_FALLING and a further extension (61-100 under the same rule) is queued for the user; above +0.010 reads DEGRADING (reported, no further extension).
- Form re-read at ep60, G4a.9's PASS conditions verbatim on `k2lat_20_ma3000_x60`: PER < 0.50 AND rate in [5.80, 14.49] AND minus `ctrl_20_x60` <= -M AND minus `k2shuf_20_ma3000_x60` <= -M AND the health clause. Any miss leaves G4a.9's reading as it stands (FAIL / CANNOT_TELL as audited); a PASS needs the fresh-context audit and a second seed (standing rule).
- Abort rule as G4a.9's, re-armed from sub-epoch 21.

**Cost (corrected at code review, before launch).** The parents' own complete sub-epochs (consecutive `:meta:time` stamps, sub-epochs 16-20 of each parent's `learning_rates`, `reports/impl_k2_ext60_fixes_2026-09-22.md`): k2lat_20_ma3000 760.6 s, k2shuf_20_ma3000 948.6 s (the null graph is the slower one), k2lat_20 734.8 s, ctrl_20 610.0 s. Projections for 40 sub-epochs plus start-up: 8.47 / 10.56 / 8.18 / 6.79 h; the null binds with 0.9 h of slack under the 11.5 h allocation, and a wall hit costs one sub-epoch plus a resubmission (the seeding hook never overwrites an existing file, so a resubmitted pack keeps every arm's own record). The continuation's extra start-up (checkpoint + Adam load) is unmeasured. Original estimate: step probes at the run shape (Results "Step probes, the real train step"): rung 3000 827 s, rung 1000 789 s per sub-epoch, bed 601 s. 40 sub-epochs = 9.2 h / 8.8 h / 6.7 h plus five kept-epoch reads, inside one 11.5 h wall with about 2 h slack; the pack's resume contract (one sub-epoch lost per wall kill) is the mechanism the continuation itself runs on, so the continuity clause doubles as the resume test. About 46 GPU-h charged (the node whole) plus the reads.

**Launch record.** Build: speech-llm 9d79d0e5 + d80698b0 (`reports/impl_k2_ext60_2026-09-22.md`, review `reports/review_k2_ext60_2026-09-22.md`, fixes `reports/impl_k2_ext60_fixes_2026-09-22.md`). The first launch `PackedBlankfreeContinueJob.40FNqppkNZGK` trained nothing (the seeded `learning_rates` was not where the config's relative path looked; fixed in speech-llm 9e9f50d3, `reports/impl_k2_ext60_lrfile_fix_2026-09-22.md`) and is an orphan. The run of record is `PackedBlankfreeContinueJob.xsWAIaMMsWOw`, Slurm 1957557 on jpbo-053-01, from 2026-09-22 18:50 (`reports/exec_k2_ext60_relaunch2_2026-09-22.md`).

**E60 continuity read (2026-09-23 00:15; the pack's own `BlankfreeGreedyPerJob` readers, dev-other, 2864 utterances, N = 177275; `reports/extract_e60_continuity_2026-09-23.md`, two readers checked on disk).** Every arm's log loads `epoch.020.pt` and `epoch.020.opt.pt` and starts at sub-epoch 21, global train step 1140. PER at ep21 against the parent's ep20, delta, S / D / I at ep21 (parent):

| arm | ep21 | parent ep20 | delta | S / D / I at ep21 (parent ep20) | retained rate ep21, Hz |
|---|---|---|---|---|---|
| `ctrl_20_x60` | 0.8605 | 0.8746 | -0.0141 | 121757 / 23471 / 7312 (126601 / 18578 / 9860) | 10.31 |
| `k2lat_20_x60` | 0.8398 | 0.8435 | -0.0036 | 108198 / 37158 / 3525 (112527 / 32754 / 4245) | 9.19 |
| `k2lat_20_ma3000_x60` | 0.8158 | 0.8186 | -0.0029 | 100096 / 41819 / 2697 (104400 / 37315 / 3405) | 8.84 |
| `k2shuf_20_ma3000_x60` | 0.8135 | 0.8142 | -0.0007 | 92249 / 49760 / 2197 (98155 / 43400 / 2777) | 8.30 |

Clause: all four |delta| <= M = 0.015, so **continuity PASS in every arm**; the runs are continuations and the later reads stand. The control's -0.0141 sits 0.001 inside the bound. Descriptive, no gate: the one sub-epoch at the restored lr 1e-4 moves every arm the same way, deletions up by 4.4k-6.4k with substitutions and insertions down, so the warm restart first thins the output; the ep20 values were not a fixed point under the larger step, as D2 predicted. The treatment and null orderings at ep21 are not read (registered rows start at 30).

**E60 read at ep60 (2026-09-23; pack `PackedBlankfreeContinueJob.xsWAIaMMsWOw` FINISHED, Slurm 1957557 COMPLETED after 10 h 31 min; 20 greedy-PER, 20 derangement-gap and 29 paired readers FINISHED; numbers `reports/extract_e60_final_2026-09-23.md`; audit CONFIRMED_WITH_CAVEATS `reports/audit_e60_gate_2026-09-23.md`).** The auditor traced every decisive reader to its checkpoint in this run. Sub-epochs 21-60 ran at lr 1e-4, tau 2.0 and lexicon weight 1.0, and the null graph `UGE5MH3v5lyT` is a true derangement.

Dev-other greedy PER at parent ep20 and ep21 / 30 / 40 / 50 / 60:

| arm | ep20 | ep21 | ep30 | ep40 | ep50 | ep60 |
|---|---|---|---|---|---|---|
| `ctrl_20_x60` | 0.8746 | 0.8605 | 0.8734 | 0.8748 | 0.8747 | 0.8735 |
| `k2lat_20_x60` | 0.8435 | 0.8398 | 0.8490 | 0.8444 | 0.8441 | 0.8484 |
| `k2lat_20_ma3000_x60` | 0.8186 | 0.8158 | 0.8244 | 0.8203 | 0.8205 | 0.8240 |
| `k2shuf_20_ma3000_x60` | 0.8142 | 0.8135 | 0.8114 | 0.8142 | 0.8160 | 0.8169 |

Paired rows at ep60, delta [95% CI], negative = first arm better:
- `k2lat_20_ma3000_x60` minus ctrl: -0.0495 [-0.0562, -0.0439].
- `k2shuf_20_ma3000_x60` minus ctrl: -0.0566 [-0.0637, -0.0502].
- `k2lat_20_x60` minus ctrl: -0.0251 [-0.0304, -0.0202].
- `k2lat_20_ma3000_x60` minus `k2shuf_20_ma3000_x60`: +0.0071 [+0.0035, +0.0107].
- `k2lat_20_ma3000_x60` minus `k2lat_20_x60`: -0.0244 [-0.0278, -0.0214].

Ep60 minus ep50, per arm:
- ctrl: -0.0012 [-0.0029, +0.0001].
- k2lat_20: +0.0043 [+0.0029, +0.0058].
- ma3000: +0.0035 [+0.0025, +0.0044].
- k2shuf ma3000: +0.0008 [-0.0007, +0.0025].

Monitors from ep20 to ep60, flat throughout: ma3000's lexicon term 0.310 to 0.320, reverse per frame -3.28 to -3.28, prior per token -3.148 to -3.142. The other arms move alike.

Gate E60, clause by clause:
- **Plateau: PLATEAU in all four arms.** Every ep60 - ep50 interval lies inside [-0.010, +0.010], so no 61-100 extension is queued. Caveat from the audit: both lexicon treatments drift slightly upward (their ep60 - ep50 intervals are above zero, and ep60 is worse than ep21), so this plateau is not convergence at their best point.
- **Form re-read on `k2lat_20_ma3000_x60`: not PASS.** It misses two clauses: PER 0.824 against < 0.50, and minus its null +0.0071 against <= -0.015. It meets minus ctrl (-0.0495), the rate (7.81 Hz original audio, 9.20 retained) and the health clause (derangement gap 3.84). G4a.9's reading stands as audited.
- **Abort rule: no abort.** No NaN, |dev agg| <= 1.56, and the expected rate never goes below 8.19 Hz against a floor of 5.80.

**Reading.** D2 / D6's hypothesis that the ep20 plateau was a step-size artefact is refuted. Forty sub-epochs at the peak learning rate (2,280 more updates, twice the launch round's 1,140) moved no arm more than 0.01 from its ep20 PER and moved no monitored term. On this bed the objective sits at a fixed point, and training longer is not the missing ingredient.

At rung 3000 the gain over the trigram-only control is shared by the deranged lexicon: -0.057 for the null against -0.050 for the treatment. So it needs a word graph but not the correct word-to-pronunciation mapping. On the cold line the true lexicon carries no signal specific to the lexicon, even at 60 sub-epochs (+0.007 against its derangement). On a phone-like start it carries a large one ("D10e read": -0.186).

**E60 loss curves (2026-09-23, user question "check if all losses still reduced"; descriptive; the packs' CV-holdout dev scores, `reports/extract_e60_loss_curves_2026-09-23.md`).** Not all terms still fall, and the objective does not return to its ep20 level. ep20 was the end of the anneal at lr 1e-5. The warm restart at ep21 (lr 1e-4) raises l_tau by 0.05-0.06 in every arm. Over ep21-60 at the held 1e-4:
- l_tau recovers only part of that rise. ep20 / ep60: ctrl 1.811 / 1.836; k2lat 1.845 / 1.873; k2lat_ma3000 1.831 / 1.853; k2shuf_ma3000 1.850 / 1.864.
- The lexicon term gets worse in every k2 arm: 0.297 / 0.306, 0.310 / 0.320 and 0.314 / 0.326.
- agg falls in the two treatment arms (1.319 / 1.264; 1.092 / 1.025), is flat in ctrl (1.516 / 1.519) and rises in the null (1.357 / 1.369).
- rate and the reverse model's per-frame score are flat.
The comparison with ep20 is confounded by the learning rate: the extension never annealed.

## D12: word-histogram floor, the screen for the external word-level aggregate proposal (orchestrator 2026-09-22, pre-registered before any number)

**Context.** The user forwarded an external proposal (`reports/external_review_lexdist_2026-09-22.md`): (1) a word-level EMA aggregate term KL(c_text || c_hat) on posterior expected word counts of the HLG lattice, (2) per-word bonuses before pruning if support is limiting, (3) a one-substitution pronunciation channel. Assessment `reports/plan_external_review_assessment_2026-09-22.md`, design review `reports/design_review_external_lexdist_2026-09-22.md` (ADOPT_WITH_CHANGES, all changes applied here). Decisions: (2) not funded, stays conditional (its trigger, support of UNDERUSED words, is not on the record; D7 stored support counts and top-10 words only); (3) not funded on the science (the paths the recognizer holds are a unit code, D10a NMI(symbol, phone) 0.07, not words with one wrong phone; relaxation moves the graph toward H alone and dilutes the only lexicon-specific signal, treatment minus derangement <= 0.01; the proposal's own caveat), the graph-expansion form is also out of reach (edited-branch out-degree x ~40 against the int32 window and the rung-10000 memory limit), the emission-side confusion smear is cheap and was considered; (1) has a low prior (odm3, `SAE_4A_attrib.md` 477-498: the order-3 phone aggregate reached 0.64 / 0.45 nats of KL at lam 0.1 / 1.0 while PER stayed 0.88-0.91; a word histogram is invariant to which segment carries which word; ma3000 already has a gold-like top-100 share, 46 % vs 49 %, at word recall 0.032) and is screened by D12 before any GPU. Engineering: per the design review the installed k2 1.24.4 `Fsa.get_arc_post` is differentiable, so E[N_v] is one `index_add_` over word aux_labels on `_expected_words_one`'s pattern; to be confirmed by a finite-difference test before any use. D10e's branch does not govern (1).

**D12 read (CPU, banked data only).** Rows: the nine ep20 dev-other greedy decodes of D3 (`LexlatDecodeWordStructureJob.80Lypk0oLUVt`, same segmentation: best segmentation on `LexiconTrieBuildJob.rlMsnTBSZXsB`, escape at the registered price), gold phone strings through the same segmentation, the reference word transcripts, the GAN reference. Target c_text: word counts of the text the treatment graph's word trigram was estimated on, same normalisation. Buckets fixed now: the 1000 most frequent word types of that text individually, the remaining in-lexicon words pooled by pronunciation length (1, 2, 3, 4, 5, >= 6 phones), one escape / OOV bucket; row counts smoothed by +0.5 per bucket. Statistic K(row) = KL(c_text || c_row) in nats, 95 % utterance-block bootstrap (1000 resamples, paired across rows); also the top-100 share and the paired treatment-minus-null K. The read is on GREEDY histograms; posterior (E[N_v]) histograms are a different quantity and are not read here.

**Rule (fixed now).** SATISFIABLE_CONTENT_FREE if any row with D3 word precision < 0.10 has point K <= max(1.5 x K(gold phone strings), K(GAN)): a content-free output already matches the text's word histogram as well as a working recognizer, so the statistic cannot carry content and the word-aggregate arm is not funded. Otherwise ROOM: the arm test below becomes eligible and goes to the user; ROOM does not show the term would carry content (odm3 descended content-free), only that it has something to pull on.

**Arm test, drafted, not funded (needs D12 = ROOM and user go).** Warm-start pack from `k2lat_20` ep20 (`euWIAvH0xO5p`), E60's mechanism verbatim (lr 1e-4 held, tau 2.0, seeds), one node: `k2lat_20_wagg_x` (+ word term), `k2shuf_20_wagg_x` (deranged lexicon, word targets mapped through the same derangement), `k2shuf_20_x`, `k2lat_20_x` (in-pack plain continuations). Before code: the word term is a unigram aggregate BESIDE the trigram G, not the sequence prior (orchestrator reading of the trigram-or-better rule; user to confirm); ratio form with the EMA only in the detached denominator; lam_word from a gradient-norm profile at ep20, tau 2.0, >= 20 batches (attrib Step 5 rule); FD gradient test at production dtype and sum_v E[N_v] equal to the `lexlat_k2_expected_words` monitor to 1e-6. Reads at 30 / 40: treatment minus `k2lat_20_x` <= -0.015 AND treatment minus `k2shuf_20_wagg_x` <= -0.015 (M = 0.015, E60's cross-pack spread); word-K descent on treatment AND null; one-to-one PER (emitted phones must change); posterior and greedy histograms.

**D12 read (2026-09-23; `LexlatWordHistogramFloorJob.y2E7GNOuDjLK` FINISHED, `output/summary.md`, `word_floor.json`; audit CONFIRMED_WITH_CAVEATS, `reports/audit_d12_word_floor_2026-09-23.md`, the rule was committed before the job started).** VERDICT **ROOM**. Bound = max(1.5 x K(gold phone strings) = 0.2955, K(GAN) = 0.3632) = 0.3632 nats. K in nats (point) / D3 word precision / words per utterance: reference words 0.032 / 1.00 / 17.8; gold phone strings 0.197 / 0.80 / 18.2; GAN 0.363 / 0.56 / 20.1; the seven eligible ep20 decodes 1.786 (off4_k2lat_20, the nearest) to 2.229 (ctrl_20), precision 0.035-0.087. The nearest eligible row sits 1.42 nats above the bound (1.38 in the auditor's most adverse construction), about 100x the 0.010-0.019 size floor, so not borderline. The auditor recomputed seven rows to six decimals. Paired treatment minus null K: k2lat_20 -0.105 [-0.185, -0.067], ma3000 +0.178 [+0.128, +0.227], off4 -0.359 [-0.490, -0.304]: the true lexicon's decodes are not consistently closer to the text's word histogram than their derangement's (the rung-3000 treatment is further). Caveats from the audit: (a) every row, the nulls included, is parsed on the true-lexicon trie as registered (the job's "arm's trie" wording is a mislabel); on their own deranged trie the nulls sit at 3.06, further from the bound; (b) the percentile intervals lie above the points because the plug-in KL is biased upward and resampling adds the bias again (0.012-0.028 nats); the verdict uses point K and is unaffected; (c) the zero-mass escape bucket inflates content-free rows by at most 0.08 nats; (d) the screen reads GREEDY histograms; the arm would train on POSTERIOR expected counts, and ROOM says nothing about how far the posterior histogram already is from the text. Reading: the greedy word histograms of every content-free arm are about 9x further from the text than the gold phone strings, so a word-aggregate term has something to pull on; whether pulling carries content is untested (odm3 descended content-free). Per the rule, the drafted arm test goes to the user. Orchestrator addition to the drafted test, before any pack: read K on the ep20 POSTERIOR histogram E[N_v] (one dev-other forward on the treatment graph, the `lexlat_k2_expected_words` path) against the same bound; a posterior K at or below the bound means the term has nothing to pull on and the arm is not funded.

## D13: is the phone-like solution lower on the objective than the cold end point? (user question 2026-09-23, registered before any number)

User: "compare the loss/performance of reverse model from k2lat with and without supervised init ... to know if phoneme is at least kind of 'global optimum'"; "in case they are not comparable, just run a quick eval on dev other fairly".

**Logged read (descriptive; the packs' own dev split = CV holdout of the train stream, not dev-other; `reports/extract_goldinit_vs_cold_k2lat_losses_2026-09-23.md`).** Conditions match at gold ep8 vs cold ep20 / ep60: temperature 2.0, lam_lex 1, graph cdcxYJMjiYj5 at max_active 1000, prior RtzbESkOedsT, loss scales, dev set. The cold arms' code version is not checked. Per-frame dev terms, gold `supphi_k2lat` ep8 / cold `k2lat_20` ep20 / cold `k2lat_20_x60` ep60: l_tau 1.749 / 1.845 / 1.873; lexlat_k2 0.247 / 0.297 / 0.306; agg 0.211 / 1.319 / 1.264; rate 0.017 / 0.016 / 0.016; reverse model per frame (higher is better) -3.160 / -3.353 / -3.363. `supphi_plain` ep8 reads l_tau 1.725 at PER 0.256.

**CANCELLED by the user before any job (2026-09-23: "the score was comparable so I don't need that").** The logged read above is the answer: at matched conditions, the gold-init solution is lower on every term except rate, which ties, and its reverse model scores better. This answers the question in the sense of "PHONES LOWER", without the paired intervals and without the untrained gold start. The eval as registered is kept for the record:

**Eval (one registered job family, the training step's own loss code, no parameter update).** dev-other, 2864 utterances, identical settings for every row: temperature 2.0, lam_lex 1, lam_agg 0.1, lam_rate 3, the treatment graph at max_active 1000, the prior above, identical batches. Rows:
- G0, the gold start: p0 recognizer `YtuVg6ZYuK0P` + gold phi `16v7R6ztSq1u` epoch.008, untrained.
- G8k, `supphi_k2lat` ep8.
- G8p, `supphi_plain` ep8.
- C20, cold `k2lat_20` ep20.
- C60, cold `k2lat_20_x60` ep60.
Every row reports every term (per frame, as the dev scores) and the weighted total, plus per-utterance l_tau, lexlat_k2, rate and reverse per frame. Paired rows (speaker-clustered bootstrap, 2000 resamples) on the per-utterance sum l_tau + lexlat_k2 + 3 rate and per term: G8k - C60, G0 - C60, G8k - G0, G8p - G0; G8p - G0 also without the lexicon term.
- Identity check first: C20 through the eval path on the CV holdout, with the training dev batching, reproduces its logged dev l_tau 1.8446 and lexlat_k2 0.2965 within 0.005. A miss voids the read until explained.

**Reading (descriptive, no gate).**
- PHONES LOWER: G8k and G0 below both C20 and C60 on the weighted total, and G8k - C60 with its interval below zero. The phone-like solution sits lower on this objective than anywhere the cold run reached, so the cold failure is a search failure, not the objective preferring the unit code. This does not show that phones are the global optimum.
- CODE LOWER: C60 below G8k on the total, with the G8k - C60 interval above zero. The objective prefers the cold solution at this bed.
- MIXED otherwise; report per term.
- Secondary, stationarity: G8p below G0 on the objective without the lexicon term, while its PER is worse (0.256 against 0.189), means the bed objective descends away from phones: a phone-like point is not a local minimum without the lexicon term.

## D14: a reverse model fitted on the supervised recognizer's own output (user request 2026-09-23, registered before any job)

User: "supervised init asr model on gold, train reverse model on its output and see if joint/frozen training on k2 lat would improve or degradate". Disclosed label-using diagnostic: p0 is gold-trained. It enters no gate and initialises no main-line arm.

**Inputs.** theta = p0 (`SupervisedRecognizerExportJob.YtuVg6ZYuK0P`, dev-other 0.1894). decphi = `BlankfreeSupervisedReverseInitJob` with the gold phi's recipe and constants on the same 10 h seed utterances and split. Its phone targets are p0's greedy decodes of those utterances, dumped by a `ReturnnForwardJobV2` through the `BlankfreeGreedyPerJob` decode path, in place of the gold strings. The label source is the only delta from D10e's gold phi `16v7R6ztSq1u`. Descriptive, beside the packs: the PER of p0's seed decodes against seed gold (the label noise decphi is fit on), and the D10e precondition read under decphi (`BlankfreeDecodeGapJob`, dev-other, 500 utterances).

**Arms.** D10e pack constants verbatim (tau 2.0 held, lr schedule, 8 sub-epochs, kept 1 / 2 / 4 / 8, seed, batches; k2 block = `supphi_k2lat`'s: treatment graph `cdcxYJMjiYj5`, rung 1000, on-set 1, ramp 3). One arm per GPU.
- Pack W1: `decphi_plain` (p0 + decphi, both trainable); `decphi_frz` (phi frozen); `decphi_k2lat` (decphi_plain + the k2 term); `decphi_k2lat_frz` (decphi_k2lat with phi frozen).
- Pack W2: `supphi_k2lat_frz` (D10e's supphi_k2lat with the gold phi frozen, completing D10e's gold-phi 2x2); `supphi_k2lat_rep` (supphi_k2lat verbatim: the warm identity band); `supphi_k2lat_rp` (D15's warm arm); `decphi_k2shuf` (decphi_plain + the null graph `UGE5MH3v5lyT`).

**Band.** B_warm = |supphi_k2lat_rep - supphi_k2lat| (paired, dev-other) at ep4 and at ep8; M_w = max(0.010, B_warm at the same epoch).

**Reads.** Dev-other greedy PER at ep4 and ep8, `PairedPerDeltaJob` (speaker-clustered bootstrap, 2000 resamples), delta = first minus second, negative = first better.
1. Against the init, each W1 arm and `supphi_k2lat_frz` minus p0: REFINES if delta <= -M_w with the interval below zero; DEGRADES if delta >= +M_w with the interval above zero; PRESERVES otherwise. This differs from the D10e addendum's rule by the band and the symmetric DEGRADES clause; the addendum's read stands as registered.
2. Joint against frozen under the k2 term: `decphi_k2lat_frz - decphi_k2lat` and `supphi_k2lat_frz - supphi_k2lat_rep`: FROZEN BETTER if delta <= -M_w with the interval below zero; JOINT BETTER if delta >= +M_w with the interval above zero; TIE otherwise.
3. The k2 term's effect: `decphi_k2lat - decphi_plain` and `decphi_k2lat_frz - decphi_frz`: HELPS / HURTS / TIE by the same rule.
4. Lexicon specificity on the decode start: `decphi_k2lat - decphi_k2shuf` <= -M_w with the interval below zero = LEXICON-SPECIFIC.
5. Label source, descriptive: `decphi_k2lat - supphi_k2lat_rep` and `decphi_k2lat_frz - supphi_k2lat_frz`.
6. D10b at ep8 for the four W1 arms (gold minus decoded, gold minus deranged gold), descriptive.

Amendment before any job (2026-09-23, implementation): a seed utterance that p0 decodes to an empty string cannot be a fit target. Such utterances are dropped from the decphi fit, and the conversion job prints their count and IDs. If more than 1% of the seed (more than 28 of 2849) is empty, the job fails and D14 does not launch.

**Result (W1 and W2 COMPLETED, all arms at ep8; `reports/extract_d14_read_2026-09-24.md`, outputs `output/exp2025_11_06_speech_llms/librispeech/sae_4a_supervised_decphi/`).** Paired rows use 2864 utterances and 33 speakers, 95 % speaker-clustered, 2000 resamples.
- **Fit.** p0's seed decodes score 0.1277 PER against seed gold (2849 utterances). None is empty, so none is dropped.
- **Band.** B_warm = |supphi_k2lat_rep - supphi_k2lat|: 0.0019 at ep4 and 0.0010 at ep8. So M_w = 0.010 at both epochs.
- **Dev-other PER at ep8:**

  | Arm | PER |
  |---|---|
  | decphi_plain | 0.2416 |
  | decphi_frz | 0.2220 |
  | decphi_k2lat | 0.1822 |
  | decphi_k2lat_frz | 0.1728 |
  | supphi_k2lat_frz | 0.1701 |
  | supphi_k2lat_rep | 0.1808 |
  | supphi_k2lat_rp | 0.2180 |
  | decphi_k2shuf | 0.3618 |

  p0 is 0.1894, and the banked supphi_k2lat is 0.1797.
- **Read 1, against p0:**

  | Arm | ep4 | ep8 |
  |---|---|---|
  | decphi_plain | +0.0945 [+0.0874, +0.1016] DEGRADES | +0.0521 [+0.0450, +0.0589] DEGRADES |
  | decphi_frz | +0.0901 [+0.0849, +0.0958] DEGRADES | +0.0326 [+0.0278, +0.0374] DEGRADES |
  | decphi_k2lat | +0.0093 [+0.0030, +0.0150] PRESERVES | -0.0072 [-0.0129, -0.0017] PRESERVES |
  | decphi_k2lat_frz | +0.0126 [+0.0067, +0.0179] DEGRADES | -0.0166 [-0.0220, -0.0114] REFINES |
  | supphi_k2lat_frz | +0.0033 [-0.0024, +0.0084] PRESERVES | -0.0193 [-0.0244, -0.0144] REFINES |

- **Read 2, joint against frozen under the k2 term:**
  - decphi_k2lat_frz - decphi_k2lat: +0.0033 [+0.0009, +0.0058] at ep4, TIE. At ep8, -0.0094 [-0.0113, -0.0073], TIE, just inside M_w.
  - supphi_k2lat_frz - supphi_k2lat_rep: -0.0064 [-0.0079, -0.0047] at ep4, TIE. At ep8, -0.0107 [-0.0129, -0.0084], **FROZEN BETTER**.
- **Read 3, the k2 term:** it **HELPS** at both epochs, frozen or not.
  - decphi_k2lat - decphi_plain: -0.0852 at ep4 and -0.0593 [-0.0629, -0.0560] at ep8.
  - decphi_k2lat_frz - decphi_frz: -0.0776 at ep4 and -0.0492 [-0.0536, -0.0452] at ep8.
- **Read 4:** decphi_k2lat - decphi_k2shuf is -0.1593 at ep4 and -0.1796 [-0.1847, -0.1742] at ep8, so the read is **LEXICON-SPECIFIC**. The shuffled graph degrades the decode start to 0.36.
- **Read 5, label source (descriptive):**
  - decphi_k2lat - supphi_k2lat_rep: +0.0015 [+0.0003, +0.0026] at ep8.
  - decphi_k2lat_frz - supphi_k2lat_frz: +0.0027 [+0.0007, +0.0046] at ep8.
  - A phi fitted on p0's 12.8 %-PER decodes does within 0.003 of the gold phi under the k2 term.
- **Read 6, D10b at ep8 (gold minus decoded / gold minus deranged gold, nats per frame):**
  - p0 with decphi: -0.097 / +4.087.
  - decphi_plain: -0.473 / +4.484.
  - decphi_frz: -0.130 / +4.087.
  - decphi_k2lat: -0.378 / +4.488.
  - decphi_k2lat_frz: -0.140 / +4.087.
- **What it means.**
  - Without the k2 term, the pair drifts away from p0 whether phi is frozen or not.
  - With the k2 term, the pair holds or refines p0, and the gain depends on the lexicon.
  - The reverse model's label source hardly matters at this competence.

## D15: ablating the phone trigram in the k2 arm (user request 2026-09-23, registered before any job; answers State fork 3's arm choice)

User: "ablation of current 3gram phone LM. Do it on one or two most representative k2 arm is enough". Arms chosen: the cold line's best treatment `k2lat_20_ma3000` (the E60 candidate) and the warm-start treatment `supphi_k2lat`, where the term is lexicon-specific (D10e).

**Operationalisation (orchestrator): replace the phone trigram where the word graph carries the text, do not remove it everywhere.** beta(e) = 1 - lam(e) / full_lam, with lam(e) the arm's own k2 ramp. So beta falls as the word graph's weight rises and is 0 wherever the word graph is at full weight. beta scales the phone trigram's emit score in the l_tau lattice DP, and through that DP the rate term's tilted passes (`reports/code_phone_prior_vs_k2_2026-09-23.md`). This follows the one precedent that scales the two models to sum to 1 "to avoid double counting" (`reports/lit_phone_lm_with_word_lm_2026-09-23.md`). Before the on-set nothing changes, so the cold arm equals its beta-1 parent through ep7; removing beta from ep1 on the cold line would leave the pre-on-set lattice with no text prior at all, which is a different experiment. Unchanged, disclosed: the agg term's order-1 / order-2 target from the same prior, and the escape path's unigram price (0.02 % of expected words). The word trigram G keeps the objective at trigram order.
- Warm: `supphi_k2lat_rp` in pack W2 (D14); beta 2/3, 1/3, then 0 from sub-epoch 3.
- Cold, pack C (one exclusive node, pack2 `2j2bZo1TrkC0` constants verbatim, 20 sub-epochs, rung 3000, on-set 8, ramp 3): `k2lat_20_ma3000_rp` and `k2shuf_20_ma3000_rp` (beta 1 through ep7, then 2/3, 1/3, and 0 from ep10); `k2lat_20_ma3000_rep` and `k2shuf_20_ma3000_rep`, verbatim replicates of pack2's two arms, as in-pack references and the rung-3000 identity band.
- The effective beta per sub-epoch is logged in `learning_rates`, and the code review checks that it reaches the DP.

**Bands.** Warm: M_w as in D14. Cold: M_c = max(0.0136, the G4a.9 margin; |k2lat_20_ma3000_rep - k2lat_20_ma3000| at ep20; |k2shuf_20_ma3000_rep - k2shuf_20_ma3000| at ep20), the parents being pack2's arms.

**Reads.** Dev-other greedy PER, `PairedPerDeltaJob`, conventions as D14.
- (a) Warm, ep4 and ep8: `supphi_k2lat_rp - supphi_k2lat_rep`. TRIGRAM NEEDED if delta >= +M_w with the interval above zero; DOUBLE COUNT HURTS if delta <= -M_w with the interval below zero; NOT NEEDED otherwise.
- (b) Cold, ep10 and ep20: `k2lat_20_ma3000_rp - k2lat_20_ma3000_rep`, the same three classes with M_c.
- (c) Cold, ep20, the main-line question: `k2lat_20_ma3000_rp - k2shuf_20_ma3000_rp` <= -M_c with the interval below zero = LEXICON-SPECIFIC ONCE THE TRIGRAM IS REPLACED. The double count masked the lexicon, and the k2 form reopens on the cold line (State fork 1). Otherwise the tie with the null is not the double count's doing. Read beside the beta-1 pair `k2lat_20_ma3000_rep - k2shuf_20_ma3000_rep` (pack2: +0.0044).
- Identity check: `_rp - _rep` at ep4 on the cold pair (identical configs before the on-set) is recorded as a further band sample, not a gate.

**Cost.** W1 and W2 about 1.8 h each (the D10e pack ran 23:13 to 01:00). C about 5 h (pack2's node took 4:58 h). The seed decode and the decphi fit precede W1 and W2.

**Result, cold reads (b) and (c) (pack C `SZO7xTfO9x7Y` COMPLETED, all four arms at ep20).** Sources: `reports/exec_d15_wake_2026-09-23.md` and `reports/extract_d15_cold_2026-09-23.md`; outputs `output/exp2025_11_06_speech_llms/librispeech/sae_4a_lexlat_k2_prior_ablation/`. Paired statistics use 2864 utterances and 33 speakers, 95 % speaker-clustered.
- **Fade.** prior_weight_eff in the `_rp` arms is 1.0 at ep1-7, then 0.667, 0.333, and 0.0 from ep10 on, as registered. The `_rep` arms log no such key; they run the parent's constant beta 1.
- **Band.** M_c = max(0.0136; k2lat_rep - pack2 parent +0.0204 [+0.0173, +0.0237]; k2shuf_rep - pack2 parent +0.0032) = **0.0204**. The identity check at ep4, where the configs are identical before the on-set, reads -0.0174 (k2lat) and +0.0133 (k2shuf). So identical cold configs differ by 0.013-0.020 PER.
- **Dev-other PER at ep20:**

  | Arm | PER |
  |---|---|
  | k2lat_rp | 0.8378 |
  | k2lat_rep | 0.8390 |
  | k2shuf_rp | 0.8449 |
  | k2shuf_rep | 0.8174 |

  The pack2 parents are 0.8186 (k2lat) and 0.8142 (k2shuf). Every arm sits at or inside the chance band 0.83-0.91.
- **(b) k2lat_rp - k2lat_rep: NOT NEEDED at both epochs.**
  - ep10: -0.0124 [-0.0155, -0.0092]
  - ep20: -0.0012 [-0.0040, +0.0014]
  Both are inside M_c. On the cold line, replacing the phone trigram where the word graph carries the text changes nothing measurable.
- **(c) k2lat_rp - k2shuf_rp at ep20: -0.0072 [-0.0095, -0.0047].** The interval is below zero, but the delta is not below -M_c, so the read is **not LEXICON-SPECIFIC**. The tie with the random-pronunciation null is not the double count's doing, and State fork 1 does not reopen through the prior.
  - The beta-1 pair beside it, k2lat_rep - k2shuf_rep, is +0.0216 [+0.0190, +0.0243] (pack2: +0.0044). Its sign flips relative to (c), and both are within the replicate scale.
- **(a) Warm, from D14's W2: TRIGRAM NEEDED at both epochs.** supphi_k2lat_rp - supphi_k2lat_rep is +0.0337 [+0.0305, +0.0372] at ep4 and +0.0372 [+0.0349, +0.0396] at ep8, against M_w = 0.010 (`reports/extract_d14_read_2026-09-24.md`). With a competent phi, fading out the phone trigram costs 0.037 PER. On the cold line (b) it changes nothing. D18 reads follow.

## D16: cross-evaluation of the gold-start and cold pairs (user request 2026-09-23, registered before any job; descriptive, no gate)

User: "a small cross-evaluation [theta_g, theta_c] x [phi_g, phi_c], the same objective and a fixed data subset; theta_g and phi_g are the matched pair after joint training. Do the ckpt loading and evaluate on dev-other as the cancelled plan last round." This is D13's cancelled eval, reduced to the 2x2.

**Checkpoints.** theta_g, phi_g = `supphi_k2lat` ep8 (`PackedBlankfreeTrainJob.WEmvqF2yBFG8`). theta_c, phi_c = `k2lat_20_x60` ep60 (C60, the cold end point, rung 1000, the same graph as supphi_k2lat). Each cell loads theta from one checkpoint and phi from the other; phi must load strictly into the partner's model. theta's own non-phi state (including the agg count EMA) comes with theta.

**Objective.** The training step's own loss code, no parameter update, D13's settings for every cell: dev-other, 2864 utterances, identical batches; temperature 2.0; lam_lex 1 on `cdcxYJMjiYj5` at max_active 1000; lam_agg 0.1; lam_rate 3; prior `RtzbESkOedsT` at beta 1. Each cell reports every term per frame, the weighted total L, and per-utterance values of each term.

**Checks, before any reading.**
- Identity: each diagonal cell run through the eval path on its own run's CV holdout reproduces that run's logged dev l_tau and lexlat_k2 at the checkpoint's sub-epoch within 0.005. A miss voids the read until explained.
- Consistency: lexlat_k2 and agg depend on theta only, so within a row they agree across the two phi columns to float tolerance.

**Reads.** Paired per utterance, speaker-clustered bootstrap over 2000 resamples, on L and per term:
- phi effect at fixed theta: L_gc - L_gg, L_cg - L_cc.
- theta effect at fixed phi: L_cg - L_gg, L_gc - L_cc.
- Interaction I = L_gg + L_cc - L_gc - L_cg. I < 0 means the matched pairs beat the swapped pairs beyond the sum of the component effects (co-adaptation).
An inequality counts only when its interval excludes zero.
- GOLD DOMINATES: theta_g is lower in both phi columns and phi_g is lower in both theta rows. Each gold component is better whichever partner it has, so the cold run found neither.
- TWO BASINS: each theta is lower with its own phi and each phi is lower with its own theta. The pairs are co-adapted local optima.
- MIXED otherwise; report per term.

Amendment before any job (2026-09-23, implementation, `reports/impl_d16_crosseval_2026-09-23.md`):
- The consistency tolerance is 1e-4 nats per frame on each cell's dev-other mean of lexlat_k2 and of agg. As with the identity check, a miss voids the read until it is explained.
- The per-utterance total is L = l_tau + lexlat_k2 + 3 rate. This is D13's sum. agg is batch-level and depends on theta only, so it enters as a point contrast on the theta effects.
- The pairing set is the utterances kept in all four cells; the reader prints their count.
- All cells run at supphi_k2lat's ep8 settings. The eval asserts that tau and lam at k2lat_20_x60's ep60 are the same.

**Result: TWO BASINS (audit CONFIRMED, `reports/audit_d16_crosseval_2026-09-23.md`: the identity side was computed on the GPU, not copied; strict loading of phi; settings asserted; contrasts recomputed independently, positive in all 33 speakers).** Source: `CrossEvalReadJob.NQsiwtafITc6` (`output/report.txt`, `crosseval_read.json`), cells Slurm 1968020-1968025, read in `reports/exec_d16_read_2026-09-23.md`.
- Checks pass. Identity: the diagonal cells reproduce the logged dev values printed to 6 decimals (g ep8: l_tau 1.748996, lexlat_k2 0.247171; c ep60: 1.872530, 0.306184). Consistency: the differences across phi columns are 0 and 5.6e-17. The pairing set is 2864 of 2864 utterances from 33 speakers.
- Per-frame dev-other means, in the order l_tau / agg / rate / lexlat_k2 / total (total = L + 0.1 agg):

  | Cell | l_tau | agg | rate | lexlat_k2 | total |
  |---|---|---|---|---|---|
  | gg | 1.856090 | 0.210296 | 0.033350 | 0.257804 | 2.234974 |
  | gc | 3.419827 | 0.210296 | 0.042647 | 0.257804 | 3.826603 |
  | cg | 3.194092 | 1.264019 | 0.038585 | 0.303347 | 3.739597 |
  | cc | 2.001126 | 1.264019 | 0.030957 | 0.303347 | 2.523745 |

- Paired contrasts on L, with 95% intervals:
  - phi effect: L_gc - L_gg = +1.5923 [+1.5599, +1.6241]; L_cg - L_cc = +1.2197 [+1.1928, +1.2448].
  - theta effect: L_cg - L_gg = +1.4014 [+1.3720, +1.4287]; L_gc - L_cc = +1.4105 [+1.3798, +1.4396].
  - Interaction: I = -2.8119 [-2.8655, -2.7544].

  Every interval excludes zero, and adding agg changes no sign. Per term: l_tau TWO BASINS; rate TWO BASINS; lexlat_k2 MIXED, because it depends on theta only, so it has no phi effect, while theta_g is lower in both columns.
- Reading, as registered: the pairs are co-adapted. Swapping either component alone raises L by 1.2-1.6 nats per frame, so from the cold end point neither single-component swap reaches the gold pair, although the gold pair is lower on the total (D13 agrees). This is the premise `SAE_4A_lexlat_v2.md` acts on.
- Matched-pair gap (user question 2026-09-23; a descriptive follow-up reader, `CrossEvalMatchedGapJob.LHZdl41pticW`, `output/report.txt`; its self-check reproduces L_cg - L_gg to 0). Values are cc - gg, per frame, paired over 2864 utterances, with 95% intervals:
  - L: +0.1818 [+0.1704, +0.1935]; positive for 2620/2864 utterances and 33/33 speakers.
  - l_tau: +0.1473 [+0.1388, +0.1551].
  - lexlat_k2: +0.0437 [+0.0361, +0.0521].
  - rate: -0.0031 [-0.0059, -0.0003].
  - agg (point): +1.054; L + 0.1 agg: +0.287.

  The gold pair is lower by 0.18, while a swap costs 1.2-1.6: the pairs are far apart relative to their gap.
- Scope, per the audit:
  - D16 tests only the discrete 2x2 swap on the objective. It does not test that either pair is a continuous local optimum, reads no PER, and does not show that a phi fitted first escapes.
  - It cannot separate "two basins" from "each phi is fitted to its own theta's output conventions".
  - The reverse model's own score, expected_reverse per retained frame, was not dumped by the cells; it enters only inside l_tau.

**D16r, the reverse model's own score (user question 2026-09-23, "no reverse model term alone?"; registered before any job; descriptive, no gate).**
- Six new cell forwards: D16's cells with D16's checkpoints, settings and batches. Each dumps two extra per-utterance values:
  - rev = expected_reverse / retained frames, the reverse model's posterior-expected segment log score (duration included), exactly as the training monitor `reverse_per_frame` computes it. Higher is better.
  - pri = expected_prior / expected tokens.
- D16's banked jobs must keep their hashes.
- Identity check first: on its run's CV holdout, each diagonal cell reproduces the logged dev `blankfree_reverse_per_frame` at the checkpoint's sub-epoch within 0.005 (g ep8, c ep60). A miss voids the read.
- Reads on rev: paired, speaker-clustered, 2000 resamples, as in D16.
  - phi effect: rev_gc - rev_gg and rev_cg - rev_cc.
  - theta effect: rev_cg - rev_gg and rev_gc - rev_cc.
  - Matched gap: rev_cc - rev_gg.
  - The same D16 labels, with the signs flipped for higher-is-better.
- pri is reported the same way, descriptively.

**Result: rev reads TWO BASINS, like D16 on the objective.** Source: `CrossEvalRevReadJob.fETQJ2fZ3MwJ` (`output/report.txt`), cells Slurm 1973352-1973357, read in `reports/exec_d16r_read_2026-09-23.md`.
- Checks pass. The self-check reproduces D16's l_tau in all six cells to 8.9e-16. Identity: g ep8 -3.160050 against the logged -3.160049; c ep60 -3.362877 against -3.362877. The pairing set is 2864 utterances from 33 speakers.
- Per-frame cell means of rev / pri:
  - gg: -3.3462 / -2.5945
  - gc: -5.1348 / -3.0825
  - cg: -4.4808 / -3.0802
  - cc: -3.5951 / -2.8574
- rev contrasts, with 95% intervals; negative means worse:
  - phi effect: gc - gg = -1.797 [-1.839, -1.754]; cg - cc = -0.889 [-0.923, -0.854].
  - theta effect: cg - gg = -1.144 [-1.179, -1.106]; gc - cc = -1.542 [-1.577, -1.506].
  - Matched gap: cc - gg = -0.255 [-0.273, -0.234].
- pri shows the same pattern (every interval excludes zero): the phi effects are -0.480 and -0.222, the theta effects -0.479 and -0.223, and the matched gap -0.257.
- Reading: the reverse model alone is co-adapted to its own recognizer. Each phi explains the other recognizer's decodes 0.9-1.8 nats per frame worse. The gold pair's phi is better on its own decodes by 0.25, a gap 3.5-7 times smaller than a swap. The swap penalty on l_tau (D16) is carried by the reverse score, not only by the lattice marginal around it.

## D17: a general-knowledge duration prior at the edge (user request 2026-09-23, registered before any job; disclosed label-using diagnostic, no gate)

User: "for things we have good prior knowledge, we should use the prior knowledge. I don't think fixed it would make previous k2 arms suddenly work, but it could really make a difference for model at the edge"; clarification the same day, before any job: "we can use a 'general knowledge' of phoneme length to e.g. init model or do some restriction, but using duration model learned from supervised data is one more step".

**Facts before any job.** phi's duration law is a per-type categorical masked to [2, D_k] (D 25, SIL 50), initialised uniform (E[d] 13.5 for phones). MFA 39-phone mean 4.14 frames on dev-other and 4.22 on train-clean-100; p99.9 19 / 18; 0.019 % / 0.011 % above 25 (`reports/exec_mfa_duration_quantiles_2026-09-23.md`). D10's sup_plain (p0 + the untrained phi) collapsed in sub-epoch 1 (0.846 at ep1), sup_k2lat to 0.691.

**Prior (corrected before any job; the first draft's MFA per-type table is withdrawn by the clarification).** General knowledge only. Every non-SIL type gets the same maximum-entropy law on [2, D_k] with mean m = (50 / rho) x (retained / original frames on the train stream); rho = 9.66/s is the label-free rate (phones per word of the text x the read-speech constant 2.7 words/s, `SAE_4A.md` S3b-R). SIL keeps the bed's uniform init. Modes: `durinit` (phone rows copied into phi's duration logits, trainable) and `durfrz` (phone rows frozen, the restriction; SIL trains). MFA stays a diagnostic reference for E[d]. The same prior serves `SAE_4A_lexlat_v2.md` A9.

**Arms.** One pack, the diagnostic pack's constants and batches verbatim (as D10e), one delta each against its banked uniform counterpart in `5XNGdxndoY1Y`: `sup_durinit`, `sup_durfrz` (sup_plain + prior); `sup_k2lat_durinit`, `sup_k2lat_durfrz` (sup_k2lat + prior).

**Reads.** Dev-other greedy PER at ep1 / 2 / 4 / 8; paired rows at ep1 and ep8 against the uniform counterpart and against p0 (`kL6MFqeFIfCy`); per kept epoch E[d] per type and the emitted non-SIL rate. Band M_e = max(0.03, B_warm at the same epoch), 0.03 being the bed's measured same-config spread. Against the uniform counterpart at ep8: HELPS AT THE EDGE if delta <= -M_e with the interval below zero; HURTS if delta >= +M_e with the interval above zero; NO EFFECT otherwise. D17 initialises no main-line arm; the main-line use is L2-1.

**Cost.** One node, about 1.8 h (as D10e).

**Built (commit e1cd038c, `reports/impl_durprior_2026-09-23.md`).** m = (50 / 9.6619) x 0.852915 (rVAD-retained / original frames) = 4.4138 retained frames per phone (`ReQtJKYpZgsN`); pack `DNrT49EggYTY`. SIL cap, code reading: a SIL token holds at most 50 retained frames and SIL cannot follow SIL; 0.14 % of retained SIL runs exceed 50 (the 4.3 % is on original frames, before rVAD), so the cap is not a live issue. Code review APPROVE_WITH_NOTES (`reports/review_d17_durprior_2026-09-23.md`). Reading caveat: in the durfrz arms the global-norm clip leaves out the frozen phone rows, so durfrz minus uniform measures prior plus freeze, durinit minus uniform the prior alone.

**Result (PER read; pack `DNrT49EggYTY` COMPLETED, all arms ep8; `reports/exec_d17_wake_2026-09-23.md`, outputs `output/exp2025_11_06_speech_llms/librispeech/sae_4a_lexlat_durprior/`).** The prior does not stop the collapse. Every arm falls from p0 (0.1894) to 0.75-0.80 at ep1 and ends at 0.82-0.86 at ep8, near or inside the chance band 0.83-0.91.

Dev-other PER at ep1 / 2 / 4 / 8:

| Arm | ep1 | ep2 | ep4 | ep8 |
|---|---|---|---|---|
| sup_durinit | 0.7544 | 0.8499 | 0.8553 | 0.8551 |
| sup_durfrz | 0.7545 | 0.8488 | 0.8476 | 0.8509 |
| sup_k2lat_durinit | 0.7990 | 0.8603 | 0.8202 | 0.8258 |
| sup_k2lat_durfrz | 0.7990 | 0.8610 | 0.8255 | 0.8159 |

Paired rows against the uniform counterpart (2864 utterances, 33 speakers; negative = the prior arm is better):

| Pair | ep1 | ep8 |
|---|---|---|
| sup_durinit vs sup_plain | -0.0920 [-0.1090, -0.0764] | -0.0363 [-0.0401, -0.0326] |
| sup_durfrz vs sup_plain | -0.0920 [-0.1088, -0.0764] | -0.0406 [-0.0438, -0.0373] |
| sup_k2lat_durinit vs sup_k2lat | +0.1075 [+0.0987, +0.1154] | +0.0119 [+0.0098, +0.0142] |
| sup_k2lat_durfrz vs sup_k2lat | +0.1075 [+0.0987, +0.1153] | +0.0020 [-0.0006, +0.0045] |

Against p0, every arm is worse: +0.565 to +0.610 at ep1 and +0.626 to +0.666 at ep8, with all intervals excluding zero.

- **Classification at ep8, band M_e = max(0.03, B_warm).** B_warm = 0.0010 at ep8 (D14 Result).
  - k2lat arms: **NO EFFECT**. Both deltas are inside the 0.03 floor, and a larger B_warm cannot change that.
  - sup arms: **HELPS AT THE EDGE** (final; B_warm at ep8 = 0.0010 from D14, so M_e = 0.03). The deltas -0.0363 and -0.0406 are both beyond -0.03 with intervals below zero. Both arms still end at 0.85, inside the collapse.
  - Either way, the "help" moves a collapsed arm from about 0.89 to 0.85, inside the chance band 0.83-0.91. It is not a recovery of phonetic content.
- **Durations (report).** At ep8, the phone E[d] mean is 4.61 (3.43-6.02) for durinit and 4.68 (3.27-6.46) for k2lat_durinit, against the prior's 4.41. SIL E[d] falls from 25.8 to 13.6-14.8 in every arm. The non-SIL share of emitted tokens goes from 0.941 to 0.964 (sup arms) and from 0.928 to 0.895-0.899 (k2lat arms).
- **What it means.** Consistent with D10e (the untrained phi drives the collapse): fixing the duration law alone leaves phi's emissions untrained, and they carry the collapse. At ep1 the prior slows the sup arms' collapse (-0.092) and speeds the k2lat arms' collapse (+0.108). By ep8 both effects have mostly washed out.
- The D18 battery (JS, convergence, common-objective loss) follows when D14 finishes.

## D18: beyond-PER diagnostics for D14, D15 and D17 (user instruction 2026-09-23 evening, "audit results to find difference above PER like js divergence or convergence status/loss (e.g. for length controlled arm and trigram ablation)"; registered before any of their reads; descriptive, no gate)

Every D14, D15 and D17 arm, the banked counterparts they are read against (`5XNGdxndoY1Y`: sup_plain, sup_k2lat; supphi_k2lat from `WEmvqF2yBFG8`, the run D14's PER read and D16's gg cell use; pack2's `k2lat_20_ma3000` and `k2shuf_20_ma3000`), and p0. Plain PER keeps its registered role. These reads explain a PER difference or a PER tie; they never decide.

- **B1, convergence.** The run's own per-sub-epoch dev (CV holdout) trajectory of each logged term:
  - the terms: l_tau, lexlat_k2, agg unigram and bigram KL, rate, reverse_per_frame and prior_per_token, plus the weighted training objective;
  - also the train-minus-dev gap.
  - Status at the last kept epoch, from the change of the weighted objective over the last kept interval, against the replicate spread on the same quantity at the same epoch (warm: |supphi_k2lat_rep - supphi_k2lat|; cold: |k2lat_20_ma3000_rep - k2lat_20_ma3000|): STILL DESCENDING if the decrease exceeds the spread, RISING if the increase exceeds it, FLAT otherwise.
- **B2, loss on one common objective.** The D15 `_rp` arms train a different objective from `_rep`, so their logged losses are not comparable. Each arm's final kept checkpoint is evaluated through the D16 eval path on dev-other at D16's settings (beta 1). The D15 arms are evaluated a second time at beta 0. The contrasts are paired per utterance and speaker-clustered (2000 resamples) on L = l_tau + lexlat_k2 + 3 rate and on each term, arm minus the counterpart the PER read uses. agg enters as a point contrast.
- **B3, output distribution.** From the dev-other greedy decodes at each kept epoch:
  - the Jensen-Shannon divergence (log 2) of the decoded phone unigram, and of the bigram, against the gold dev-other phone distribution (analysis only) and against the text prior's;
  - JS between each arm and its counterpart;
  - the number of phone types used, and the SIL share.
  - Differences of JS between an arm and its counterpart get speaker-clustered intervals.
- **B4, duration and rate.** From the same decodes: the mean emitted non-SIL segment length in retained frames (review note: rVAD removes mostly non-phone frames, so a phone's length is about the same in both frame units; converting with the pooled original/retained ratio would read about 17% long), and the emitted non-SIL rate on original frames. For D17 add phi's E[d] per type (already registered). References: prior m 4.41 retained frames; the MFA 4.14 dev-other mean, a diagnostic.

One registered reader job family computes B1, B3 and B4; B2 reuses the D16 cell forward. The launch waits until the source packs' managers finish, so no job runs under two managers.

**Read (2026-09-24; descriptive, no gate; `reports/extract_d18_read_2026-09-24.md`, finish check `reports/exec_d18_finish_2026-09-24.md`).** Readers: BeyondPerReadJob `HjaozIiyUULt`/`TgVm9wCM6kV1`/`z7bFhZ9HCcUu` (B1, B3, B4), CommonObjectiveReadJob `H4yWhTvb82yn`/`NE7p2t7F4pMu`/`akb9aS1PQZO0` (B2; dev-other 2864 utterances, 33 speakers). B2 intervals are 95% speaker-clustered. The replicate pair supphi_k2lat_rep - supphi_k2lat reads L +0.0032 [+0.0021, +0.0042], which sets the noise scale.
- **B1: no arm has converged.** Every D14, D15 and D17 arm reads STILL DESCENDING at its last kept epoch. Warm dJ runs from -0.026 to -0.043 over ep4 to ep8, against a spread of 0.0067; the cold spread is 0.0013. Each ep8 PER is therefore a snapshot of a run that is still moving.
- **B2: the objective and PER disagree on training phi.**
  - Freezing phi lowers PER, yet on the common objective the frozen arm is worse: decphi_k2lat_frz - decphi_k2lat L +0.107 [+0.096, +0.117], and supphi_k2lat_frz - supphi_k2lat_rep +0.120 [+0.107, +0.133]. Almost all of it is l_tau (+0.113 and +0.125); lexlat_k2 ties.
  - So a trained phi lowers the objective by co-adapting to the recognizer, not by a better transcription. This is the same direction as A10 in `SAE_4A_lexlat_v2.md`, where EM phis beat the gold phi on S while their decodes sit at chance.
- **B2: the k2 term and the lexicon agree with PER.** k2lat - plain L -0.038 (lexlat_k2 -0.072, l_tau +0.032), with frz -0.045. k2lat - k2shuf L -0.067 [-0.076, -0.058]. decphi_k2lat - supphi_k2lat_rep L +0.000006 [-0.0022, +0.0022], a tie, matching the PER tie within 0.003.
- **B2: without the trigram, the objective's optimum moves away from the better-PER solution.**
  - Warm rp - rep: +0.040 at beta 1, -0.025 at beta 0, while PER reads 0.2180 against 0.1808.
  - Cold rp - rep: +0.060 at beta 1, -0.160 at beta 0.
  - The beta-0 objective prefers the arm that is worse in PER.
- **B2, D17: duration control lowers L by 0.015-0.030 against plain (sup and sup_k2lat, all intervals exclude 0), while every D17 arm collapses in PER.** The cold band pair (rep vs pack2) ties on L: -0.0047 [-0.0098, +0.0008].
- **B3: every arm uses all 39 types.**
  - Decoded-unigram JS against gold is 0.0015-0.0032 for all but k2shuf, which reads 0.0123.
  - The bigram JS against gold orders as PER does at ep8: decphi_k2lat_frz 0.0175, supphi_k2lat_frz 0.0192, decphi_k2lat 0.0235, decphi_frz 0.0249, decphi_plain 0.0395, decphi_k2shuf 0.0626. p0 reads 0.0231.
- **B4: mean non-SIL segment length is 4.24-4.52 retained frames**, against the prior's 4.41 and MFA's 4.14. k2shuf is longest at 4.935, and p0 reads 4.711. In D17, the durfrz arms' E[d] stays pinned at 4.4138, and the durinit arms drift to 4.61-4.68 at ep8 (per-type range 3.3-6.5).
- Note: BeyondPerReadJob finished before the B2 forwards. By design it computes only B1, B3 and B4, and B2 comes from CommonObjectiveReadJob alone.
