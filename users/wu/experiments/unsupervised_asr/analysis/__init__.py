"""New in the port (package marker, no source counterpart): base analysis readers of phase 4a.

posterior dump (:mod:`.posterior`, :mod:`.posterior_steps`), greedy PER and decode statistics
(:mod:`.per`), derangement / decode gap (:mod:`.gaps`, :mod:`.reverse_score_steps`), paired PER delta
(:mod:`.paired`) and the JSD reads (:mod:`.jsd`).
"""
