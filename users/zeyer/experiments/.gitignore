# While working on dependency_boundary, ignore those files.
# Later, we might want to have them in Git (that was the original idea, maybe need to be discussed further).
_dependency_boundary_autogenerated_cache.*.py
